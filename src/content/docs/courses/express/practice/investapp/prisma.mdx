---
title: "InvestApp: Prisma ORM"
description: "Sexta etapa do InvestApp: migração da camada de dados do SQL escrito à mão para o Prisma, com schema declarativo, migrations versionadas e relações com Category e Broker."
course: express
sidebar:
  label: "7. Prisma ORM"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import ApiRequest from '@components/ApiRequest.astro';
import ApiResponse from '@components/ApiResponse.astro';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import Mermaid from '@components/Mermaid.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Etapa **7 de 13** · Nível **Avançado** · TypeScript · Express.js · Prisma · SQLite

O SQL escrito à mão dá lugar a um schema declarativo. E, com o ORM, o InvestApp ganha as relações que faltavam: cada investimento passa a ter categoria e corretora.

Todo o modelo desta etapa cabe em um único arquivo declarativo: três entidades e duas relações um-para-muitos. O `schema.prisma` que produz esse diagrama é comentado linha a linha no passo 1:

<Mermaid title="Modelo de dados do InvestApp após a introdução das relações">
{`
erDiagram
  CATEGORY ||--o{ INVESTMENT : classifica
  BROKER ||--o{ INVESTMENT : custodia

  INVESTMENT {
    string id PK
    string name
    int value
    string interest
    datetime createdAt
    string categoryId FK
    string brokerId FK
  }
  CATEGORY {
    string id PK
    string name
    string color
  }
  BROKER {
    string id PK
    string name
  }
`}
</Mermaid>

<Aside type="note" title="Aulas conceituais associadas">
  - **ORMs e Relacionamentos**: veja [Prisma ORM no Express](../../../persistence/prisma/) e [Relacionamentos entre Tabelas](../../../persistence/relations/)
</Aside>

## Requisitos, histórias e critérios

**Épico EP01 · Experiência e Carteira** › **Feature FT03 · Classificação por categoria e corretora**

| Requisito | Recorte desta etapa | Estado ao final |
| --------- | ------------------- | --------------- |
| **RNF03** Persistência Relacional | schema declarativo, migrations versionadas e integridade referencial | atendido |
| **RF01** Gestão de Investimentos | investimento passa a ter categoria e corretora | atendido |

### US07 — Enxergar como a carteira está distribuída · RF01, RNF03

> **Como** investidor,
> **quero** classificar cada ativo por categoria e corretora,
> **para** enxergar como o meu patrimônio está distribuído.

```gherkin
Cenário: CA07.1 - Reconstruir o banco via migrations
  Quando rodo npx prisma migrate deploy
  Então a estrutura inteira é recriada no SQLite a partir das migrations

Cenário: CA07.2 - A listagem já traz as relações
  Quando busco os investimentos
  Então cada registro inclui a sua categoria e a sua corretora

Cenário: CA07.3 - Corretora é criada se não existir
  Quando crio um investimento informando uma corretora que ainda não existe
  Então recebo o status 201
  E a corretora passa a existir para os próximos investimentos

Cenário: CA07.4 - Categoria precisa existir
  Quando crio um investimento com um categoryId inexistente
  Então recebo o status 400

Cenário: CA07.5 - O que se versiona é a migration
  Quando inspeciono o repositório
  Então a pasta prisma/migrations está versionada
  Mas o arquivo prisma/dev.db está no gitignore
```

### Tasks da etapa

As tarefas abaixo implementam US07 e são a ordem sugerida de execução. Cada uma tem a sua
seção detalhada logo em seguida.

<Steps>

1. **TK07.1 · Instalar Prisma e criar `prisma/schema.prisma`**: Declaração do schema ORM com tabelas e relacionamentos.
2. **TK07.2 · Substituir `src/database/database.ts`**: Conexão singleton reutilizável do `PrismaClient`.
3. **TK07.3 · Modificar `src/models/Investment.ts`**: Substituição de SQL cru por consultas autocompletadas do ORM.
4. **TK07.4 · Remover `src/database/migration.ts`, `seeders.ts` e `load.ts`**: Remoção da camada SQL manual legada.
5. **TK07.5 · Criar os recursos `Category` e `Broker`**: As duas entidades novas ganham tipo, model, controller, schema e rota.
6. **TK07.6 · Mostrar categoria e corretora no front**: Quatro campos novos no formulário, o `select` alimentado pela API e o cartão com os dados da relação.

</Steps>

## Estrutura da aplicação

A pasta `prisma/` passa a ser o centro de gravidade dos dados: é ali que ficam o schema, o
histórico de migrations e os dados iniciais.

<ProjectLinks path="examples/courses/express/projects/invest-app-prismajs-relation" />

<FileTree>
{`
  invest-app-prismajs-relation/
  ├── .env.example                DATABASE_URL
  ├── package.json
  ├── prisma.config.ts            conexão, migrations e comando de seed
  ├── prisma/
  │   ├── schema.prisma           a fonte única de verdade do modelo
  │   ├── migrations/             histórico versionado, gerado pelo Prisma
  │   └── seeders.json            categorias e corretoras iniciais
  ├── public/                     as quatro telas, servidas por express.static
  └── src/
      ├── index.ts
      ├── database/
      │   ├── database.ts         instância única do PrismaClient
      │   └── seed.ts             substitui o load.ts da etapa 6
      ├── controllers/
      │   ├── investments.controller.ts
      │   ├── categories.controller.ts   novo nesta etapa
      │   └── brokers.controller.ts      novo nesta etapa
      ├── models/
      │   ├── Investment.ts       agora escrito com o Prisma Client
      │   ├── Category.ts         novo nesta etapa
      │   └── Broker.ts           novo nesta etapa
      ├── routes/
      │   ├── investments.routes.ts
      │   ├── categories.routes.ts       novo nesta etapa
      │   └── brokers.routes.ts          novo nesta etapa
      ├── schemas/                um schema Zod por recurso
      └── types/                  um .d.ts por entidade
`}
</FileTree>

## O que muda nesta etapa

Comparando com a etapa anterior, o SQL manual é substituído pelo Prisma ORM. A pasta `prisma/` assume a responsabilidade do esquema declarativo e migrations versionadas. Além disso, a aplicação ganha os modelos `Category` e `Broker`, relacionando investimentos a categorias e corretoras de custódia com integridade referencial.

## Descrição das tarefas

Abaixo estão detalhadas as tarefas de implementação desta etapa, com orientações e trechos de código.

### TK07.1 · Instalar Prisma e criar `prisma/schema.prisma` (Schema declarativo)

```bash title="Terminal: instalação do Prisma ORM e do driver adapter"
npm install prisma @prisma/client @prisma/adapter-better-sqlite3
npx prisma init --datasource-provider sqlite
```

O arquivo abre com dois blocos de configuração: o `generator client` das linhas 4 a 6, que define o que será gerado a partir do schema, e o `datasource db` das linhas 8 a 11, que aponta para o SQLite e lê a string de conexão de `env("DATABASE_URL")` — por isso o `.env` é obrigatório.

O `model Investment` das linhas 12 a 22 é o mesmo investimento das etapas anteriores, agora descrito em vez de criado por SQL. A linha 13 substitui o `AUTOINCREMENT` da etapa 6 por `@id @default(uuid())`, e o `@default(now())` da linha 17 transfere ao banco a responsabilidade pelo `createdAt`.

O trecho destacado nas linhas 18 a 21 é a novidade da etapa. Cada relação aparece em **duas** linhas: o campo escalar `categoryId` (linha 19), que vira a coluna de chave estrangeira no banco, e o campo de relação `category` (linha 18), que só existe no código e é o que permite escrever `investment.category.name`. O `@relation(fields: [categoryId], references: [id])` é o que amarra os dois.

Do outro lado, `Category` e `Broker` (linhas 24 a 35) declaram `investments Investment[]` — a ponta "muitos" da relação. Repare também nos `@unique` das linhas 26, 27 e 33: é essa restrição que torna possível o `connectOrCreate` por nome que aparece no model.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/prisma/schema.prisma"
  title="prisma/schema.prisma"
  showLineNumbers
  mark="18-21"
/>

<Aside type="note" title="No Prisma 7, a conexão sai do schema">
  Até a versão 6, o `datasource` trazia `url = env("DATABASE_URL")` dentro do próprio
  `schema.prisma`. A partir da 7 isso mudou de lugar: o schema descreve **o modelo**, e um
  `prisma.config.ts` na raiz descreve **o ambiente** — conexão, caminho das migrations e
  comando de seed. É também ali que o `.env` é carregado.
</Aside>

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/prisma.config.ts"
  title="prisma.config.ts"
  showLineNumbers
  mark="5,13,16,18"
/>

A linha 5 usa `process.loadEnvFile()`, do próprio Node — não é preciso instalar o carregamento nativo do `.env`. A
linha 16 registra o comando de seed, que na versão 6 morava no `package.json`.

### TK07.2 · Substituir `src/database/database.ts` (Client com driver adapter)

O arquivo que na etapa 6 abria o SQLite à mão e envolvia a API síncrona em promessas encolheu para catorze linhas.

A novidade do Prisma 7 está na linha 9: o client recebe um **driver adapter**. Até a versão 6, o Prisma embarcava um binário próprio para falar com o banco; agora quem conversa com o SQLite é um driver Node comum, e o Prisma se apoia nele. Na prática isso significa instalação mais leve e o mesmo driver que o resto do ecossistema usa.

A linha 12 cria **uma** instância de `PrismaClient` e a linha 14 a exporta; todos os models importam essa mesma instância. A instância única não é detalhe de estilo: cada `new PrismaClient()` abre seu próprio pool de conexões.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/src/database/database.ts"
  title="src/database/database.ts"
  showLineNumbers
  mark="1,9,12,14"
/>

Junto com ele saem o `migration.ts`, o `seeders.ts` e o `load.ts`: o `CREATE TABLE` passa a ser
gerado pelo `prisma migrate` a partir do schema, e a carga inicial vira `src/database/seed.ts`.

### TK07.3 · Modificar `src/models/Investment.ts` (De SQL para ORM)

As cinco funções continuam com os mesmos nomes e as mesmas assinaturas tipadas, exportadas na linha 92. Nenhuma linha de controller precisou mudar. O que desapareceu foram as strings de SQL e o `mapRow`.

O `include` das linhas 8 a 11 é o que substitui o `JOIN` escrito à mão: declarado uma vez, no topo, e reutilizado em todas as consultas — é isso que garante que a entidade saia com o mesmo formato em todas as rotas.

O trecho destacado nas linhas 13 a 37 é o `create` e mostra as duas formas de ligar um registro a outro. Em `category` (linha 31), o `connect` exige que a categoria **já exista** e recebe o id. Em `broker` (linha 33), o `connectOrCreate` procura a corretora pelo nome e, se não achar, cria — só funciona porque `Broker.name` é `@unique` no schema.

O `read` da linha 39 merece uma segunda olhada: a linha 40 reescreve o filtro de nome como `{ contains: ... }`, que é a tradução Prisma do `LIKE '%...%'` que a etapa 6 escrevia à mão. O `update` e o `remove`, nas linhas 55 a 90, repetem o desenho do `create` e estão recolhidos.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/src/models/Investment.ts"
  title="src/models/Investment.ts"
  showLineNumbers
  mark="8-11,13-37"
  collapse="55-90"
/>

### TK07.4 · Substituir os scripts de banco pelo `prisma db seed`

Os dois scripts que criavam a tabela e carregavam os dados na etapa 6 saem do projeto. O `CREATE TABLE` agora é gerado pelo `prisma migrate` a partir do schema, e a carga inicial passa a ser o `src/database/seed.ts`, registrado no `package.json` sob a chave `prisma.seed`.

O seeder ficou mais curto e mais seguro: o `upsert` das linhas 14 a 18 grava a categoria se ela não existir e não faz nada se já existir — rodar o seed duas vezes deixa de ser um erro. O `$disconnect` das linhas 23 e 26 é obrigatório em um script avulso: sem ele, o processo não termina.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/src/database/seed.ts"
  title="src/database/seed.ts"
  showLineNumbers
  mark="13-19,23,26"
/>

<Aside type="caution" title="O `.env` é obrigatório">
  O `prisma.config.ts` lê a conexão de `process.env.DATABASE_URL`. Sem um `.env` na raiz do
  projeto, qualquer comando do Prisma falha antes de tocar no banco — por isso o passo de
  execução começa com `cp .env.example .env`.
</Aside>

<Aside type="tip" title="O caminho do banco mudou de referência">
  Com o driver adapter, `file:./prisma/dev.db` é resolvido a partir de **onde o processo roda**,
  e não mais da pasta do schema. É por isso que o `.env` desta etapa escreve o caminho completo
  a partir da raiz do projeto.
</Aside>

### TK07.5 · Criar os recursos `Category` e `Broker` (Duas entidades, cinco camadas cada)

O `schema.prisma` da TK07.1 declarou `Category` e `Broker`, e o formulário do front passou a ter
um `<select>` de categoria. Falta o que alimenta esse `select`: as duas entidades precisam
existir como recurso HTTP, e cada uma percorre as mesmas cinco camadas que `Investment` já
percorria desde a etapa 3.

São dez arquivos, mas apenas dois padrões — o de `Category` e o de `Broker` são idênticos, com o
nome trocado. Ambas as entidades são **somente leitura** pela API: quem as cria é o `seed.ts` da
TK07.4, e no caso da corretora também o `connectOrCreate` do model de investimento.

| Camada | Category | Broker | O que faz |
| ------ | -------- | ------ | --------- |
| Tipo | `types/Category.d.ts` | `types/Broker.d.ts` | a forma da entidade no TypeScript |
| Model | `models/Category.ts` | `models/Broker.ts` | as consultas ao Prisma, sem HTTP |
| Controller | `controllers/categories.controller.ts` | `controllers/brokers.controller.ts` | traduz a consulta em resposta e erro |
| Schema | `schemas/category.schema.ts` | `schemas/broker.schema.ts` | valida `params` e `query` |
| Rota | `routes/categories.routes.ts` | `routes/brokers.routes.ts` | liga o caminho ao controller |

O model tem duas funções. O `read` da linha 4 aceita um filtro opcional por nome e o converte em
`{ name: { contains: where.name } }` na linha 5 — o mesmo `contains` que o investimento usa. O
`readById` da linha 10 lança `Error` quando não encontra: o model não conhece status HTTP, e é o
controller que traduz isso em 404.

<CodeTabs
  files={[
    { path: 'examples/courses/express/projects/invest-app-prismajs-relation/src/models/Category.ts', label: 'models/Category.ts', mark: '5,13-15' },
    { path: 'examples/courses/express/projects/invest-app-prismajs-relation/src/models/Broker.ts', label: 'models/Broker.ts', mark: '5,13-15' },
  ]}
/>

O controller é a tradução para HTTP. O `read` da linha 6 devolve a lista e o seu `catch` da linha
11 vira `400`; o `readById` da linha 16 transforma o `Error` do model em um `HttpError` de `404`
na linha 20. É a mesma divisão de responsabilidade da etapa 3, agora aplicada a duas entidades
novas.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/src/controllers/categories.controller.ts"
  title="src/controllers/categories.controller.ts"
  showLineNumbers
  mark="10,12,18,20"
/>

O schema e a rota fecham o recurso. O `params` da linha 4 exige um UUID válido e o `query` da
linha 8 recusa um filtro `name` vazio; a rota aplica os dois pelo `validate` das linhas 9 e 10,
que já existia desde a etapa 4. O `.d.ts` é a forma da entidade no TypeScript, e é o que o model
promete devolver.

<CodeTabs
  files={[
    { path: 'examples/courses/express/projects/invest-app-prismajs-relation/src/schemas/category.schema.ts', label: 'category.schema.ts', mark: '4,8' },
    { path: 'examples/courses/express/projects/invest-app-prismajs-relation/src/routes/categories.routes.ts', label: 'categories.routes.ts', mark: '9-10' },
    { path: 'examples/courses/express/projects/invest-app-prismajs-relation/src/types/Category.d.ts', label: 'Category.d.ts' },
  ]}
/>

Por fim, o `src/index.ts` importa os dois roteadores nas linhas 5 e 6 e os monta junto do de
investimentos, sob o mesmo prefixo `/api`. É o que faz o `select` do formulário encontrar
`GET /api/categories`.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/src/index.ts"
  title="src/index.ts"
  showLineNumbers
  mark="5-6"
/>

O contrato acompanha. O `openapi.ts` da etapa 5 ganha os blocos de categoria e de corretora nas
linhas 39 a 69, no mesmo formato do bloco de investimento — os `shapeOf` continuam lendo os
schemas Zod, então a documentação nasce sincronizada com a validação, sem nenhuma descrição
escrita à mão.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/src/docs/openapi.ts"
  title="src/docs/openapi.ts — os recursos novos"
  lines="39-69"
  showLineNumbers
/>

O `requests.http` ganha os blocos correspondentes, e é a forma mais rápida de conferir as rotas
novas sem abrir o navegador.

### TK07.6 · Mostrar categoria e corretora no front

As relações só existem de verdade quando aparecem na tela. O formulário ganha quatro campos e o
cartão ganha quatro informações — e é a primeira vez na trilha que o front pede dados a **duas**
rotas diferentes.

No `index.html`, os campos das linhas 94 a 123 completam o formulário. Três são texto ou data
comuns; o interessante é o `<select>` da linha 107, que nasce **vazio**. Ele não pode ter opções
escritas à mão: as categorias vivem no banco, e o `value` de cada opção precisa ser o `id` que a
API vai receber em `categoryId`.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/public/index.html"
  title="public/index.html — os campos novos"
  lines="94-123"
  showLineNumbers
  mark="105-107"
/>

Quem preenche esse `select` é o `loadCategoriesSelect` das linhas 213 a 223. Ele chama
`GET /api/categories` — a rota da TK07.5 — e insere uma `<option>` por categoria, com o `id` no
`value` e o nome no texto. É por isso que a rota de categoria precisava existir: sem ela, o
formulário não teria o que oferecer.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/public/js/index.js"
  title="public/js/index.js — o select de categorias"
  lines="213-223"
  showLineNumbers
  mark="216,218-220"
/>

O cartão também muda. A `badge` das linhas 34 a 39 pinta a categoria com a cor que veio do banco
— o `style="background-color: ${investment.category.color}"` da linha 36 é o `color` da entidade
`Category` chegando à tela. As linhas 59 a 78 acrescentam taxa, data e corretora, todas vindas do
`include` que o model faz.

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/public/js/index.js"
  title="public/js/index.js — o cartão com a relação"
  lines="32-79"
  showLineNumbers
  mark="34-39,59-78"
/>

Repare que `investment.category.name` e `investment.broker.name` só funcionam porque o model da
TK07.3 pediu `include: { category: true, broker: true }`. Sem o `include`, o Prisma devolveria
apenas `categoryId` e `brokerId`, e o cartão mostraria `undefined`.

## O model, antes e depois

O contrato das funções não muda — `create`, `read`, `readById`, `update` e `remove`, todas com
as mesmas assinaturas tipadas. O corpo, sim: compare a leitura por id nas duas etapas.

<SourceCode
  path="examples/courses/express/projects/invest-app-db-simple/src/models/Investment.ts"
  title="Etapa 6 — SQL escrito à mão"
  lines="55-71"
  showLineNumbers
/>

<SourceCode
  path="examples/courses/express/projects/invest-app-prismajs-relation/src/models/Investment.ts"
  title="Etapa 7 — a mesma leitura com Prisma"
  lines="45-53"
  showLineNumbers
  mark="46"
/>

Duas diferenças cabem na linha 46. A string SQL sumiu: o `findUnique` recebe um objeto e o
Prisma gera a consulta. E o `include` que acompanha traz `category` e `broker` resolvidos na
mesma ida ao banco — na etapa 6, isso teria exigido escrever um `JOIN` com apelidos de coluna e
uma função para remontar os objetos aninhados.

## Migrations

```bash title="Terminal"
npx prisma migrate dev --name init
npx prisma generate
npx prisma studio
```

| Comando                 | Quando usar                                   |
| ----------------------- | --------------------------------------------- |
| `migrate dev`           | ao alterar o schema, em desenvolvimento        |
| `migrate deploy`        | em produção — aplica sem gerar nem resetar     |
| `migrate reset`         | recomeçar do zero (**apaga tudo**)             |
| `db seed`               | popular com os dados iniciais                  |
| `studio`                | inspecionar visualmente                        |

## Executando

<Steps>

1. Entre na pasta desta etapa e instale:

   ```bash title="Terminal"
   cd examples/courses/express/projects/invest-app-prismajs-relation
   npm install
   cp .env.example .env
   ```

2. Crie o banco a partir do schema e popule as categorias:

   ```bash title="Terminal"
   npx prisma migrate dev
   npx prisma db seed
   ```

3. Suba o servidor:

   ```bash title="Terminal"
   npm run dev
   ```

4. Inspecione os dados no navegador:

   ```bash title="Terminal"
   npx prisma studio
   ```

</Steps>

## Testando

A resposta agora traz as entidades relacionadas já resolvidas — o front-end não precisa de
uma segunda chamada:

<ApiRequest method="GET" path="/api/investments" baseUrl="http://localhost:3000" />

<ApiResponse status={200}>
{`
  [
    {
      "id": "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d",
      "name": "Tesouro Selic 2029",
      "value": 20000,
      "interest": "100% Selic",
      "category": { "id": "8f14", "name": "Pós-fixado", "color": "#6366f1" },
      "broker": { "id": "45c4", "name": "Banco Inter" }
    }
  ]
`}
</ApiResponse>

## O diff que importa

```bash title="Terminal"
git diff --no-index -- \
  examples/courses/express/projects/invest-app-db-simple/src/models \
  examples/courses/express/projects/invest-app-prismajs-relation/src/models || true

git diff --no-index -- \
  examples/courses/express/projects/invest-app-db-simple/src/routes.js \
  examples/courses/express/projects/invest-app-prismajs-relation/src/routes.js || true
```

O primeiro diff é grande: os models foram reescritos de ponta a ponta. O segundo é curto — as
rotas continuam chamando `Investment.create`, `Investment.read` e `Investment.readById` com os
mesmos argumentos. É a segunda vez no projeto que a camada de dados é trocada inteira sem que
as camadas acima percebam, e é exatamente isso que a separação em camadas promete.

<Aside type="caution" title="`prisma/dev.db` não vai para o Git">
  O arquivo do banco é binário e muda a cada requisição. O que se versiona é a pasta
  `prisma/migrations/`: ela reconstrói o banco inteiro em qualquer máquina com
  `npx prisma migrate dev`.
</Aside>

## Conceitos abordados

- Schema declarativo e geração do client tipado
- Migrations versionadas e o histórico em `prisma/migrations/`
- Instância única do `PrismaClient` com driver adapter (Prisma 7)
- `prisma.config.ts` separando o modelo do ambiente
- Relações um-para-muitos, `connect`, `connectOrCreate` e `include`
- Carga inicial de dados com `prisma db seed`

<Aside type="tip">
  As aulas correspondentes são [Prisma](../../../persistence/prisma/),
  [CRUD com Prisma](../../../persistence/crud/) e
  [Relações com Prisma](../../../persistence/relations/).
</Aside>

## Próxima etapa

[InvestApp: Cadastro de Usuário](../user/) — o model `User` e o hash de senha.
