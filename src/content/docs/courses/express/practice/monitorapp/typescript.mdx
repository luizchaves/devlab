---
title: "MonitorApp: TypeScript em camadas"
description: "Terceira etapa do MonitorApp: migração da API para TypeScript com separação em rotas, controllers e models, tipos do domínio e tratamento de erros centralizado."
course: express
sidebar:
  label: "3. TypeScript em camadas"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import Mermaid from '@components/Mermaid.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Etapa **3 de 13** · Nível **Intermediário** · TypeScript · Arquitetura em camadas

O `routes.js` da etapa 2 fazia tudo: lia a requisição, validava campo, mexia no array e montava a
resposta. Funciona com cinco rotas e um recurso. A partir da etapa 7 serão quatro recursos e
quatro entidades — e é agora, antes de o domínio crescer, que o arquivo único se divide em
camadas e ganha tipos.

O comportamento visível **não muda**: os mesmos caminhos, os mesmos status codes, o mesmo front.
O que muda é onde cada decisão mora.

<Aside type="note" title="Aulas conceituais associadas">
  - **TypeScript e camadas**: veja [TypeScript no Express](../../../basics/typescript/) e [Arquitetura MVC](../../../architecture/mvc/)
  - **Erros**: veja [Tratamento de Erros](../../../api/error-handling/)
</Aside>

## Requisitos, histórias e critérios

**Épico EP04 · Fundação Técnica** › **Feature FT09 · Arquitetura em camadas e tipos**

| Requisito | Recorte desta etapa | Estado ao final |
| --------- | ------------------- | --------------- |
| **RNF06** Manutenibilidade & Tipagem | tipos do domínio, camadas e erro centralizado | atendido |
| **RF01** Gestão de Hosts | o mesmo CRUD, agora tipado | inalterado por fora |

### US03 — Mudar o código sem medo · RNF06

> **Como** pessoa desenvolvedora,
> **quero** tipos estáticos e responsabilidades separadas por camada,
> **para** alterar uma parte do sistema sem quebrar as outras por engano.

```gherkin
Cenário: CA03.1 - O projeto verifica os tipos
  Quando executo npm run typecheck
  Então o comando termina sem nenhum erro

Cenário: CA03.2 - O servidor é todo TypeScript
  Quando procuro arquivos .js dentro de back/src
  Então não encontro nenhum

Cenário: CA03.3 - O roteador só declara rotas
  Dado o arquivo hosts.routes.ts
  Então ele contém apenas caminho, middlewares e handler
  E nenhuma regra de negócio

Cenário: CA03.4 - O model não conhece HTTP
  Dado o arquivo models/Host.ts
  Então ele não importa Request nem Response
  E lança Error comum, não HttpError

Cenário: CA03.5 - Todo erro passa pelo mesmo lugar
  Quando uma rota lança HttpError
  Então o errorHandler registrado no fim da cadeia responde com err.code

Cenário: CA03.6 - O contrato HTTP não mudou
  Quando repito as requisições do requests.http da etapa 2
  Então recebo exatamente os mesmos status codes
```

### Tasks da etapa

<Steps>

1. **TK03.1 · Criar `tsconfig.json`**: modo `strict`, execução direta de `.ts` e o alias `@/*`.
2. **TK03.2 · Converter o entrypoint para `src/index.ts`**: o app registra as camadas em ordem.
3. **TK03.3 · Declarar os tipos em `src/types/Host.d.ts`**: o formato do domínio, uma vez só.
4. **TK03.4 · Criar `src/models/Host.ts`**: a regra de negócio, assíncrona e sem HTTP.
5. **TK03.5 · Criar `src/controllers/hosts.controller.ts`**: a tradução entre HTTP e model.
6. **TK03.6 · Criar `src/errors/HttpError.ts` e os manipuladores de erro**.
7. **TK03.7 · Criar `src/routes/hosts.routes.ts`**: só o mapa de caminhos.

</Steps>

## Estrutura da aplicação

O `back/` deixa de ser três arquivos e passa a ser sete pastas, uma por responsabilidade. O nome
de cada pasta é a pergunta que ela responde: `routes/` diz **onde**, `controllers/` diz **como
entra e sai por HTTP**, `models/` diz **o que a regra faz**, e `types/` diz **qual é a forma do
dado**. O `front/` não é tocado.

<ProjectLinks path="examples/courses/express/projects/monitor-app-typescript" />

<FileTree>
{`
  monitor-app-typescript/
  ├── back/
  │   ├── package.json          tsx, typescript e os @types
  │   ├── tsconfig.json         strict e o alias @/*
  │   ├── requests.http
  │   └── src/
  │       ├── index.ts          monta o app e registra as camadas
  │       ├── controllers/
  │       │   └── hosts.controller.ts    HTTP entra e sai aqui
  │       ├── data/
  │       │   └── hosts.ts               o array, agora tipado
  │       ├── errors/
  │       │   └── HttpError.ts           erro com status embutido
  │       ├── middlewares/
  │       │   ├── errorHandlers.ts       404 e o tratamento final
  │       │   └── requireJsonContentType.ts
  │       ├── models/
  │       │   └── Host.ts                a regra, sem HTTP
  │       ├── routes/
  │       │   └── hosts.routes.ts        só o mapa de caminhos
  │       └── types/
  │           └── Host.d.ts              o formato do domínio
  └── front/                    inalterado nesta etapa
`}
</FileTree>

O diagrama a seguir percorre uma requisição por essas pastas, na ordem em que o Express as
executa. As setas cheias são o caminho de sucesso; as pontilhadas, o de erro.

<Mermaid title="O caminho de uma requisição pelas camadas">
{`
flowchart LR
  R["routes/<br/>caminho + middlewares"] --> C["controllers/<br/>lê req, monta res"]
  C --> M["models/<br/>regra de negócio"]
  M --> D[("data/<br/>array em memória")]
  C -. "throw HttpError" .-> E["middlewares/<br/>errorHandler"]
  M -. "throw Error" .-> C
`}
</Mermaid>

A seta pontilhada de baixo é a regra que sustenta o desenho: o **model lança `Error` comum**, e
quem traduz isso em status HTTP é o **controller**. É por isso que o mesmo `models/Host.ts` vai
sobreviver à troca do array por SQLite (etapa 6) e por Prisma (etapa 7) sem que nenhuma rota
mude.

## O que muda nesta etapa

| Etapa 2 (`monitor-app-api`) | Etapa 3 (`monitor-app-typescript`) |
| --------------------------- | ---------------------------------- |
| `src/routes.js` com tudo dentro | sete arquivos, um por responsabilidade |
| `HttpError` declarado no meio do arquivo de rotas | `errors/HttpError.ts`, importado por quem precisa |
| `if (!name \|\| !address)` dentro da rota | a mesma checagem no model, e o `try`/`catch` no controller |
| manipulador de erro registrado no `router` | `notFoundHandler` e `errorHandler` registrados no app |
| `node --watch src/index.js` | `tsx watch src/index.ts` |
| nenhuma verificação estática | `npm run typecheck` |

## Descrição das tarefas

### TK03.1 · Criar `tsconfig.json` (Configuração do TypeScript)

```bash title="Terminal: dentro de back/"
npm install tsx
npm install -D typescript @types/node @types/express @types/morgan @types/cors
```

Três opções deste arquivo merecem atenção. A `"strict": true` da linha 8 é a que dá valor ao
resto: sem ela, `undefined` passaria despercebido em metade das funções. A
`"allowImportingTsExtensions": true` da linha 13 permite escrever `import … from './x.ts'` com a
extensão real do arquivo — é o que o Node moderno espera, e o que o `tsx` executa direto, sem
passo de build. E o bloco `paths` da linha 15 cria o alias `@/*`, que evita a escada de `../../`
entre camadas.

O `"noEmit": true` deixa explícito o papel do `tsc` aqui: ele **verifica**, não compila. Quem
executa é o `tsx`, e é por isso que o script `dev` continua tão curto quanto era com `node --watch`.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/tsconfig.json"
  title="back/tsconfig.json"
  showLineNumbers
  mark="8,13,15-17"
/>

### TK03.2 · Converter o entrypoint para `src/index.ts` (Montagem do app)

O arquivo ficou mais curto que o da etapa 2, e a ordem dos `app.use` é o que ele comunica: os
middlewares gerais primeiro, as rotas no meio, e os dois manipuladores de erro **por último**.

Essa posição não é estética. O `notFoundHandler` só pode responder depois que todas as rotas
tiveram a chance de casar com o caminho, e o `errorHandler` é reconhecido pelo Express pela sua
assinatura de quatro parâmetros — ele precisa ser o último elo da cadeia para receber o que os
outros lançaram.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/index.ts"
  title="back/src/index.ts"
  showLineNumbers
  mark="16-20"
/>

### TK03.3 · Declarar os tipos em `src/types/Host.d.ts` (O formato do domínio)

Dois tipos, e a diferença entre eles é o ponto da tarefa. `Host` é o recurso **completo**, como
ele existe depois de criado: todos os campos obrigatórios, `id` incluído. `HostInput` é o que
**chega pela requisição**: sem `id`, e com tudo opcional, porque o corpo de uma requisição é
território hostil até que alguém verifique.

Separar os dois evita o erro mais comum desta camada — aceitar um objeto pela metade em uma
função que promete devolver um recurso inteiro.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/types/Host.d.ts"
  title="back/src/types/Host.d.ts"
  showLineNumbers
/>

### TK03.4 · Criar `src/models/Host.ts` (A regra de negócio)

O model concentra as cinco operações e nada mais. Repare no que ele **não** importa: não há
`Request`, não há `Response`, não há `HttpError`. Ele lança `Error` comum — linhas 8, 30 e 46,
entre outras — e cabe ao controller decidir se aquilo é um `400` ou um `404`.

As funções são `async` mesmo manipulando um array em memória, o que hoje é desnecessário. É uma
escolha deliberada: da etapa 6 em diante toda operação passa a ser realmente assíncrona, e a
assinatura já está pronta. Nenhum controller vai precisar mudar.

O `create` das linhas 6 a 16 é o modelo do arquivo: valida o mínimo, gera o `id` com
`randomUUID()` e devolve o objeto tipado como `Host`. As demais operações repetem o padrão de
localizar por índice e falhar quando não encontram, e por isso aparecem recolhidas.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/models/Host.ts"
  title="back/src/models/Host.ts"
  showLineNumbers
  mark="6-16"
  collapse="18-64"
/>

### TK03.5 · Criar `src/controllers/hosts.controller.ts` (A camada HTTP)

O controller é a única camada que fala HTTP. Cada função segue a mesma forma em três passos: lê o
que precisa da requisição, chama o model, escolhe o status da resposta.

O `create` das linhas 7 a 17 mostra o padrão inteiro. A linha 9 faz o *cast* de `req.body` para
`HostInput` — nesta etapa é apenas uma promessa ao compilador, e é exatamente essa promessa não
verificada que a etapa 4 vai substituir por validação de verdade. A linha 15 converte qualquer
falha do model em `HttpError('Unable to create host', 400)`.

O contraste entre o `create` e o `readById` das linhas 31 a 41 é o que justifica esta camada
existir: o model lança o mesmo tipo de erro nos dois casos, e é aqui que ele vira `400` em um e
`404` no outro.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/controllers/hosts.controller.ts"
  title="back/src/controllers/hosts.controller.ts"
  showLineNumbers
  mark="7-17,31-41"
  collapse="43-66"
/>

### TK03.6 · Criar `src/errors/HttpError.ts` e os manipuladores de erro

O `HttpError` é a peça que liga as duas camadas anteriores: uma subclasse de `Error` com um campo
`code`. Nada mais.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/errors/HttpError.ts"
  title="back/src/errors/HttpError.ts"
  showLineNumbers
/>

O `errorHandlers.ts` traduz. As linhas 10 a 12 tratam o caso mais sutil: quando o corpo chega com
JSON malformado, quem lança é o próprio `express.json()`, e o erro é um `SyntaxError` — sem esse
caso explícito, um JSON com vírgula sobrando viraria `500` em vez de `400`. As linhas 14 a 16
convertem o `HttpError` no status guardado, e o final do arquivo transforma qualquer outra
exceção em `500`, registrando o `stack` no log mas nunca no corpo da resposta.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/middlewares/errorHandlers.ts"
  title="back/src/middlewares/errorHandlers.ts"
  showLineNumbers
  mark="10-16"
/>

O `requireJsonContentType.ts` é o segundo middleware da etapa, e existe para o mesmo tipo de
falha: um cliente que envia corpo sem declarar `Content-Type: application/json` receberia
`req.body` vazio e um `400` confuso. Aqui a recusa é explícita.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/middlewares/requireJsonContentType.ts"
  title="back/src/middlewares/requireJsonContentType.ts"
  showLineNumbers
/>

### TK03.7 · Criar `src/routes/hosts.routes.ts` (O mapa de caminhos)

O resultado das seis tarefas anteriores cabe em cinco linhas legíveis. Este arquivo é hoje a
melhor documentação do recurso: método, caminho, middlewares e handler, na ordem em que rodam.

Ele vai crescer duas vezes no projeto — na etapa 4, quando o `validate(schema)` entrar antes do
handler, e na etapa 10, quando o `isAuthenticated` entrar antes de tudo. Nas duas vezes a
mudança será só de composição.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/routes/hosts.routes.ts"
  title="back/src/routes/hosts.routes.ts"
  showLineNumbers
  mark="8-12"
/>

O array em memória acompanha a migração: o `hosts.ts` declara `Host[]` na linha 3, e é esse tipo
que as cinco funções do model prometem devolver.

<SourceCode
  path="examples/courses/express/projects/monitor-app-typescript/back/src/data/hosts.ts"
  title="back/src/data/hosts.ts"
  showLineNumbers
  mark="3"
/>

{/* cobertura: back/package.json — acrescenta typescript, tsx e os @types, instalados no comando
    da primeira task */}

## Executando

<Steps>

1. Instale as dependências novas:

   ```bash title="Terminal 1"
   cd examples/courses/express/projects/monitor-app-typescript/back
   npm install
   ```

2. Verifique os tipos e suba a API:

   ```bash title="Terminal 1"
   npm run typecheck
   npm run dev
   ```

3. Suba o front, que não mudou:

   ```bash title="Terminal 2"
   cd examples/courses/express/projects/monitor-app-typescript/front
   npm install
   npm run dev
   ```

</Steps>

<Aside type="tip" title="Compare com a etapa anterior">
  ```bash title="Terminal"
  git diff --no-index -- \
    examples/courses/express/projects/monitor-app-api/back \
    examples/courses/express/projects/monitor-app-typescript/back || true
  ```

  A pasta `front/` não aparece no diff: o contrato HTTP não mudou, então a tela nem percebeu a
  refatoração. É o melhor indicador de que as camadas ficaram no lugar certo.
</Aside>

## Conceitos abordados

- `tsx` executando TypeScript sem passo de build
- Modo `strict` e o alias de import `@/*`
- Separação em router, controller e model
- Tipo do recurso completo versus tipo da entrada
- `HttpError` e o manipulador de erros no fim da cadeia
- Model assíncrono desde antes de precisar ser

## Próxima etapa

[MonitorApp: Validação](../validation/) — schemas Zod para corpo, query e parâmetros, e a regra
que define o que é um endereço válido.
