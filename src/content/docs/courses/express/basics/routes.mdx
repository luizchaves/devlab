---
title: "Express.js: Rotas"
description: Organizando as rotas em módulos com express.Router().
course: express
project: express-router
sidebar:
  label: "Rotas"
---
import { Aside } from '@astrojs/starlight/components';
import ApiRequest from '@components/ApiRequest.astro';
import ApiResponse from '@components/ApiResponse.astro';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import SourceCode from '@components/SourceCode.astro';

Nesta aula vamos aprender como organizar as rotas. Um arquivo único funciona bem com
três rotas — com trinta, vira um problema.

## Objetivo

Extrair as rotas de `app.js` para módulos independentes usando `express.Router()`, e
separar a **montagem da aplicação** da **subida do servidor**.

## Conceitos

- `express.Router()` cria um mini-aplicativo com suas próprias rotas.
- `app.use('/users', userRouter)` monta esse router sob um **prefixo**.
- Dentro do router, os caminhos são relativos ao prefixo: `/` vira `/users`.
- `app.js` exporta a aplicação; `server.js` apenas a coloca para escutar uma porta.

## Estrutura de diretórios

<FileTree>
{`
  express-router/
  ├── package.json
  └── src/
      ├── app.js
      ├── server.js
      └── routes/
          └── user-router.js
`}
</FileTree>

## O código

<CodeTabs
  syncKey="express-router"
  files={[
    { path: 'examples/express/projects/router/src/app.js', title: 'src/app.js', showLineNumbers: true, mark: '8' },
    { path: 'examples/express/projects/router/src/routes/user-router.js', title: 'src/routes/user-router.js', showLineNumbers: true, mark: ['router.get'] },
    { path: 'examples/express/projects/router/src/server.js', title: 'src/server.js', showLineNumbers: true },
  ]}
/>

<Aside type="tip" title="Por que separar `app.js` de `server.js`?">
  Porque `app` sem `listen` é testável: ferramentas de teste importam o `app` e fazem
  requisições sem ocupar nenhuma porta. `server.js` fica com a única
  responsabilidade de subir o processo.
</Aside>

## Do arquivo único para o router

A mudança em `app.js` é pequena, mas muda a arquitetura:

```js title="src/app.js" del={2-4} ins={6}
// antes
app.get('/users', (req, res) => {
  res.json(users);
});

app.use('/users', userRouter);
```

## Parâmetros de rota

Dentro do router, `/:id` já está sob o prefixo — o caminho final é `/users/:id`:

```js title="src/routes/user-router.js" {2}
router.get('/:id', (req, res) => {
  const user = users.find((item) => item.id === Number(req.params.id));

  if (!user) {
    return res.status(404).json({ message: 'Usuário não encontrado' });
  }

  res.json(user);
});
```

<Aside type="caution" title="`req.params` é sempre string">
  `/users/2` entrega `req.params.id === '2'`. Sem o `Number()`, a comparação com
  `item.id === 2` falha silenciosamente.
</Aside>

## Parâmetros de Requisição

### Tipos de Parâmetros

![](/lp2/imgs/expressjs/params/flow.png)

Parâmetros:

- Query Param
- Route Param
- Body Param

### Query Param

![](/lp2/imgs/expressjs/bmi-query-param/flow.png)

Estrutura do código:

<FileTree>
{`
bmi-query-param/
├── src/
│   ├── bmi.js
│   └── index.js
├── .gitignore
├── package-lock.json
├── package.json
└── requests.http
`}
</FileTree>

<SourceCode path="examples/express/lp2/bmi-query-param/src/bmi.js" />

<SourceCode path="examples/express/lp2/bmi-query-param/src/index.js" mark="7,8" />

<SourceCode path="examples/express/lp2/bmi-query-param/requests.http" />

### Route Param

![](/lp2/imgs/expressjs/bmi-route-param/flow.png)

Estrutura do código:

<FileTree>
{`
bmi-route-param/
├── src/
│   ├── bmi.js
│   └── index.js
├── .gitignore
├── package-lock.json
├── package.json
└── requests.http
`}
</FileTree>

<SourceCode path="examples/express/lp2/bmi-route-param/src/index.js" mark="7,8" />

<SourceCode path="examples/express/lp2/bmi-route-param/requests.http" />

### Body Param

![](/lp2/imgs/expressjs/bmi-body-param/flow.png)

Estrutura do código:

<FileTree>
{`
bmi-body-param/
├── src/
│   ├── bmi.js
│   └── index.js
├── .gitignore
├── package-lock.json
├── package.json
└── requests.http
`}
</FileTree>

<SourceCode path="examples/express/lp2/bmi-body-param/src/index.js" mark="6,9" />

<SourceCode path="examples/express/lp2/bmi-body-param/requests.http" />

### Resumo das Rotas

| Tipo        | Node.js      | HTTP | Rota                                 |
| ----------- | ------------ | ---- | ------------------------------------ |
| Query Param | `req.query`  | GET  | `/bmi?weight=x&height=y`             |
| Route Param | `req.params` | GET  | `/bmi/weight/:weight/height/:height` |
| Body Param  | `req.body`   | POST | `/bmi`                               |

| Rota                                 | Dados                              |
| ------------------------------------ | ---------------------------------- |
| `/bmi?weight=x&height=y`             | `?weight=100&height=1.5`           |
| `/bmi/weight/:weight/height/:height` | `/bmi/weight/100/height/1.5`       |
| `/bmi`                               | `{ "weight": 100, "height": 1.5 }` |


## Testando

<ApiRequest method="GET" path="/users" baseUrl="http://localhost:3000" />

<ApiResponse status={200}>
{`
  [
    { "id": 1, "name": "Ana" },
    { "id": 2, "name": "Bruno" }
  ]
`}
</ApiResponse>

<ApiRequest method="GET" path="/users/99" baseUrl="http://localhost:3000" />

<ApiResponse status={404}>
{`
  {
    "message": "Usuário não encontrado"
  }
`}
</ApiResponse>

## Executando

```bash
cd examples/express/projects/router
pnpm install
pnpm dev
```

## Exercício

Crie um `product-router.js` com as rotas `GET /products` e `GET /products/:id`,
monte-o em `app.js` sob o prefixo `/products` e teste as duas rotas.

## Próxima aula

[Controllers](../controllers/) — tirando a lógica de dentro das rotas.

## Referências

- [Routing Guide | Express](https://expressjs.com/en/guide/routing.html)
