---
title: "Introdução ao Express.js"
description: "Introdução ao Express.js - material da disciplina Linguagem de Programação II."
course: ctii-jp-lp2
sidebar:
  label: "Express.js"
  order: 5
---
import SourceCode from '@components/SourceCode.astro';
import FileTree from '@components/FileTree.astro';

## Definição

[Site Express](http://expressjs.com/):

> Express is a minimal and flexible Node.js web application framework that provides a robust set of features for web and mobile applications.

## Características

- Rápido
- Minimalista
- Flexível
- Não opinativo
- Usado para montar APIs
- Alguns frameworks são baseados no Express

## Exemplo de Express.js

<FileTree>
{`
hello-simple/
├── src/
│   └── index.js
├── .gitignore
├── package.json
└── requests.http
`}
</FileTree>

Fluxo da aplicação hello-simple:

![](/lp2/imgs/expressjs/hello-simple/flow.png)

Código da aplicação hello-simple:

<SourceCode path="examples/lp2/codes/expressjs/hello-simple/src/index.js" />

Para a criação do arquivo `package.json` use este comando no terminal:

```sh
$ npm init -y
```

Adição do pacote [express.js](https://www.npmjs.com/package/express) como dependência do projeto e dos scripts de execução do projeto:

```sh
$ npm install express
```

<SourceCode path="examples/lp2/codes/expressjs/hello-simple/package.json" mark="8,9,15" />

Para executar o projeto use algum destes comandos:

```sh
$ node src/index.js
```

```sh
$ npm start
```

Já a execução no modo watch:

```sh
$ npm run dev
```

Depende da versão do node.js, o modo watch precisa de um pacoto como o [nodemon](https://www.npmjs.com/package/nodemon) para funcionar:

```js
 "scripts": {
    ...
    "dev": "nodemon src/index.js"
  },
```

```sh
$ npm install nodemon -D
$ npx nodemon src/index.js
$ npm run dev
```

Já para testar as requisições use o arquivo `requests.http` do plugin [Rest Client](https://marketplace.visualstudio.com/items?itemName=humao.rest-client) do VSCode:

<SourceCode path="examples/lp2/codes/expressjs/hello-simple/requests.http" />

![](/lp2/imgs/expressjs/hello-simple/response.png)

> Cuidado no git para não versionar a pasta `node_modules/`, use o arquivo `.gitignore` para desconsiderar a pasta.

```txt title="codes/expressjs/hello-simple/.gitignore"
node_modules
```

## Rotas da Aplicação

Rotas:

- `/hello/en` => `Hello World!`
- `/hello/pt` => `Olá Mundo!`

![](/lp2/imgs/expressjs/hello-lang/flow.png)

Estrutura do código:

<FileTree>
{`
hello-lang/
├── src/
│   └── index.js
├── .gitignore
├── package-lock.json
├── package.json
└── requests.http
`}
</FileTree>

<SourceCode path="examples/lp2/codes/expressjs/hello-lang/src/index.js" />

<SourceCode path="examples/lp2/codes/expressjs/hello-lang/requests.http" />

## Parâmetros de Requisição

### Tipos de Parâmetros

![](/lp2/imgs/expressjs/params/flow.png)

Parâmetros:

- Query Param
- Route Param
- Body Param

### Query Param

![](/lp2/imgs/expressjs/bmi-query-param/flow.png)

Estrutura do código:

<FileTree>
{`
bmi-query-param/
├── src/
│   ├── bmi.js
│   └── index.js
├── .gitignore
├── package-lock.json
├── package.json
└── requests.http
`}
</FileTree>

<SourceCode path="examples/lp2/codes/expressjs/bmi-query-param/src/bmi.js" />

<SourceCode path="examples/lp2/codes/expressjs/bmi-query-param/src/index.js" mark="7,8" />

<SourceCode path="examples/lp2/codes/expressjs/bmi-query-param/requests.http" />

### Route Param

![](/lp2/imgs/expressjs/bmi-route-param/flow.png)

Estrutura do código:

<FileTree>
{`
bmi-route-param/
├── src/
│   ├── bmi.js
│   └── index.js
├── .gitignore
├── package-lock.json
├── package.json
└── requests.http
`}
</FileTree>

<SourceCode path="examples/lp2/codes/expressjs/bmi-route-param/src/index.js" mark="7,8" />

<SourceCode path="examples/lp2/codes/expressjs/bmi-route-param/requests.http" />

### Body Param

![](/lp2/imgs/expressjs/bmi-body-param/flow.png)

Estrutura do código:

<FileTree>
{`
bmi-body-param/
├── src/
│   ├── bmi.js
│   └── index.js
├── .gitignore
├── package-lock.json
├── package.json
└── requests.http
`}
</FileTree>

<SourceCode path="examples/lp2/codes/expressjs/bmi-body-param/src/index.js" mark="6,9" />

<SourceCode path="examples/lp2/codes/expressjs/bmi-body-param/requests.http" />

### Resumo das Rotas

| Tipo        | Node.js      | HTTP | Rota                                 |
| ----------- | ------------ | ---- | ------------------------------------ |
| Query Param | `req.query`  | GET  | `/bmi?weight=x&height=y`             |
| Route Param | `req.params` | GET  | `/bmi/weight/:weight/height/:height` |
| Body Param  | `req.body`   | POST | `/bmi`                               |

| Rota                                 | Dados                              |
| ------------------------------------ | ---------------------------------- |
| `/bmi?weight=x&height=y`             | `?weight=100&height=1.5`           |
| `/bmi/weight/:weight/height/:height` | `/bmi/weight/100/height/1.5`       |
| `/bmi`                               | `{ "weight": 100, "height": 1.5 }` |

## Referências

- [Express](https://expressjs.com/)
  - [Guide](https://expressjs.com/en/guide/routing.html)
  - [API](https://expressjs.com/en/4x/api.html)
