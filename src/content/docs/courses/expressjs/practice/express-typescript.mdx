---
title: "Projeto: Express TypeScript"
description: "API de usuários em TypeScript executada nativamente pelo Node, com camadas, tipos do domínio e erros centralizados."
course: expressjs
sidebar:
  label: "Express TypeScript"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import ApiRequest from '@components/ApiRequest.astro';
import ApiResponse from '@components/ApiResponse.astro';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Nível **Intermediário** · Node.js · Express.js · TypeScript

O mesmo CRUD do projeto MVC, migrado para TypeScript — sem passo de build, sem `tsx` e sem
bundler.

<ProjectLinks path="examples/courses/express/projects/typescript" devcontainer="express-typescript" />

## Estrutura

<FileTree>
{`
  express-typescript/
  ├── package.json             imports "#*" e script typecheck
  ├── tsconfig.json
  ├── requests.http
  └── src/
      ├── app.ts               monta a aplicação
      ├── server.ts            sobe o servidor
      ├── controllers/
      │   └── user-controller.ts
      ├── errors/
      │   └── HttpError.ts
      ├── middlewares/
      │   ├── error-handler.ts
      │   └── require-json.ts
      ├── models/
      │   └── user-model.ts
      ├── routes/
      │   └── user-router.ts
      └── types/
          └── user.ts
`}
</FileTree>

## Configuração

O `tsconfig.json` é ajustado para o Node executar `.ts` diretamente — `noEmit`, extensão
`.ts` nos imports e recusa de sintaxe não apagável:

<SourceCode
  path="examples/courses/express/projects/typescript/tsconfig.json"
  title="tsconfig.json"
  showLineNumbers
  mark="4,10-13"
/>

O alias `#` é declarado no `package.json`, e não no `tsconfig.json`: assim o Node também o
entende, sem loader nem bundler.

<SourceCode
  path="examples/courses/express/projects/typescript/package.json"
  title="package.json"
  showLineNumbers
  mark="7-14"
/>

## Código

<CodeTabs
  syncKey="express-typescript"
  files={[
    { path: 'examples/courses/express/projects/typescript/src/app.ts', title: 'src/app.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/typescript/src/routes/user-router.ts', title: 'src/routes/user-router.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/typescript/src/controllers/user-controller.ts', title: 'src/controllers/user-controller.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/typescript/src/models/user-model.ts', title: 'src/models/user-model.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/typescript/src/types/user.ts', title: 'src/types/user.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/typescript/src/errors/HttpError.ts', title: 'src/errors/HttpError.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/typescript/src/middlewares/error-handler.ts', title: 'src/middlewares/error-handler.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/typescript/src/middlewares/require-json.ts', title: 'src/middlewares/require-json.ts', showLineNumbers: true },
  ]}
/>

## Executando

<Steps>

1. Entre no projeto:

   ```bash title="Terminal"
   cd examples/courses/express/projects/typescript
   ```

2. Instale as dependências:

   <PackageManagerTabs />

3. Suba o servidor em modo watch:

   <PackageManagerTabs run="dev" />

4. Verifique os tipos — é o `tsc` que faz isso, não o Node:

   <PackageManagerTabs run="typecheck" />

</Steps>

## Rotas

| Método | Caminho      | Corpo             | Sucesso | Erros                     |
| ------ | ------------ | ----------------- | ------- | ------------------------- |
| GET    | `/users`     | —                 | `200`   | —                         |
| GET    | `/users/:id` | —                 | `200`   | `400`, `404`              |
| POST   | `/users`     | `{ name, email }` | `201`   | `400`, `409`, `415`       |
| PUT    | `/users/:id` | `{ name, email }` | `200`   | `400`, `404`, `415`       |
| DELETE | `/users/:id` | —                 | `204`   | `400`, `404`              |

## Exemplo

O `requireJson` responde antes do controller quando o `Content-Type` não é JSON:

<ApiRequest method="POST" path="/users" baseUrl="http://localhost:3000" description="Content-Type: text/plain" />

<ApiResponse status={415}>
{`
  {
    "error": {
      "status": 415,
      "message": "Content-Type precisa ser application/json"
    }
  }
`}
</ApiResponse>

E o `409` distingue o conflito de estado do erro de entrada:

<ApiRequest method="POST" path="/users" baseUrl="http://localhost:3000">
{`
  {
    "name": "Ana de novo",
    "email": "ana@example.com"
  }
`}
</ApiRequest>

<ApiResponse status={409}>
{`
  {
    "error": {
      "status": 409,
      "message": "E-mail ja cadastrado"
    }
  }
`}
</ApiResponse>

## Conceitos abordados

- Execução nativa de `.ts` pelo Node (*type stripping*)
- `tsconfig.json` com `noEmit`, `allowImportingTsExtensions` e `erasableSyntaxOnly`
- *Subpath imports* (`#`) declarados no `package.json`
- Tipagem de `Request`, `Response` e `NextFunction`
- Separação entre o tipo que **entra** (`UserInput`) e o que **sai** (`User`)
- `HttpError` e tratamento de erro centralizado

<Aside type="tip">
  As aulas correspondentes são [TypeScript no Express](../../basics/typescript/) e
  [MVC](../../architecture/mvc/).
</Aside>

<Aside type="caution" title="O Node não verifica os tipos">
  `npm run dev` sobe mesmo com erro de tipagem. Rode `npm run typecheck` antes de commitar —
  é ele que valida.
</Aside>

## Próximo projeto

[Express + Prisma](../express-prisma/) — trocando a memória por um banco de dados.
