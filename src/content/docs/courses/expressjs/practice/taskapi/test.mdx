---
title: "TaskAPI: testes e deploy"
description: "Décima segunda etapa da TaskAPI: testes de unidade e de rota com o runner nativo do Node e supertest, e a imagem de produção em multi-estágio."
course: expressjs
sidebar:
  label: "12. Testes e deploy"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Nível **Avançado** · Node.js · Express.js · TypeScript · `node:test` · supertest · Docker

A última etapa: 26 testes que provam o que as onze anteriores afirmaram, e uma imagem que
leva tudo isso para fora da sua máquina.

<ProjectLinks path="examples/courses/expressjs/projects/task-api-test" devcontainer="task-api-test" />

## Requisitos, histórias e critérios

**Épico EP05 · Qualidade e Operação** › **Features FT17 · Suíte de testes** e **FT18 · Empacotamento e deploy**

As duas features compartilham a mesma exigência: não alterar a aplicação para caber nelas. É
por isso que `src/` só ganha arquivos `.test.ts`, e a raiz ganha os três arquivos do Docker.

| Requisito | Recorte desta etapa | Estado ao final |
| --------- | ------------------- | --------------- |
| **RNF08** Testabilidade | unidade e rota com `node --test` e `supertest`, sem abrir porta | atendido |
| **RNF09** Portabilidade & Conteinerização | imagem multi-estágio, Compose, segredo por ambiente e volumes | atendido |

### US20 — Alterar sem quebrar o existente · RNF08

> **Como** pessoa que mantém a API,
> **quero** uma suíte que rode em segundos e fixe as decisões de segurança em asserções,
> **para** descobrir uma regressão antes de publicá-la.

```gherkin
Cenário: CA20.1 - A suíte inteira, sem porta
  Quando executo pnpm test
  Então 26 testes em 10 suítes passam
  E nenhuma porta é aberta
  E nenhum e-mail é enviado

Cenário: CA20.2 - O teste importa a aplicação
  Quando abro qualquer arquivo .test.ts de src/routes/
  Então ele importa app, e nunca server

Cenário: CA20.3 - O isolamento por dono está fixado
  Quando leio src/routes/task-router.test.ts
  Então existe um teste que consulta a tarefa de outro dono
  E ele afirma o status 404, e não 403

Cenário: CA20.4 - As regras de autenticação estão fixadas
  Quando leio src/routes/auth-router.test.ts
  Então existe um teste afirmando que a senha não sai na resposta
  E outro afirmando que senha errada e e-mail inexistente respondem o mesmo 401

Cenário: CA20.5 - Duas execuções seguidas passam
  Quando executo pnpm test duas vezes
  Então as duas passam
  E os usuários criados no before são apagados no after

Cenário: CA20.6 - Sem framework de teste
  Quando abro o package.json
  Então o script test roda node --test
  E supertest, com os seus tipos, é a única dependência nova, em devDependencies

Cenário: CA20.7 - A forma do cabeçalho e o parser de cookies estão fixados
  Quando leio src/routes/task-router.test.ts
  Então existe um teste com "Authorization: Token abc" afirmando 401 e a mensagem do schema
  E quando leio src/utils/cookies.test.ts
  Então parseCookies é testado sem servidor, com cabeçalho ausente e encoding inválido
```

### US21 — Subir a API em qualquer máquina · RNF09

> **Como** pessoa responsável pelo deploy,
> **quero** subir a API inteira com um comando,
> **para** não depender do que está instalado na máquina de destino.

```gherkin
Cenário: CA21.1 - Um comando
  Dado uma máquina com Docker e sem Node
  Quando executo JWT_SECRET=... docker compose up --build
  Então a API responde em http://localhost:3000

Cenário: CA21.2 - Migrations na subida
  Dado um volume vazio
  Quando o contêiner sobe
  Então as migrations pendentes são aplicadas antes de o servidor atender

Cenário: CA21.3 - Sem root
  Quando inspeciono o processo dentro do contêiner
  Então ele roda como o usuário app

Cenário: CA21.4 - O que não entra na imagem
  Quando inspeciono a imagem
  Então .env, node_modules, uploads e *.db não estão nela

Cenário: CA21.5 - Contêiner descartável, dados não
  Dado usuários e avatares criados
  Quando executo docker compose down e depois up
  Então o banco e os uploads continuam lá

Cenário: CA21.6 - Sem segredo, não sobe
  Quando executo docker compose up sem JWT_SECRET no ambiente
  Então o Compose recusa subir

Cenário: CA21.7 - O ciclo se fecha no /health
  Quando leio o Dockerfile
  Então o HEALTHCHECK consulta a rota /health que nasceu na etapa 1
```

### Tasks da etapa

As tarefas abaixo implementam US20 e US21 e são a ordem sugerida de execução. As seções
seguintes explicam a decisão por trás de cada uma, e o código completo está ao final da página.

<Steps>

1. **TK12.1 · Instalar o `supertest` e criar `src/routes/auth-router.test.ts`**: cadastro, senha fora da resposta, 409 e o 401 único.
2. **TK12.2 · Criar `src/routes/task-router.test.ts`**: isolamento por dono, 404 em vez de 403 e campo extra recusado.
3. **TK12.3 · Criar `src/services/ping-service.test.ts`**: a extração da latência em três formatos de saída.
4. **TK12.4 · Criar `src/utils/cookies.test.ts` e o caso de cabeçalho malformado**: `parseCookies` como função pura e `Authorization: Token abc` respondendo 401 com a mensagem do schema.
5. **TK12.5 · Criar `Dockerfile`**: estágio de build com `pnpm` e o estágio final só com o necessário, usuário sem privilégio e `HEALTHCHECK`.
6. **TK12.6 · Criar `.dockerignore`**: temporários, segredos e dependências locais fora do contexto.
7. **TK12.7 · Criar `compose.yaml`**: o serviço, o `command` que migra antes de subir, `${JWT_SECRET:?}` e os volumes de banco e uploads.

</Steps>

## O que muda nesta etapa?

| Antes (etapa 11) | Agora (etapa 12) |
| ---------------- | ---------------- |
| Testes só de `jwt` e `password` | + testes de rota com `supertest` |
| — | `Dockerfile` multi-estágio |
| — | `compose.yaml` com volumes |
| — | `.dockerignore` |

Nenhuma dependência de runtime nova: o runner é o do próprio Node, e `supertest` é `devDependency`.

## Estrutura

<FileTree>
{`
  task-api-test/
  ├── Dockerfile                          novo — build e runtime separados
  ├── compose.yaml                        novo
  ├── .dockerignore                       novo
  └── src/
      ├── routes/
      │   ├── auth-router.test.ts         novo
      │   └── task-router.test.ts         novo
      ├── services/
      │   └── ping-service.test.ts        novo
      └── utils/
          ├── cookies.test.ts             novo
          ├── jwt.test.ts
          └── password.test.ts
`}
</FileTree>

## Descrição das tarefas

Abaixo estão detalhadas as tarefas de implementação desta etapa, com orientações e trechos de código.

### TK12.1 · Instalar o supertest e criar src/routes/auth-router.test.ts (Testes de integração de autenticação)

O supertest executa requisições HTTP contra o objeto `app` sem abrir portas de rede, viabilizando execução paralela rápida das suítes de teste:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/auth-router.test.ts"
  title="src/routes/auth-router.test.ts"
  region="setup"
/>

Os testes fixam as regras de negócio declaradas no contrato: a senha nunca deve retornar no payload de resposta e tentativas de login com credenciais incorretas retornam mensagem unificada de erro 401:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/auth-router.test.ts"
  title="src/routes/auth-router.test.ts"
  region="cases"
/>

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/auth-router.test.ts"
  title="src/routes/auth-router.test.ts"
  region="signin"
/>

### TK12.2 · Criar src/routes/task-router.test.ts (O teste que mais importa: isolamento por dono)

O isolamento de dados por usuário é a garantia de segurança mais crítica da aplicação. O teste verifica que acessar tarefas de outro usuário retorna status `404 Not Found` em vez de `403 Forbidden`:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/task-router.test.ts"
  title="src/routes/task-router.test.ts"
  region="isolation"
/>

<Aside type="tip" title="Testar o status, não só o dado">
  Afirmar `404` em vez de `403` é a diferença entre uma API que oculta a existência de recursos alheios e uma que permite a enumeração de IDs existentes.
</Aside>

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/task-router.test.ts"
  title="src/routes/task-router.test.ts"
  region="guards"
/>

### TK12.3 · Criar src/services/ping-service.test.ts (Unidade para o que é puro: extração do ping)

Funções determinísticas e puras, como a extração de latência a partir do texto de saída de comandos de rede, são testadas isoladamente sem requisições de rede ou criação de processos filhos:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/services/ping-service.test.ts"
  title="src/services/ping-service.test.ts"
  region="unit"
/>

### TK12.4 · Criar src/utils/cookies.test.ts e o caso de cabeçalho malformado (Testes de cookies e autenticação)

O utilitário `parseCookies` é testado para cenários de borda (valores com `=`, cabeçalho ausente e caracteres inválidos):

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/utils/cookies.test.ts"
  title="src/utils/cookies.test.ts"
  region="unit"
/>

### TK12.5 · Criar Dockerfile (A imagem separa build de runtime)

A imagem multi-estágio compila e instala dependências no estágio de build e entrega uma imagem de produção limpa, executando sob usuário sem privilégios (`USER app`) e com sonda de integridade `HEALTHCHECK`:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/Dockerfile"
  title="Dockerfile"
  region="build"
/>

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/Dockerfile"
  title="Dockerfile"
  region="runtime"
/>

<Aside type="danger" title="Não execute contêineres como root">
  O usuário padrão da imagem oficial do Node é `root`. Criar e utilizar um usuário dedicado sem privilégios (`app`) limita o raio de alcance caso o processo seja comprometido.
</Aside>

### TK12.6 · Criar .dockerignore (O que não entra na imagem)

O arquivo `.dockerignore` previne que segredos locais (`.env`), bancos SQLite de desenvolvimento e pastas de cache entrem no contexto da imagem:

```txt title=".dockerignore"
node_modules
.env
*.db
*.db-journal
uploads
prisma/dev.db
.git
```

### TK12.7 · Criar compose.yaml (Volumes e segredo no ambiente)

O Docker Compose gerencia a inicialização, garantindo que o `JWT_SECRET` seja fornecido via ambiente e que o banco de dados e os arquivos enviados fiquem protegidos em volumes persistentes:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/compose.yaml"
  title="compose.yaml"
  lang="yaml"
/>

<Aside type="danger" title="O compose carrega o `.env` do diretório por padrão">
  Em ambientes de produção, utilize `--env-file` apontando para o arquivo seguro de variáveis e certifique-se de que segredos de desenvolvimento não sejam utilizados na publicação.
</Aside>

## Código completo

<CodeTabs
  syncKey="task-api-test"
  files={[
    { path: 'examples/courses/expressjs/projects/task-api-test/src/routes/auth-router.test.ts', title: 'src/routes/auth-router.test.ts', showLineNumbers: true },
    { path: 'examples/courses/expressjs/projects/task-api-test/src/routes/task-router.test.ts', title: 'src/routes/task-router.test.ts', showLineNumbers: true },
    { path: 'examples/courses/expressjs/projects/task-api-test/src/services/ping-service.test.ts', title: 'src/services/ping-service.test.ts', showLineNumbers: true },
    { path: 'examples/courses/expressjs/projects/task-api-test/src/utils/cookies.test.ts', title: 'src/utils/cookies.test.ts', showLineNumbers: true },
    { path: 'examples/courses/expressjs/projects/task-api-test/Dockerfile', title: 'Dockerfile', lang: 'dockerfile' },
    { path: 'examples/courses/expressjs/projects/task-api-test/compose.yaml', title: 'compose.yaml', lang: 'yaml' },
  ]}
/>

## Executando

<Steps>

1. ```bash
   cd examples/courses/expressjs/projects/task-api-test
   ```

2. <PackageManagerTabs />

3. ```bash
   cp .env.example .env
   ```

4. <PackageManagerTabs run="db:migrate" />

   <PackageManagerTabs run="db:seed" />

5. Rode a suíte inteira:

   <PackageManagerTabs run="test" />

6. E, com o Docker disponível, suba a imagem:

   ```bash
   JWT_SECRET=$(openssl rand -hex 32) docker compose up --build
   ```

</Steps>

## A suíte

```txt title="pnpm test"
ℹ tests 26
ℹ suites 10
ℹ pass 26
ℹ fail 0
ℹ duration_ms 704.944083
```

| Arquivo | O que fixa |
| ------- | ---------- |
| `password.test.ts` | sal por chamada, formato PHC, senha errada recusada |
| `jwt.test.ts` | três partes, assinatura adulterada e token expirado |
| `cookies.test.ts` | pares separados, valor com `=`, cabeçalho ausente, encoding inválido |
| `ping-service.test.ts` | extração da latência em três formatos de saída |
| `auth-router.test.ts` | senha fora da resposta, `409` no e-mail repetido, `422` com o campo |
| `task-router.test.ts` | isolamento por dono, `404` em vez de `403`, cabeçalho fora do formato Bearer, campo extra recusado |

<Aside type="caution" title="Os testes de rota usam o banco de desenvolvimento">
  Eles criam e apagam o próprio usuário no `before`/`after`, mas leem as tarefas semeadas.
  Numa suíte de verdade, `DATABASE_URL` apontaria para um banco de teste descartável — é o
  próximo passo natural deste projeto.
</Aside>

## Conceitos abordados

- Runner nativo (`node --test`) sem framework de teste
- `supertest` contra o `app`, sem abrir porta
- Teste de unidade para função pura, de rota para contrato
- Fixar decisões de segurança em asserção
- `before`/`after` para deixar o estado como estava
- Imagem multi-estágio e cache de camadas
- Usuário sem privilégio, `HEALTHCHECK` e `.dockerignore`
- Volumes e segredo vindo do ambiente

<Aside type="tip">
  Os tópicos correspondentes são [Testes](../../../advanced/testing/) e
  [Deploy](../../../advanced/deploy/).
</Aside>

## Fim da trilha

Esta é a última etapa da TaskAPI. Da primeira rota (um `/health` de quatro linhas) até
uma API autenticada, validada, observável e conteinerizada, sem nunca instalar um pacote
que o Node já resolvia.

Para construir uma **aplicação** completa, com front-end, backlog e infraestrutura, siga
para o [InvestApp](../../investapp/) ou o [MonitorApp](../../monitorapp/).
