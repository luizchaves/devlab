---
title: "TaskAPI: documentação OpenAPI"
description: "Sexta etapa da TaskAPI: o contrato publicado em OpenAPI 3, gerado a partir dos mesmos schemas Zod que validam as requisições, e navegável no Swagger UI."
course: expressjs
sidebar:
  label: "6. Documentação"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import Mermaid from '@components/Mermaid.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Nível **Intermediário** · Node.js · Express.js · TypeScript · Zod · OpenAPI

A documentação que não envelhece: ela nasce do mesmo schema que valida a requisição.

<ProjectLinks path="examples/courses/expressjs/projects/task-api-openapi" devcontainer="task-api-openapi" />

## Requisitos, histórias e critérios

**Épico EP04 · Fundação Técnica** › **Feature FT14 · Documentação OpenAPI**

| Requisito | Recorte desta etapa | Estado ao final |
| --------- | ------------------- | --------------- |
| **RNF05** Documentação da API | OpenAPI 3 gerado dos schemas Zod e servido no Swagger UI | atendido para `/tasks` |

### US08 — Integrar sem ler o código · RNF05

> **Como** pessoa desenvolvedora que vai consumir a API,
> **quero** uma referência navegável e sempre atualizada das rotas,
> **para** integrar sem ler o código-fonte nem perguntar a alguém.

```gherkin
Cenário: CA08.1 - A interface para pessoas
  Quando abro http://localhost:3000/docs
  Então vejo as cinco rotas de /tasks
  E cada uma se expande em corpo, parâmetros e respostas

Cenário: CA08.2 - O documento para ferramentas
  Quando envio GET /openapi.json
  Então recebo um documento OpenAPI 3 válido
  E consigo importá-lo no Insomnia ou no Postman

Cenário: CA08.3 - O documento diz o que o schema faz
  Quando comparo /openapi.json com src/schemas/task.ts
  Então minLength, o enum de priority e a lista de required são os mesmos

Cenário: CA08.4 - Uma linha muda os dois
  Quando altero o mínimo de "title" no schema
  Então o minLength exibido muda
  E a regra da rota muda
  E nenhuma linha de openapi.ts foi editada

Cenário: CA08.5 - A posição na cadeia
  Quando abro src/app.ts
  Então o docsRouter está montado depois das rotas da API
  E antes do notFound

Cenário: CA08.6 - Documentar não é alterar
  Quando repito as requisições da etapa 5
  Então nenhuma rota de /tasks mudou de comportamento
```

### Tasks da etapa

As tarefas abaixo implementam US08 e são a ordem sugerida de execução. As seções seguintes
explicam a decisão por trás de cada uma, e o código completo está ao final da página.

<Steps>

1. **TK06.1 · Instalar o `swagger-ui-express` e criar `src/docs/openapi.ts`**: `z.toJSONSchema()` com alvo OpenAPI 3.0 e o documento montado.
2. **TK06.2 · Criar `src/routes/docs-router.ts`**: `/docs` para pessoas e `/openapi.json` para ferramentas.
3. **TK06.3 · Modificar `src/app.ts`**: o router montado sem prefixo, no lugar certo da cadeia.

</Steps>

## O que muda nesta etapa?

Nenhuma rota de `/tasks` muda. O que entra é a publicação do contrato:

| Antes (etapa 5) | Agora (etapa 6) |
| --------------- | --------------- |
| O contrato existia só no `requests.http` | `src/docs/openapi.ts` |
| — | `GET /openapi.json`, o documento cru |
| — | `GET /docs`, o Swagger UI |

## O problema que isso resolve

Documentação escrita à mão descreve a API que existia no dia em que foi escrita. Quando a
regra muda no schema e ninguém lembra de atualizar o `.yaml`, o contrato publicado passa a
mentir — e o cliente que confiou nele quebra em produção.

<Mermaid title="Uma fonte, três consumidores">
{`
flowchart LR
  S["src/schemas/task.ts<br/>schema Zod"] --> V["validate<br/>recusa 422"]
  S --> J["z.toJSONSchema()"]
  J --> D["/openapi.json<br/>ferramentas"]
  J --> U["/docs<br/>Swagger UI"]
`}
</Mermaid>

Mudar `min(3)` para `min(5)` no schema muda, na mesma linha, o que a API recusa e o que a
documentação promete.

## O Zod converte sozinho

O Zod 4 gera JSON Schema sem biblioteca auxiliar. Como o OpenAPI 3.0 usa um dialeto
próprio, o alvo é informado na chamada:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-openapi/src/docs/openapi.ts"
  title="src/docs/openapi.ts"
  region="to-json-schema"
/>

Os schemas da etapa 5 declaram `body`, `params` e `query` como chaves de um objeto maior,
então `shapeOf` recorta a fonte que cada parte do documento precisa. O corpo do `POST`
vira o `requestBody`; a `query` vira a lista de `parameters`; o `params` vira o parâmetro
de caminho.

## O documento

O restante do arquivo é o documento OpenAPI propriamente dito — os caminhos, os verbos e
as respostas de cada um, com os schemas gerados encaixados nos lugares certos:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-openapi/src/docs/openapi.ts"
  title="src/docs/openapi.ts"
  region="components"
/>

## Duas rotas, dois públicos

`/openapi.json` serve o documento cru, que Insomnia, Postman e geradores de client
consomem. `/docs` serve o Swagger UI, que é onde uma pessoa navega:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-openapi/src/routes/docs-router.ts"
  title="src/routes/docs-router.ts"
  region="routes"
/>

O router é montado **sem prefixo**, depois das rotas da API e antes do `notFound` — se
viesse depois, o 404 responderia primeiro:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-openapi/src/app.ts"
  title="src/app.ts"
  region="docs"
/>

<Aside type="caution" title="`/docs` em produção é uma decisão, não um padrão">
  O Swagger UI expõe todos os endpoints, incluindo os administrativos. Numa API pública,
  ou você publica o contrato de propósito, ou protege `/docs` atrás de autenticação. O
  que não vale é deixá-lo aberto sem ter decidido.
</Aside>

## Código completo

<CodeTabs
  syncKey="task-api-openapi"
  files={[
    { path: 'examples/courses/expressjs/projects/task-api-openapi/src/docs/openapi.ts', title: 'src/docs/openapi.ts', showLineNumbers: true },
    { path: 'examples/courses/expressjs/projects/task-api-openapi/src/routes/docs-router.ts', title: 'src/routes/docs-router.ts', showLineNumbers: true },
    { path: 'examples/courses/expressjs/projects/task-api-openapi/src/app.ts', title: 'src/app.ts', showLineNumbers: true },
  ]}
/>

## Executando

<Steps>

1. ```bash
   cd examples/courses/expressjs/projects/task-api-openapi
   ```

2. <PackageManagerTabs />

3. <PackageManagerTabs run="dev" />

4. Abra `http://localhost:3000/docs` e experimente as rotas pelo próprio navegador.

</Steps>

## Rotas

| Método | Caminho          | O que serve                        |
| ------ | ---------------- | ---------------------------------- |
| GET    | `/docs`          | Swagger UI, para pessoas           |
| GET    | `/openapi.json`  | O documento cru, para ferramentas  |

As rotas de `/health` e `/tasks` são as mesmas da [etapa 5](../validation/).

## Conceitos abordados

- OpenAPI 3 como contrato da API
- `z.toJSONSchema()` com alvo `openapi-3.0`
- Uma fonte de verdade para validação e documentação
- Swagger UI servido pelo próprio Express
- Ordem de montagem do router de documentação

<Aside type="tip">
  O tópico correspondente é [Documentação de API](../../../api/documentation/).
</Aside>

## Próxima etapa

[SQLite](../sqlite/) — os dados saem da memória para um arquivo, usando o módulo nativo
`node:sqlite`.
