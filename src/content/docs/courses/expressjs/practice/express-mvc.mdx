---
title: "Projeto: Express MVC"
description: Aplicação Express organizada em MVC, com CRUD completo e tratamento de erros centralizado.
course: expressjs
project: express-mvc
sidebar:
  label: "Express MVC"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import ApiRequest from '@components/ApiRequest.astro';
import ApiResponse from '@components/ApiResponse.astro';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';
import ProjectLinks from '@components/ProjectLinks.astro';

Nível **Intermediário** · Node.js · Express.js · MVC

CRUD completo de usuários, com cada responsabilidade em seu lugar.

<ProjectLinks path="examples/courses/express/projects/mvc" devcontainer="express-mvc" />

## Estrutura

<FileTree>
{`
  express-mvc/
  ├── package.json
  └── src/
      ├── app.js
      ├── server.js
      ├── controllers/
      │   └── user-controller.js
      ├── middlewares/
      │   └── error-handler.js
      ├── models/
      │   └── user-model.js
      └── routes/
          └── user-router.js
`}
</FileTree>

## Código

<CodeTabs
  syncKey="express-mvc"
  files={[
    { path: 'examples/courses/express/projects/mvc/src/app.js', title: 'src/app.js', showLineNumbers: true },
    { path: 'examples/courses/express/projects/mvc/src/routes/user-router.js', title: 'src/routes/user-router.js', showLineNumbers: true },
    { path: 'examples/courses/express/projects/mvc/src/controllers/user-controller.js', title: 'src/controllers/user-controller.js', showLineNumbers: true },
    { path: 'examples/courses/express/projects/mvc/src/models/user-model.js', title: 'src/models/user-model.js', showLineNumbers: true },
    { path: 'examples/courses/express/projects/mvc/src/middlewares/error-handler.js', title: 'src/middlewares/error-handler.js', showLineNumbers: true },
  ]}
/>

## Executando

<Steps>

1. ```bash
   cd examples/courses/express/projects/mvc
   ```

2. <PackageManagerTabs />

3. <PackageManagerTabs run="dev" />

</Steps>

## Rotas

| Método | Caminho      | Sucesso | Erros        |
| ------ | ------------ | ------- | ------------ |
| GET    | `/users`     | `200`   | —            |
| GET    | `/users/:id` | `200`   | `404`        |
| POST   | `/users`     | `201`   | `400`        |
| PUT    | `/users/:id` | `200`   | `404`        |
| DELETE | `/users/:id` | `204`   | `404`        |

## Exemplo

Nesta seção, testamos a criação de recursos na API estruturada sob o padrão MVC.

A requisição `POST /users` envia um objeto JSON contendo nome e e-mail. O controller valida o payload, chama a camada de model para persistir e retorna o usuário criado com seu novo `id` e status `201 Created`:

<details>
  <summary>Requisição .http correspondente</summary>

  ```http
  ### Criar usuário na estrutura MVC
  POST http://localhost:3000/users
  Content-Type: application/json

  {
    "name": "Carla",
    "email": "carla@example.com"
  }
  ```
</details>

<ApiRequest method="POST" path="/users" baseUrl="http://localhost:3000">
{`
  {
    "name": "Carla",
    "email": "carla@example.com"
  }
`}
</ApiRequest>

<ApiResponse status={201}>
{`
  {
    "id": 3,
    "name": "Carla",
    "email": "carla@example.com"
  }
`}
</ApiResponse>

## Conceitos abordados

- Separação em models, controllers, routes e middlewares
- Erros com status HTTP (`HttpError`) e handler centralizado
- Middleware de 404 e middleware de erro com quatro parâmetros
- CRUD completo sobre um model em memória

<Aside type="tip">
  A aula correspondente é [Express.js → MVC](../../architecture/mvc/).
</Aside>

## Próximo projeto

[Express TypeScript](../express-typescript/) — as mesmas camadas, agora tipadas.
