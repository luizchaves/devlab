---
title: "Projeto: Express Auth"
description: "Cadastro, autenticação por JWT e autorização por posse e papel, com hash Argon2id — tudo usando apenas node:crypto."
course: expressjs
sidebar:
  label: "Express Auth"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import ApiRequest from '@components/ApiRequest.astro';
import ApiResponse from '@components/ApiResponse.astro';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Nível **Avançado** · Node.js · Express.js · TypeScript · `node:crypto`

Cadastro, login e rotas protegidas sem `bcrypt` e sem `jsonwebtoken`: hash de senha com
Argon2id e assinatura de JWT com HMAC-SHA256, ambos do módulo nativo.

<ProjectLinks path="examples/courses/express/projects/auth" devcontainer="express-auth" />

## Estrutura

<FileTree>
{`
  express-auth/
  ├── .env.example
  ├── package.json
  ├── tsconfig.json
  ├── requests.http
  └── src/
      ├── app.ts
      ├── server.ts
      ├── controllers/
      │   ├── auth-controller.ts       signup, signin, me
      │   ├── investment-controller.ts recurso protegido por posse
      │   └── user-controller.ts       rota administrativa
      ├── errors/
      │   └── HttpError.ts
      ├── middlewares/
      │   ├── authenticate.ts          quem está chamando (401)
      │   ├── authorize.ts             o que pode fazer (403/404)
      │   └── error-handler.ts
      ├── models/
      │   ├── investment-model.ts      filtra por dono na consulta
      │   └── user-model.ts            hash da senha no model
      ├── routes/
      │   ├── auth-router.ts
      │   ├── investment-router.ts
      │   └── user-router.ts
      ├── types/
      │   ├── express.d.ts             estende Request com req.auth
      │   └── index.ts
      └── utils/
          ├── jwt.ts                   assina e verifica HS256
          ├── jwt.test.ts
          ├── password.ts              Argon2id no formato PHC
          └── password.test.ts
`}
</FileTree>

## As duas peças criptográficas

O hash de senha usa `argon2Sync` e devolve o resultado no formato PHC — parâmetros e sal
viajam junto do hash:

<SourceCode
  path="examples/courses/express/projects/auth/src/utils/password.ts"
  title="src/utils/password.ts"
  lines="32-46"
  showLineNumbers
/>

A assinatura do token é um HMAC-SHA256 sobre `header.payload`:

<SourceCode
  path="examples/courses/express/projects/auth/src/utils/jwt.ts"
  title="src/utils/jwt.ts"
  lines="39-56"
  showLineNumbers
/>

## Código

<CodeTabs
  syncKey="express-auth"
  files={[
    { path: 'examples/courses/express/projects/auth/src/app.ts', title: 'src/app.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/auth/src/controllers/auth-controller.ts', title: 'src/controllers/auth-controller.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/auth/src/middlewares/authenticate.ts', title: 'src/middlewares/authenticate.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/auth/src/middlewares/authorize.ts', title: 'src/middlewares/authorize.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/auth/src/models/user-model.ts', title: 'src/models/user-model.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/auth/src/utils/password.ts', title: 'src/utils/password.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/auth/src/utils/jwt.ts', title: 'src/utils/jwt.ts', showLineNumbers: true },
    { path: 'examples/courses/express/projects/auth/src/types/express.d.ts', title: 'src/types/express.d.ts', showLineNumbers: true },
  ]}
/>

## Executando

<Steps>

1. Entre no projeto:

   ```bash title="Terminal"
   cd examples/courses/express/projects/auth
   ```

2. Instale as dependências:

   <PackageManagerTabs />

3. Crie o `.env` a partir do exemplo:

   ```bash title="Terminal"
   cp .env.example .env
   ```

4. Suba o servidor:

   <PackageManagerTabs run="dev" />

5. Rode os testes das funções criptográficas:

   <PackageManagerTabs run="test" />

   ```txt title="Output"
   ℹ tests 9
   ℹ pass 9
   ℹ fail 0
   ```

</Steps>

Usuário de demonstração: `ana@example.com` / `senha-secreta`, com papel `admin`.

## Rotas

| Método | Caminho            | Proteção              | Sucesso | Erros              |
| ------ | ------------------ | --------------------- | ------- | ------------------ |
| POST   | `/auth/signup`     | pública               | `201`   | `409`, `422`       |
| POST   | `/auth/signin`     | pública               | `200`   | `401`              |
| GET    | `/auth/me`         | token                 | `200`   | `401`              |
| GET    | `/investments`     | token (só os do dono) | `200`   | `401`              |
| POST   | `/investments`     | token                 | `201`   | `401`, `422`       |
| DELETE | `/investments/:id` | token + posse         | `204`   | `401`, `404`       |
| GET    | `/users`           | token + `role=admin`  | `200`   | `401`, `403`       |

## Exemplo

O login devolve o token que autoriza as demais chamadas:

<ApiRequest method="POST" path="/auth/signin" baseUrl="http://localhost:3000">
{`
  {
    "email": "ana@example.com",
    "password": "senha-secreta"
  }
`}
</ApiRequest>

<ApiResponse status={200}>
{`
  {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxYTAyIiwibmFtZSI6IkFuYSJ9.qk3Xr6…"
  }
`}
</ApiResponse>

Sem ele, a rota protegida recusa antes de qualquer consulta:

<ApiRequest method="GET" path="/investments" baseUrl="http://localhost:3000" />

<ApiResponse status={401}>
{`
  {
    "error": {
      "status": 401,
      "message": "Token de acesso ausente"
    }
  }
`}
</ApiResponse>

E o investimento de outra pessoa responde `404`, não `403` — para não revelar que o id
existe:

<ApiRequest method="DELETE" path="/investments/e7a04a8a" baseUrl="http://localhost:3000" description="Token de outro usuário" />

<ApiResponse status={404}>
{`
  {
    "error": {
      "status": 404,
      "message": "Investimento nao encontrado"
    }
  }
`}
</ApiResponse>

## Conceitos abordados

- Hash de senha com Argon2id e formato PHC, sem dependências
- Comparação em tempo constante com `timingSafeEqual`
- Assinatura e verificação de JWT HS256 com `createHmac`
- Middleware de autenticação (`401`) separado do de autorização (`403`/`404`)
- Autorização por posse do recurso e por papel
- Extensão do `Request` do Express com `declaration merging`
- Testes de unidade com `node:test`

<Aside type="tip">
  As aulas correspondentes são [Senhas e Hash](../../auth/passwords/),
  [Cadastro de Usuário](../../auth/user-registration/),
  [Autenticação](../../auth/authentication/) e [Autorização](../../auth/authorization/).
</Aside>

<Aside type="caution" title="`argon2Sync` exige Node 24">
  Em versões anteriores, troque por `scryptSync`: o formato PHC, a estrutura da verificação e
  a comparação em tempo constante permanecem idênticos.
</Aside>

## Próximo projeto

[InvestApp](../investapp/) — a mesma trilha aplicada a uma aplicação completa, etapa por
etapa.
