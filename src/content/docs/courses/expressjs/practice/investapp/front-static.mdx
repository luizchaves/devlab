---
title: "InvestApp: Front estático"
description: "Primeira etapa do InvestApp: telas estáticas em HTML e Tailwind CSS, sem JavaScript, preparando a UX que será conectada à API nas próximas etapas."
course: expressjs
sidebar:
  label: "1. Front estático"
---
Materiais: [slides da aula](../../../../../slides/courses/expressjs/practice/investapp/front-static/) e [mapa mental](../../../../../mindmaps/courses/expressjs/practice/investapp/front-static/).

import { Aside, Steps } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import HtmlPreview from '@components/HtmlPreview.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';

Etapa **1 de 13** · Nível **Iniciante** · HTML · Tailwind CSS · Sem JavaScript

O InvestApp começa pela experiência visual completa. Nesta etapa existem telas, navegação,
formulários e estados estáticos, mas nenhum `fetch`, nenhum manipulador de evento e nenhum
arquivo JavaScript. A ideia é congelar a UX antes de ligar comportamento.

<Aside type="note" title="Aulas conceituais associadas">
  - **Estrutura de Aplicações Web**: veja [Introdução ao Express.js](../../../basics/introduction/) e [Rotas e Arquivos Estáticos](../../../basics/routes/)
</Aside>

## Requisitos, histórias e critérios

**Épico EP01 · Experiência e Carteira** › **Feature FT01 · Telas do sistema**

Esta etapa não fecha nenhum requisito: ela desenha a superfície de quatro deles, que serão
implementados mais adiante.

| Requisito | Recorte desta etapa | Estado ao final |
| --------- | ------------------- | --------------- |
| **RF01** Gestão de Investimentos | a tela da carteira e o formulário de novo ativo | preparado |
| **RF02** Cadastro de Usuários | o formulário de `signup.html` e seus quatro campos | preparado |
| **RF03** Autenticação & Sessão | o formulário de `signin.html` | preparado |
| **RF06** Gestão de Perfil & Avatar | o painel de perfil e a área de soltar arquivo | preparado |

### US01 — Conhecer o sistema antes de usá-lo · RF01, RF02, RF03, RF06

> **Como** visitante,
> **quero** navegar pelas telas de carteira, cadastro, login e perfil,
> **para** entender o que a aplicação faz antes de confiar meus dados a ela.

```gherkin
Cenário: CA01.1 - As telas abrem sem servidor
  Dado que o projeto foi apenas baixado, sem instalação
  Quando abro o arquivo index.html no navegador
  Então a página da carteira é exibida com os cartões de exemplo

Cenário: CA01.2 - A navegação liga as quatro telas
  Dado que estou na página index.html
  Quando clico nos links do cabeçalho
  Então chego a signin.html, signup.html e profile.html
  E nenhuma navegação depende de JavaScript

Cenário: CA01.3 - Os campos já declaram o contrato futuro
  Dado que abro o formulário de novo investimento
  Então os campos têm os atributos name "name", "value" e "interest"
  E é por esses nomes que a etapa 2 vai ler os dados

Cenário: CA01.4 - A etapa não tem comportamento
  Quando procuro arquivos JavaScript de aplicação no projeto
  Então existe apenas o vite.config.js
  E o único script das páginas é o do Tailwind, que só gera estilo
```

### Tasks da etapa

As tarefas abaixo implementam US01 e são a ordem sugerida de execução. Cada uma tem a sua
seção detalhada logo em seguida.

<Steps>

1. **TK01.1 · Criar `index.html`**: Dashboard principal com métricas, lista de investimentos e formulário.
2. **TK01.2 · Carregar o Tailwind e criar `css/components.css`**: utilitários pelo Play CDN e o vocabulário visual do produto.
3. **TK01.3 · Criar `signup.html`**: Formulário visual de criação de conta do investidor.
4. **TK01.4 · Criar `signin.html`**: Tela visual de autenticação com e-mail e senha.
5. **TK01.5 · Criar `profile.html`**: Painel de perfil do usuário e avatar padrão.

</Steps>

## Estrutura da aplicação

O front nasce como um protótipo navegável em HTML. Os utilitários do Tailwind vêm do Play CDN,
que os gera no navegador, e o arquivo `css/components.css` concentra o vocabulário visual do
produto. A regra da etapa continua valendo: **sem JavaScript de aplicação**.

<ProjectLinks path="examples/courses/expressjs/projects/invest-app-static" />

<FileTree>
{`
  invest-app-static/
  ├── package.json              só o Vite, e só para o build
  ├── vite.config.js            as quatro páginas do build
  ├── index.html                dashboard e formulário de investimento
  ├── signup.html               criação de conta
  ├── signin.html               entrada do usuário
  ├── profile.html              perfil e avatar
  ├── css/
  │   └── components.css        o vocabulário visual do produto
  └── public/
      └── imgs/
          └── profile/
              └── avatar.png    imagem inicial do usuário
`}
</FileTree>

## O que muda nesta etapa

Esta é a primeira etapa, então não existe um diff contra a etapa anterior — tudo é novo. O que
vale registrar é a fronteira que a etapa impõe: quatro páginas e uma folha de estilo entram, e
todo o comportamento fica para a etapa 2.

| Entra nesta etapa | Fica para as próximas |
| ----------------- | --------------------- |
| `index.html`, `signup.html`, `signin.html` e `profile.html` | a pasta `public/js/` e qualquer tag `<script>` |
| o Play CDN do Tailwind e o `css/components.css` | `fetch`, manipuladores de evento e estado de tela |
| `public/imgs/profile/avatar.png` como avatar padrão | o upload real da imagem, que chega na etapa 11 |

## Descrição das tarefas

Abaixo estão detalhadas as tarefas de implementação desta etapa, com orientações e trechos de código.

### TK01.1 · Criar `index.html` (Dashboard de Investimentos)

<HtmlPreview path="examples/courses/expressjs/projects/invest-app-static/index.html" height="24rem" serveFromPublic allowScripts />

O dashboard é montado com três blocos. O cabeçalho de navegação ocupa as linhas 11 a 27; o resumo da carteira aparece no cartão `<div class="card metric grid gap-2 p-6">` das linhas 39 a 43; e a grade de ativos repete a mesma estrutura em cada `<article class="card investment-card p-5">`, cuja primeira ocorrência está nas linhas 47 a 57. Os dois cartões seguintes (linhas 59 a 81) são cópias com outros textos e por isso aparecem recolhidos — a estrutura que importa é a do cartão visível.

Repare como as classes se dividem nesses blocos. `card`, `investment-card`, `badge` e `status-dot` vêm do `components.css` e dizem **o que a coisa é**; tudo em volta — `mx-auto max-w-7xl px-4`, `grid gap-4 md:grid-cols-3`, `text-slate-500` — são utilitários do Tailwind escritos direto no HTML, que dizem **como ela se posiciona**. Essa divisão vale nas treze etapas.

O trecho destacado nas linhas 84 a 111 é o único elemento interativo da página: o formulário de novo investimento vive dentro de um `<details class="card mt-8 p-6">`, e é o `<summary>` da linha 85 que abre e fecha o bloco, sem uma única linha de JavaScript. Dentro do `<form>` estão os três campos que a API da etapa 2 vai ler pelos atributos `name`: `name="name"` na linha 99, `name="value"` na linha 103 e `name="interest"` na linha 107.

Repare também que o botão da linha 109 usa `type="button"`, e não `type="submit"`. Essa escolha congela a estrutura visual sem disparar recarregamento de página: os nomes dos campos já são o contrato que o JavaScript vanilla vai consumir nas etapas seguintes.

<SourceCode
  path="examples/courses/expressjs/projects/invest-app-static/index.html"
  title="index.html"
  showLineNumbers
  mark="84-111"
  collapse="1-27,59-81"
/>

### TK01.2 · Carregar o Tailwind e criar `css/components.css`

Um protótipo não deveria precisar de build. É exatamente para isso que existe o **Play CDN** do
Tailwind: um `<script>` que compila as classes no próprio navegador, olhando o HTML da página e
gerando só o CSS que ela usa.

```html title="index.html — as duas linhas do head"
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<link rel="stylesheet" href="css/components.css" />
```

A divisão entre as duas linhas é o ponto da tarefa:

| Origem | O que entrega | Exemplo |
| ------ | ------------- | ------- |
| **Play CDN** | os utilitários do Tailwind, gerados em tempo de execução | `flex`, `gap-4`, `text-slate-500`, `md:grid-cols-3` |
| **`components.css`** | o vocabulário visual **deste produto** | `.card`, `.btn-primary`, `.investment-card`, `.file-drop` |

O Tailwind não tem — nem deveria ter — uma classe para "cartão de investimento". Utilitário é o
que serve a qualquer projeto; componente é o que serve a este. Separar os dois em origens
diferentes deixa a fronteira explícita.

<Aside type="caution" title="O Play CDN é para prototipar, e só">
  A própria documentação do Tailwind desaconselha o CDN em produção: ele baixa o compilador para
  o navegador e trabalha a cada carregamento de página. Aqui isso é aceitável porque a etapa é um
  protótipo — não há build, não há servidor, e trocar uma classe é recarregar a aba.

  Um projeto que vá para produção compila o Tailwind. É o que a [etapa 2](../api/) passa a fazer:
  a CLI do Tailwind lê o HTML de `public/` e escreve um `css/app.css` com apenas os utilitários
  que as telas realmente usam. O `components.css` que você escreve aqui continua exatamente o
  mesmo daquela etapa em diante — o que muda é só o caminho pelo qual os utilitários chegam.
</Aside>

O `components.css` tem três blocos. As linhas 13 a 40 declaram os tokens de cor em `:root` e o
fundo do `body`; as linhas 42 a 146 são os componentes genéricos — `.card` (linha 42), `.btn` e
suas variantes (49 a 85), o `.icon-button` dos ícones de ação (87), e `.field`, `.input` e o
`.invalid-feedback` da validação (105 a 146); e da linha 148 em diante ficam as classes
específicas do domínio: `.badge`, `.metric`, `.investment-card`, `.status-dot`, `.avatar`,
`.profile-image` e `.file-drop`.

São essas classes, e não os utilitários, que as quatro páginas reutilizam. Elas garantem
espaçamentos e cores padronizados sem depender de build step nem de pré-processador durante a
prototipagem.

Este arquivo é o mesmo nas treze etapas. O vocabulário visual do produto não muda ao longo da
trilha — o que muda é como os utilitários do Tailwind chegam até ele, e isso é assunto da
etapa 2.

<SourceCode
  path="examples/courses/expressjs/projects/invest-app-static/css/components.css"
  title="css/components.css"
  showLineNumbers
  mark="148-208"
  collapse="1-40"
/>

### TK01.3 · Criar `signup.html` (Tela de criação de conta)

<HtmlPreview path="examples/courses/expressjs/projects/invest-app-static/signup.html" height="24rem" serveFromPublic allowScripts />

O cartão centralizado das linhas 12 a 25 repete a moldura que o `signin.html` também usa, e por isso está recolhido: é a mesma combinação de `card w-full max-w-lg p-6` com logo e título, centralizada por `grid min-h-screen place-items-center`.

O `<form class="mt-6 grid gap-4">` destacado nas linhas 26 a 73 é a novidade da tela. Ele declara os quatro campos do cadastro pelos atributos `name`: `name="name"` na linha 33, `name="email"` na linha 44, `name="password"` na linha 55 e `name="confirmationPassword"` na linha 67. Esses nomes são exatamente o corpo que a etapa 8 vai enviar em `POST /api/users`.

O botão da linha 72 também usa `type="button"`: a tela define a experiência de onboarding, mas o envio real — com o hash da senha no servidor — só entra quando existir uma API para receber o formulário.

<SourceCode
  path="examples/courses/expressjs/projects/invest-app-static/signup.html"
  title="signup.html"
  showLineNumbers
  mark="26-73"
  collapse="1-25,74-80"
/>

### TK01.4 · Criar `signin.html` (Tela de autenticação)

<HtmlPreview path="examples/courses/expressjs/projects/invest-app-static/signin.html" height="24rem" serveFromPublic allowScripts />

O login é a tela mais enxuta do projeto e reaproveita a mesma moldura do cadastro nas linhas 12 a 25, recolhidas por serem idênticas às do `signup.html`.

O formulário destacado nas linhas 26 a 51 reduz a entrada a dois campos: `name="email"` na linha 33 e `name="password"` na linha 44. O botão da linha 50 fecha o formulário, e o link do rodapé leva ao cadastro.

Esses dois campos são a interface que, na etapa 9, vai receber o token JWT devolvido por `POST /api/signin` e guardá-lo no `localStorage`.

<SourceCode
  path="examples/courses/expressjs/projects/invest-app-static/signin.html"
  title="signin.html"
  showLineNumbers
  mark="26-51"
  collapse="1-25,52-58"
/>

### TK01.5 · Criar `profile.html` e `public/imgs/profile/avatar.png` (Tela de perfil)

<HtmlPreview path="examples/courses/expressjs/projects/invest-app-static/profile.html" height="24rem" serveFromPublic allowScripts />

A tela de perfil divide a área principal em dois cartões lado a lado, e é a grade da linha 30 que os posiciona: `md:grid-cols-[minmax(0,1.2fr)_minmax(18rem,0.8fr)]` dá mais espaço ao cartão de identidade do que ao de upload.

O primeiro trecho destacado, nas linhas 36 a 49, mostra a identidade do usuário: a tag `<img class="profile-image">` da linha 37 carrega o `avatar.png` que acompanha o projeto, e as linhas 40 a 49 exibem nome e e-mail como texto. São texto, e não campos de formulário, porque nesta etapa não há de onde carregá-los — a partir da etapa 11 o `profile.js` preenche esses mesmos parágrafos com o que a API devolve.

O segundo trecho destacado, nas linhas 52 a 63, é o formulário de upload: o `<label class="file-drop">` da linha 58 embrulha o `<input type="file">` da linha 60, de modo que a área inteira vira alvo de clique. Ele deixa o painel pronto para a etapa 11, quando esse mesmo bloco passa a disparar um `FormData` para o endpoint de upload com Multer.

<SourceCode
  path="examples/courses/expressjs/projects/invest-app-static/profile.html"
  title="profile.html"
  showLineNumbers
  mark="36-49,52-63"
  collapse="1-27"
/>

## Executando

<Steps>

1. Entre no exemplo estático:

   ```bash title="Terminal"
   cd examples/courses/expressjs/projects/invest-app-static
   ```

2. Abra o HTML diretamente no navegador ou gere a versão otimizada:

   <PackageManagerTabs />

   ```bash title="Terminal"
   npm run build
   npm run preview
   ```

3. Navegue entre `index.html`, `signup.html`, `signin.html` e `profile.html`.

</Steps>

O `vite.config.js` que faz isso é curto e tem uma decisão em cada bloco. O `base: './'` da linha
14 gera caminhos relativos, o que permite abrir o `dist/` direto do disco; o `publicDir` da linha
16 marca `public/` como pasta de arquivos copiados sem processamento — é de lá que sai o
`avatar.png`; e o `rollupOptions.input` das linhas 22 a 27 lista as **quatro** páginas. Sem essa
lista, o build levaria só a primeira e as outras três sumiriam do `dist/`.

<SourceCode
  path="examples/courses/expressjs/projects/invest-app-static/vite.config.js"
  title="vite.config.js"
  showLineNumbers
  mark="14,16,22-27"
/>

<Aside type="note" title="O Vite entra só como build, e não como servidor">
  O `npm run build` é a única ferramenta do front em todo o projeto, e o que ela faz é empacotar:
  os módulos das próximas etapas viram um arquivo só, o CSS é minificado e cada arquivo ganha um
  hash no nome, o que resolve cache de navegador sem esforço.

  O que ele **não** faz é servir o front. Da etapa 2 em diante quem entrega as páginas é o
  próprio Express, na mesma origem da API — e é por isso que o InvestApp nunca precisa de CORS.
  O [MonitorApp](../../monitorapp/api/) usa o Vite do outro jeito, como servidor em outra porta,
  e paga o preço de precisar de proxy em desenvolvimento e de CORS em produção.
</Aside>

<Aside type="tip" title="Por que começar sem JavaScript">
  Separar primeiro a experiência visual evita misturar três problemas ao mesmo tempo:
  layout, contrato HTTP e estado de tela. A próxima etapa mantém esse front e conecta o
  formulário à API usando JavaScript vanilla.
</Aside>

## Conceitos abordados

- Estrutura de páginas HTML sem build
- Play CDN do Tailwind para prototipar sem build
- A fronteira entre utilitário genérico e componente do produto
- Formulários preparados para etapas futuras
- Avatar e navegação de perfil já previstos
- Evolução incremental sem framework de front-end

## Próxima etapa

[InvestApp: API em memória](../api/) — o Express passa a servir o front e as rotas de
investimento entram em `/api`.
