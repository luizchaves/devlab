---
title: "Express.js: Validação"
description: "Validação de entrada em uma API Express: as camadas de validação, middleware nativo de fábrica, schemas com Zod, restrições no banco de dados e o formato da resposta de erro de validação."
course: expressjs
sidebar:
  label: "Validação"
---
import { Aside, TabItem, Tabs } from '@astrojs/starlight/components';
import ApiRequest from '@components/ApiRequest.astro';
import ApiResponse from '@components/ApiResponse.astro';
import Mermaid from '@components/Mermaid.astro';
import SourceCode from '@components/SourceCode.astro';

Todo dado que chega pela rede é hostil até prova em contrário: pode faltar, vir com o tipo
errado, com o tamanho errado ou com conteúdo malicioso. Esta aula trata de barrar isso antes
que chegue ao controller.

Materiais: [slides da aula](../../../../slides/courses/expressjs/api/validation/) e [mapa mental](../../../../mindmaps/courses/expressjs/api/validation/).

## Objetivo

Ao final você saberá em quais camadas a validação precisa existir, escrever um middleware de
validação sem dependências, descrever regras com um schema e responder erros de validação em
um formato que o front-end consiga usar.

## Validação em camadas

Validar em um lugar só é insuficiente — cada camada protege de uma coisa diferente:

<Mermaid title="Camadas de validação de uma requisição, do navegador ao banco">
{`
flowchart TD
  A["Formulário HTML<br/>required, type, pattern"] -->|"experiência"| B["Middleware de validação<br/>formato, tipo, tamanho"]
  B -->|"contrato da API"| C["Regra de negócio<br/>no controller ou serviço"]
  C -->|"consistência"| D["Banco de dados<br/>NOT NULL, UNIQUE, CHECK"]
  D --> E[("Dado íntegro")]
`}
</Mermaid>

| Camada        | Protege de                                  | Pode ser burlada?                    |
| ------------- | ------------------------------------------- | ------------------------------------ |
| Formulário    | erro de digitação, ida desnecessária ao servidor | sim — basta usar `curl`          |
| Middleware    | formato, tipo, tamanho, campo ausente        | não, se registrado antes da rota     |
| Regra de negócio | estado inválido no domínio                | não                                  |
| Banco         | dado inconsistente vindo de qualquer origem  | não — vale até para seed e migração  |

<Aside type="danger" title="Validação no front-end não é validação">
  O HTML `required` melhora a experiência e nada mais: qualquer pessoa envia a requisição
  direto para a API. Toda regra que importa precisa existir também no servidor.
</Aside>

## Middleware de validação nativo

O padrão de fábrica da aula de [Middleware](../../basics/middleware/) resolve o caso mais
comum — campos obrigatórios — sem nenhuma dependência:

```ts showLineNumbers {1,4,10}
export function requireFields(...campos: string[]) {
  return (req: Request, _res: Response, next: NextFunction) => {
    const body = (req.body ?? {}) as Record<string, unknown>;

    const faltando = campos.filter((campo) => body[campo] === undefined || body[campo] === '');

    if (faltando.length > 0) {
      throw new HttpError(422, `Campos obrigatórios: ${faltando.join(', ')}`);
    }

    next();
  };
}
```

```ts showLineNumbers {1}
router.post('/', requireJson, requireFields('title'), taskController.store);
```

Regras de formato também cabem em funções pequenas e testáveis:

```ts showLineNumbers {5,9}
const DATA = /^\d{4}-\d{2}-\d{2}$/;

export function validateTask(body: TaskInput): string[] {
  const erros: string[] = [];

  if (!body.title || body.title.trim().length < 3) {
    erros.push('title: informe ao menos 3 caracteres');
  }

  if (body.dueDate && !DATA.test(body.dueDate)) {
    erros.push('dueDate: use o formato YYYY-MM-DD');
  }

  if (body.priority && !['low', 'medium', 'high'].includes(body.priority)) {
    erros.push('priority: use low, medium ou high');
  }

  return erros;
}
```

<Aside type="caution" title="Nem tudo cabe em regex">
  Validar e-mail por expressão regular só filtra erros grosseiros — a gramática real de um
  endereço válido é longa demais. Para o que importa (o endereço existe e é do usuário?), a
  única prova é enviar um e-mail de confirmação, assunto da aula de
  [Envio de E-mail](../../advanced/email/).
</Aside>

## O que é o Zod

Quando as regras crescem, descrevê-las declarativamente evita dezenas de `if`. O
[Zod](https://zod.dev/) define o formato uma vez e ainda **infere o tipo TypeScript** a
partir dele.

Um schema Zod é um objeto que recebe um valor desconhecido, valida a forma desse valor e
devolve um resultado tipado. Na API, isso combina bem com Express porque `req.body`,
`req.params` e `req.query` entram como dados externos: o TypeScript não consegue provar em
tempo de execução que eles têm o formato prometido.

O fluxo normal é sempre o mesmo: a rota entrega os dados ao schema, o schema aceita ou recusa,
e o controller só roda depois que o middleware substitui a promessa por um valor validado.

## Schemas mais usados

Os schemas abaixo cobrem a maior parte das entradas HTTP de um CRUD. Eles aparecem isolados
na tabela, mas na prática quase sempre são compostos dentro de `z.object()` ou
`z.strictObject()`:

| Schema ou método | O que valida | Uso comum em APIs |
| ---------------- | ------------ | ----------------- |
| `z.string()` | texto | nomes, títulos, descrições, filtros de busca |
| `z.number()` | número já convertido | valores vindos de JSON, como preço ou quantidade |
| `z.boolean()` | booleano real | flags no corpo JSON, como `done` ou `active` |
| `z.coerce.number()` | string convertida para número | `params` e `query`, como `:id`, `page` e `perPage` |
| `z.enum([...])` | um valor dentro de uma lista fechada | status, prioridade, ordenação e papéis de usuário |
| `z.uuid()` | UUID válido | ids de rota quando o banco usa UUID |
| `z.iso.date()` | data `YYYY-MM-DD` | datas de vencimento, filtros por período e formulários |
| `z.object({...})` | objeto com campos nomeados | composição de `body`, `params` e `query` |
| `z.strictObject({...})` | objeto sem campos extras | `body` de criação ou edição quando a API deve recusar chaves desconhecidas |
| `z.array(schema)` | lista de itens válidos | tags, ids selecionados e itens de um pedido |
| `.optional()` | campo que pode faltar | filtros de query e campos parciais |
| `.nullable()` / `.nullish()` | campo que aceita `null` ou `null`/`undefined` | dados opcionais que podem ser limpos pelo cliente |
| `.default(valor)` | valor padrão quando o campo falta | paginação, ordenação e flags com padrão |
| `.refine(fn)` | regra customizada | validação de domínio, como endereço de host ou regra entre campos |

<Aside type="tip" title="Schema pequeno, composição explícita">
  Prefira criar schemas pequenos por fonte de dados (`body`, `params`, `query`) e compor o
  schema final da rota com eles. Isso deixa claro se a rota aceita corpo, parâmetro, query ou
  uma combinação dos três.
</Aside>

## Schemas com Zod no Express

No projeto modelo, o `HttpError` ganha um terceiro parâmetro opcional — os detalhes por
campo — em vez de uma subclasse só para isso:

<SourceCode
  path="examples/courses/express/projects/task-api-validation/src/errors/HttpError.ts"
  title="src/errors/HttpError.ts"
  region="class"
/>

O `errorHandler` passa a incluir `issues` quando ele existir:

<SourceCode
  path="examples/courses/express/projects/task-api-validation/src/middlewares/error-handler.ts"
  title="src/middlewares/error-handler.ts"
  region="handler"
/>

Com isso resolvido, o schema descreve as regras e o middleware apenas o executa. Repare que
o schema não valida só o corpo: `body`, `params` e `query` entram na mesma passagem, porque
os três vêm do cliente e os três podem mentir.

<Tabs syncKey="validation">
  <TabItem label="Schema">
    <SourceCode
      path="examples/courses/express/projects/task-api-validation/src/schemas/task.ts"
      title="src/schemas/task.ts"
      region="schemas"
    />
  </TabItem>

  <TabItem label="Middleware">
    <SourceCode
      path="examples/courses/express/projects/task-api-validation/src/middlewares/validate.ts"
      title="src/middlewares/validate.ts"
      region="validate"
    />
  </TabItem>

  <TabItem label="Uso">
    <SourceCode
      path="examples/courses/express/projects/task-api-validation/src/routes/task-router.ts"
      title="src/routes/task-router.ts"
      region="routes"
    />
  </TabItem>
</Tabs>

`strictObject` é o que transforma "validação" em **validação estrita**: um corpo com um
campo que o schema não declara é recusado, em vez de ter o campo silenciosamente removido.

<SourceCode
  path="examples/courses/express/projects/task-api-validation/src/schemas/task.ts"
  title="src/schemas/task.ts"
  region="body"
/>

| Critério                     | Validação manual        | Zod                                |
| ---------------------------- | ----------------------- | ---------------------------------- |
| Dependência                  | nenhuma                 | uma dependência de produção        |
| Regras simples               | curto e claro           | curto e claro                      |
| Objetos aninhados e arrays   | verboso e repetitivo    | declarativo                        |
| Tipo TypeScript              | escrito à mão, separado | inferido do schema                 |
| Conversão (`"3"` → `3`)      | manual                  | `z.coerce.number()`                |
| Mensagem por campo           | montada à mão           | vem pronta em `error.issues`       |

<Aside type="tip" title="`safeParse` em vez de `parse`">
  `parse` lança uma exceção do Zod que o `errorHandler` não conhece e trataria como `500`.
  `safeParse` devolve `{ success, data | error }` e deixa a decisão do status com a
  aplicação.
</Aside>

## O formato do erro de validação

Um erro de validação precisa dizer **qual campo** falhou — senão o front-end não consegue
destacar o input. Uma mensagem única em texto obriga a adivinhação:

<ApiRequest method="POST" path="/tasks" baseUrl="http://localhost:3000">
{`
  {
    "title": "ab",
    "priority": "urgentissima"
  }
`}
</ApiRequest>

<ApiResponse status={422}>
{`
  {
    "error": {
      "status": 422,
      "message": "Dados invalidos",
      "issues": [
        {
          "code": "too_small",
          "path": ["body", "title"],
          "message": "O titulo deve ter no minimo 3 caracteres"
        },
        {
          "code": "invalid_value",
          "path": ["body", "priority"],
          "message": "Invalid option: expected one of \\"low\\"|\\"medium\\"|\\"high\\""
        }
      ]
    }
  }
`}
</ApiResponse>

O `path` é um array porque o schema valida as três fontes de uma vez: o primeiro elemento
diz **de onde** veio o campo (`body`, `params` ou `query`) e o resto é o caminho dentro
dela. Um front-end que precise de um mapa plano converte em uma linha:

```ts showLineNumbers {3}
function formatIssues(error: ZodError): Record<string, string> {
  return Object.fromEntries(
    error.issues.map((issue) => [issue.path.slice(1).join('.'), issue.message]),
  );
}
```

<Aside type="note" title="`400` ou `422`?">
  `400 Bad Request` cobre a requisição malformada — JSON quebrado, `Content-Type` ausente.
  `422 Unprocessable Content` cobre a requisição bem formada com conteúdo semanticamente
  inválido — que é o caso da validação. Muita API usa `400` para os dois; o importante é ser
  consistente e documentar.
</Aside>

## Restrições no banco

A última camada é a única que vale para **toda** origem — inclusive um seed, uma migração ou
uma consulta manual. É por isso que ela não é opcional:

| Restrição SQL            | Garante                                | Equivalente no Prisma            |
| ------------------------ | -------------------------------------- | -------------------------------- |
| `NOT NULL`               | a coluna sempre tem valor              | campo sem `?`                    |
| `UNIQUE`                 | não há duplicata                       | `@unique`                        |
| `DEFAULT`                | valor quando o campo é omitido         | `@default(...)`                  |
| `CHECK`                  | condição sobre o valor                 | não há — use uma migração manual |
| `FOREIGN KEY`            | a referência existe                    | `@relation(...)`                 |

```sql title="Restrições na criação da tabela" showLineNumbers {3,5,7}
CREATE TABLE "Task" (
  "id"        INTEGER  NOT NULL PRIMARY KEY,
  "title"     TEXT     NOT NULL CHECK (LENGTH(title) >= 3),
  "done"      BOOLEAN  NOT NULL DEFAULT 0,
  "priority"  TEXT     NOT NULL DEFAULT 'medium'
                       CHECK (priority IN ('low', 'medium', 'high')),
  "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX "Task_title_key" ON "Task"("title");
```

A violação chega ao Express como erro da biblioteca e precisa de tradução — `UNIQUE` vira
`409`, como visto em [Tratamento de Erros](../error-handling/).

<Aside type="caution" title="A checagem prévia não substitui a restrição">
  `if (Task.findByTitle(title))` antes do `INSERT` deixa uma janela entre a consulta e a
  escrita: duas requisições simultâneas passam pelos dois `if` e gravam o mesmo título.
  Só o índice `UNIQUE` impede de verdade — a checagem prévia serve para a mensagem bonita.
</Aside>

## Exercício

Partindo da [etapa 4 da TaskAPI](../../practice/taskapi/typescript/), que ainda valida com
`if` dentro do controller:

1. Escreva `requireFields` conforme a aula e aplique-o em `POST /tasks`.
2. Acrescente `validateTask` com as regras de título, data e prioridade.
3. Faça a resposta de erro trazer o mapa `issues` com um campo por chave.
4. Responda `422`, e não `400`, para a falha de validação.
5. Confirme com `curl` que enviar `{}` lista todos os campos de uma vez, e não apenas o
   primeiro.

Depois compare o resultado com a [etapa 5](../../practice/taskapi/validation/), que faz o
mesmo com Zod.

<details>
  <summary>Possível resposta</summary>

  ```ts showLineNumbers
  import type { NextFunction, Request, Response } from 'express';

  import { HttpError } from '#errors/HttpError.ts';
  import { validateTask } from '#validators/task.ts';

  export function validateTaskBody(req: Request, _res: Response, next: NextFunction) {
    const erros = validateTask((req.body ?? {}) as TaskInput);

    if (erros.length > 0) {
      const issues = Object.fromEntries(
        erros.map((erro) => erro.split(': ') as [string, string]),
      );

      throw new HttpError(422, 'Dados inválidos', issues);
    }

    next();
  }
  ```

  Reunir **todos** os erros antes de responder é o ponto: validar campo a campo e responder
  no primeiro obriga o usuário a descobrir os problemas um por vez.
</details>

## Desafio

Acrescente à etapa 5 um schema para o recurso `tags`, com `name` obrigatório e `color` no
formato `#rrggbb`, e uma rota `POST /tags` validada por ele. Depois faça o `errorHandler`
converter as `issues` do Zod para o mapa plano `{ campo: mensagem }` antes de responder, e
avalie: o que o front-end ganha e o que perde com essa conversão?

## Perguntas de revisão

### Camadas

1. **Por que validar no front-end não dispensa validar no servidor?**

   <details>
     <summary>Possível resposta</summary>

     Porque o cliente é controlado por quem chama: qualquer pessoa envia a requisição direto
     com `curl`. A validação do formulário melhora a experiência; a do servidor é a que
     protege os dados.
   </details>

2. **Por que manter restrições no banco se o middleware já validou?**

   <details>
     <summary>Possível resposta</summary>

     Porque o banco é a única camada que vale para toda origem — seed, migração, script,
     consulta manual — e a única que resolve corridas entre requisições simultâneas.
   </details>

### Implementação

3. **Por que `safeParse` e não `parse`?**

   <details>
     <summary>Possível resposta</summary>

     `parse` lança uma exceção que o `errorHandler` não reconhece e trataria como `500`.
     `safeParse` devolve um resultado e deixa a aplicação escolher o status e o formato do
     erro.
   </details>

4. **Qual a vantagem de substituir `req.body` pelo dado validado?**

   <details>
     <summary>Possível resposta</summary>

     O controller passa a trabalhar com o dado já convertido e com valores padrão aplicados,
     e o tipo inferido do schema corresponde ao que está lá de fato — não a uma promessa.
   </details>

5. **Por que reunir todos os erros antes de responder?**

   <details>
     <summary>Possível resposta</summary>

     Porque responder no primeiro erro obriga o usuário a submeter o formulário várias vezes
     para descobrir todos os problemas. Um mapa `campo → mensagem` permite ao front-end
     destacar tudo de uma vez.
   </details>

## Referências

- [Zod](https://zod.dev/)
- [Express — Error handling](https://expressjs.com/en/guide/error-handling.html)
- [OWASP — Input Validation Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Input_Validation_Cheat_Sheet.html)
- [422 Unprocessable Content | MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Status/422)

## Na prática

O projeto executável desta aula é a
[etapa 5 da TaskAPI](../../practice/taskapi/validation/), onde os schemas Zod validam
`body`, `params` e `query` antes de qualquer controller.

## Próxima aula

[Paginação, Filtros e Ordenação](../pagination/): parâmetros de consulta em coleções que
crescem.
