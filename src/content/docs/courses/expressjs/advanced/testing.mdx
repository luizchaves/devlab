---
title: "Express.js: Testes"
description: "Testes de uma API Express: node:test nativo, testes de unidade e de integração com supertest, banco de testes isolado, cobertura e testes de ponta a ponta com Playwright."
course: expressjs
sidebar:
  label: "Testes"
---
import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import Mermaid from '@components/Mermaid.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';

Testar manualmente com o `requests.http` funciona até a décima rota. Depois disso, cada
alteração exige repetir o mesmo roteiro — e é aí que as regressões passam. Este tópico
automatiza essa verificação.

Materiais: [slides do tópico](../../../../slides/courses/expressjs/advanced/testing/) e [mapa mental](../../../../mindmaps/courses/expressjs/advanced/testing/).

## Objetivo

Ao final você saberá escrever testes de unidade e de integração com o executor nativo do
Node, fazer requisições ao `app` sem subir servidor, isolar o banco de testes e medir
cobertura.

## Os três níveis

Cada nível responde a uma pergunta diferente e tem um custo diferente:

<Mermaid title="Pirâmide de testes de uma API Express">
{`
flowchart TD
  A["Ponta a ponta (poucos)<br/>navegador → front → API → banco"] --> B["Integração (vários)<br/>requisição HTTP → rota → model → banco"]
  B --> C["Unidade (muitos)<br/>uma função pura, sem I/O"]
`}
</Mermaid>

| Nível        | O que exercita                     | Velocidade | Ferramenta            |
| ------------ | ---------------------------------- | ---------- | --------------------- |
| Unidade      | uma função isolada                  | ms         | `node:test`           |
| Integração   | rota + middlewares + model + banco  | dezenas de ms | `node:test` + `supertest` |
| Ponta a ponta| navegador operando a interface      | segundos   | Playwright            |

<Aside type="tip" title="Em uma API, o teste de integração é o que mais paga">
  Ele cobre o caminho completo (validação, autorização, status, formato da resposta) com
  custo baixo. Testes de unidade valem para lógica pura (hash, JWT, cálculo); os de ponta a
  ponta, para os dois ou três fluxos críticos.
</Aside>

## O executor nativo

Desde o Node 20, `node --test` é estável: não há Jest, Vitest nem configuração para
instalar.

```json title="package.json" showLineNumbers {3-5}
{
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "test:coverage": "node --test --experimental-test-coverage"
  }
}
```

O vocabulário é o mesmo de qualquer framework de testes:

<ProjectLinks path="examples/courses/expressjs/projects/task-api-test" devcontainer="task-api-auth" />

<FileTree>
{`
  task-api-test/
  ├── package.json
  ├── tsconfig.json
  ├── prisma/
  │   └── schema.prisma
  └── src/
      ├── app.ts
      ├── server.ts
      ├── controllers/
      │   ├── auth-controller.ts
      │   └── investment-controller.ts
      ├── middlewares/
      │   ├── authenticate.ts
      │   ├── authorize.ts
      │   └── error-handler.ts
      ├── models/
      │   ├── task-model.ts
      │   └── user-model.ts
      ├── routes/
      │   ├── auth-router.ts
      │   ├── auth-router.test.ts
      │   ├── task-router.ts
      │   └── task-router.test.ts
      └── utils/
          ├── jwt.ts
          ├── jwt.test.ts
          ├── password.ts
          └── password.test.ts
`}
</FileTree>

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/utils/jwt.test.ts"
  title="src/utils/jwt.test.ts"
  showLineNumbers
  mark="1-2,12-18"
/>

```bash title="Terminal"
npm test
```

```txt title="Output" frame="terminal"
▶ verifyJwt
  ✔ devolve o payload de um token valido (0.19ms)
  ✔ rejeita um token adulterado (0.18ms)
  ✔ rejeita um token expirado (0.07ms)
✔ verifyJwt (0.54ms)
ℹ tests 21
ℹ pass 21
ℹ fail 0
```

<Aside type="note" title="`node:assert/strict` em vez de `expect`">
  `assert.equal` já compara com `===`, `assert.deepEqual` compara estruturas e
  `assert.throws` verifica exceções. A API é menor que a de um framework — e não precisa ser
  instalada.
</Aside>

## Teste de unidade

O melhor candidato é a função pura, sem I/O. As funções de senha e de token do tópico de
[Senhas e Hash](../../auth/passwords/) são exemplos exatos:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/utils/password.test.ts"
  title="src/utils/password.test.ts"
  lines="6-15"
  showLineNumbers
/>

Repare que o teste verifica **propriedades**, não valores fixos: o hash muda a cada execução,
então comparar com uma constante seria impossível.

## Teste de integração com `supertest`

O [`supertest`](https://www.npmjs.com/package/supertest) faz requisições diretamente ao
`app`, sem abrir porta — é por isso que `app.ts` e `server.ts` são arquivos separados desde a
tópico de [Rotas](../../basics/routes/).

<PackageManagerTabs package="supertest @types/supertest" dev />

A suíte do cadastro fixa três decisões que as tópicos anteriores tomaram: o `201` com o
recurso criado, o `409` no e-mail repetido e (a mais importante) a senha **fora** da
resposta.

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/auth-router.test.ts"
  title="src/routes/auth-router.test.ts"
  region="cases"
/>

O `before` e o `after` deixam o banco como estavam, para que a suíte possa rodar duas vezes
seguidas:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/auth-router.test.ts"
  title="src/routes/auth-router.test.ts"
  region="setup"
/>

<Aside type="tip" title="Teste o erro tanto quanto o sucesso">
  Um teste que só cobre o caminho feliz não percebe quando a validação some. A regra prática:
  para cada rota, um teste de sucesso e um por status de erro previsto no contrato.
</Aside>

## Testando rotas protegidas

O token é obtido no próprio teste, com o mesmo fluxo que o cliente usa:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/task-router.test.ts"
  title="src/routes/task-router.test.ts"
  region="guards"
/>

E o teste que realmente importa em uma API multiusuário — o de autorização. Regra de
segurança sem teste é esperança:

<SourceCode
  path="examples/courses/expressjs/projects/task-api-test/src/routes/task-router.test.ts"
  title="src/routes/task-router.test.ts"
  region="isolation"
/>

<Aside type="tip" title="Assertar o status, não só o dado">
  Verificar `404` em vez de `403` parece detalhe e não é: é a diferença entre uma API que
  esconde a existência dos recursos alheios e uma que permite enumerá-los pelos status das
  respostas.
</Aside>

## Isolando o banco

Um teste que grava no banco de desenvolvimento apaga os dados de quem está desenvolvendo — e
falha na segunda execução. Três estratégias:

<Tabs syncKey="test-db">
  <TabItem label="Banco separado">
    ```ini title=".env.test"
    DATABASE_URL="file:./test.db"
    ```

    ```json title="package.json"
    { "scripts": { "test": "node --env-file=.env.test --test" } }
    ```

    Estrutura real, dados descartáveis. É o padrão para projetos com Prisma.
  </TabItem>

  <TabItem label="SQLite em memória">
    ```ini title=".env.test"
    DATABASE_URL="file::memory:?cache=shared"
    ```

    Rápido e sem resíduo em disco; some ao final do processo.
  </TabItem>

  <TabItem label="Estado por teste">
    ```ts showLineNumbers {2}
    beforeEach(async () => {
      await resetDatabase();   // limpa tabelas e reaplica o seed
    });
    ```

    Garante que a ordem dos testes não importe.
  </TabItem>
</Tabs>

<Aside type="caution" title="Teste que depende da ordem é teste quebrado">
  Se o segundo teste só passa porque o primeiro criou um registro, o conjunto falha ao rodar
  em paralelo ou isolado. `beforeEach` com estado conhecido resolve — e é o que permite usar
  `--test-concurrency`.
</Aside>

## Cobertura

O Node mede cobertura sem ferramenta adicional:

```bash title="Terminal"
npm run test:coverage
```

```txt title="Output" frame="terminal"
# start of coverage report
# file                          | line % | branch % | funcs %
# src/controllers/…controller.ts |  92.31 |    83.33 |  100.00
# src/middlewares/authorize.ts   |  76.47 |    62.50 |  100.00
# src/utils/password.ts          | 100.00 |   100.00 |  100.00
# all files                      |  88.14 |    79.17 |   97.06
# end of coverage report
```

<Aside type="caution" title="Cobertura alta não é qualidade">
  Ela mede quais linhas **executaram**, não se o comportamento está correto. Um teste sem
  nenhum `assert` produz cobertura idêntica a um teste rigoroso. Use o número para achar o
  que **não** foi testado — nunca como meta.
</Aside>

Duas ferramentas de foco ajudam durante o desenvolvimento:

```ts title="Isolando e pulando testes"
it.only('só este roda no arquivo', () => {});

it.skip('ignorado, mas ainda aparece no relatório', () => {});

it.todo('ainda a escrever');
```

## Ponta a ponta com Playwright

O teste de ponta a ponta abre um navegador real e opera a interface — front-end, API e banco
juntos:

```bash title="Terminal"
npm init playwright@latest
```

```ts showLineNumbers {5,10,14}
import { expect, test } from '@playwright/test';

test('cadastra um investimento pela interface', async ({ page }) => {
  await page.goto('/signin.html');
  await page.fill('#email', 'ana@example.com');
  await page.fill('#password', 'senha-secreta');
  await page.click('button[type="submit"]');

  await page.goto('/home.html');
  await page.fill('#name', 'Tesouro Selic 2029');
  await page.fill('#amount', '20000');
  await page.click('#salvar');

  await expect(page.locator('.investment-list')).toContainText('Tesouro Selic 2029');
});
```

<Aside type="note" title="Poucos, e nos fluxos que importam">
  Testes de ponta a ponta são lentos e mais frágeis: uma mudança de layout os quebra. Dois ou
  três (login, o fluxo principal, o pagamento) dão a maior parte da confiança.
</Aside>

## Executando

<Steps>

1. Rode a suíte completa:

   ```bash title="Terminal"
   npm test
   ```

2. Deixe rodando enquanto desenvolve:

   ```bash title="Terminal"
   npm run test:watch
   ```

3. Veja o que ficou sem cobertura:

   ```bash title="Terminal"
   npm run test:coverage
   ```

</Steps>

## Exercício

No projeto `express-auth`:

1. Instale o `supertest` e escreva `src/routes/auth-router.test.ts`.
2. Cubra: cadastro com sucesso (`201`), senha curta (`422`), e-mail duplicado (`409`), login
   correto (`200` com token) e senha errada (`401`).
3. Escreva um teste que confirme que **nenhuma** resposta contém a chave `password`.
4. Escreva o teste de autorização: um usuário não apaga o investimento de outro.
5. Rode com cobertura e identifique qual arquivo está menos coberto.

<details>
  <summary>Possível resposta</summary>

  ```ts showLineNumbers {8-10}
  describe('POST /auth/signup', () => {
    it('não devolve a senha em nenhuma forma', async () => {
      const response = await request(app).post('/auth/signup').send({
        name: 'Teste',
        email: `teste-${Date.now()}@example.com`,
        password: 'senha-secreta',
      });

      assert.equal(response.status, 201);
      assert.equal(response.body.password, undefined);
      assert.ok(!JSON.stringify(response.body).includes('argon2'));
    });
  });
  ```

  O `Date.now()` no e-mail evita colisão entre execuções — alternativa a limpar o estado no
  `beforeEach`.
</details>

## Desafio

Configure a suíte para rodar em uma GitHub Action a cada push: `npm ci`, `npm run typecheck`
e `npm test`, com `DATABASE_URL` apontando para um banco de teste. Faça a ação falhar
propositalmente removendo uma validação e confirme que o teste correspondente acusa.

## Perguntas de revisão

### Níveis

1. **Por que o teste de integração é o mais valioso em uma API?**

   <details>
     <summary>Possível resposta</summary>

     Porque exercita o caminho completo — validação, autorização, status, formato da
     resposta — com custo baixo. É onde moram as regressões que mais afetam quem consome a
     API.
   </details>

2. **Por que `app.ts` precisa ser separado de `server.ts` para testar?**

   <details>
     <summary>Possível resposta</summary>

     Porque o `supertest` importa o `app` e faz requisições sem abrir porta. Com o `listen`
     no mesmo arquivo, cada arquivo de teste tentaria subir um servidor na mesma porta.
   </details>

### Prática

3. **Por que testar os status de erro, e não só o sucesso?**

   <details>
     <summary>Possível resposta</summary>

     Porque a validação e a autorização são exatamente o que some sem ninguém perceber. Um
     teste que só cobre o caminho feliz continua passando com a API aberta.
   </details>

4. **O que 100% de cobertura garante?**

   <details>
     <summary>Possível resposta</summary>

     Que todas as linhas executaram durante os testes — nada além disso. Um teste sem
     `assert` produz a mesma cobertura que um rigoroso. O número serve para achar o que não
     foi testado.
   </details>

5. **Por que um teste não deve depender de outro?**

   <details>
     <summary>Possível resposta</summary>

     Porque a suíte deixa de funcionar isolada ou em paralelo, e a falha de um teste passa a
     derrubar vários outros — escondendo a causa real.
   </details>

## Referências

- [Node.js — Test runner](https://nodejs.org/api/test.html)
- [Node.js — `assert`](https://nodejs.org/api/assert.html)
- [`supertest`](https://www.npmjs.com/package/supertest)
- [Playwright](https://playwright.dev/)

## Na prática

O projeto executável deste tópico é a
[etapa 12 da TaskAPI](../../practice/taskapi/test/), com 21 testes — de unidade e de rota —
incluindo o que prova o isolamento por dono.

## Próximo tópico

[Deploy](../deploy/): colocar tudo isso no ar.
