---
title: "Express.js: Rotas"
description: "Roteamento no Express: caminhos e padrões, parâmetros de rota, query string e corpo, encadeamento de handlers e modularização com express.Router()."
course: expressjs
sidebar:
  label: "Rotas"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import ApiRequest from '@components/ApiRequest.astro';
import ApiResponse from '@components/ApiResponse.astro';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import Mermaid from '@components/Mermaid.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Roteamento é a decisão de **qual função responde a qual requisição**. Esta aula percorre as
formas de escrever um caminho, os três lugares por onde os dados chegam e a modularização
com `express.Router()`.

Materiais: [slides da aula](../../../../slides/courses/expressjs/basics/routes/) e [mapa mental](../../../../mindmaps/courses/expressjs/basics/routes/).

## Objetivo

Ao final você saberá declarar rotas com parâmetros, ler dados de `req.params`, `req.query` e
`req.body`, encadear handlers em uma mesma rota e extrair um conjunto de rotas para um
módulo próprio montado sob um prefixo.

## Caminho, método e handler

Uma rota casa quando o **método** e o **caminho** batem. O Express avalia na ordem de
registro e para na primeira que casar:

```js title="Ordem de avaliação" showLineNumbers {1,5}
app.get('/users/active', listActive); // avaliada primeiro
app.get('/users/:id', showUser);

// Invertidas, '/users/active' cairia em '/users/:id'
// com req.params.id === 'active'.
```

Os padrões de caminho aceitos pelo Express 5 são poucos e explícitos:

| Padrão            | Casa com                          | Não casa com          |
| ----------------- | --------------------------------- | --------------------- |
| `/users`          | `/users`                          | `/users/1`            |
| `/users/:id`      | `/users/1`, `/users/abc`          | `/users`, `/users/1/x`|
| `/users/:id?`     | `/users`, `/users/1`              | `/users/1/x`          |
| `/files/*splat`   | `/files/a`, `/files/a/b/c.png`    | `/files`              |
| `/^\/ab+c$/`      | `/abc`, `/abbc` (expressão regular)| `/ac`                 |

<Aside type="caution" title="Express 5 mudou os curingas">
  No Express 4 escrevia-se `'*'` e `'/files/*'`. No Express 5 o curinga precisa ser
  **nomeado** — `'/{*splat}'` e `'/files/*splat'` — e o valor chega em `req.params.splat`
  como um array de segmentos. Código antigo copiado da internet costuma quebrar aqui.
</Aside>

## Os três lugares por onde os dados chegam

Uma requisição carrega dados em três posições diferentes, e cada uma tem um uso próprio.

<Mermaid title="Origem dos dados de uma requisição HTTP no Express">
{`
flowchart TD
  A["POST /bmi/weight/70?unit=metric<br/>{ 'height': 1.75 }"] --> B["Linha da requisição"]
  A --> C["Corpo da requisição"]
  B --> D["req.params<br/>{ weight: '70' }<br/>identifica o recurso"]
  B --> E["req.query<br/>{ unit: 'metric' }<br/>modifica a consulta"]
  C --> F["req.body<br/>{ height: 1.75 }<br/>envia os dados"]
`}
</Mermaid>

A tabela resume quando usar cada um:

| Origem       | Objeto       | Tipo dos valores | Use para                                         |
| ------------ | ------------ | ---------------- | ------------------------------------------------ |
| Rota         | `req.params` | sempre string    | identificar **qual** recurso: `/users/:id`        |
| Query string | `req.query`  | string ou array  | filtrar, ordenar e paginar: `?page=2&sort=name`   |
| Corpo        | `req.body`   | tipos do JSON    | enviar dados de criação e atualização             |

### Parâmetro de rota

O trecho iniciado por `:` vira uma chave em `req.params`, sempre como string:

```js title="Parâmetro de rota" showLineNumbers {2}
app.get('/users/:id', (req, res) => {
  const id = Number(req.params.id); // '2' -> 2

  const user = users.find((item) => item.id === id);

  if (!user) {
    return res.status(404).json({ message: 'Usuário não encontrado' });
  }

  res.json(user);
});
```

<Aside type="caution" title="`req.params` é sempre string">
  `/users/2` entrega `req.params.id === '2'`. Sem a conversão, `item.id === req.params.id`
  compara número com string e falha silenciosamente — a rota responde `404` para um usuário
  que existe.
</Aside>

### Query string

Tudo depois do `?` chega em `req.query` já convertido em objeto. Um mesmo nome repetido vira
array, o que é uma armadilha comum:

```js title="Query string" showLineNumbers {5}
// GET /users?page=2&role=admin&role=editor
app.get('/users', (req, res) => {
  const page = Number(req.query.page ?? 1);
  //          => 2
  const roles = [req.query.role].flat();
  //          => ['admin', 'editor']

  res.json({ page, roles });
});
```

### Corpo da requisição

O corpo só existe depois de um middleware interpretá-lo. Cada formato tem o seu:

```js title="src/app.js" showLineNumbers
app.use(express.json()); // application/json           -> objeto
app.use(express.urlencoded({ extended: true })); // formulário HTML -> objeto
app.use(express.text()); // text/plain                 -> string
app.use(express.raw()); //  application/octet-stream   -> Buffer
```

Registrados esses middlewares, o handler apenas lê `req.body`:

```js title="Corpo JSON" showLineNumbers {2}
app.post('/users', (req, res) => {
  const { name, email } = req.body ?? {};

  if (!name || !email) {
    return res.status(400).json({ message: 'Campos obrigatórios' });
  }

  res.status(201).json({ id: 3, name, email });
});
```

## Encadeando handlers

Uma rota aceita vários handlers em sequência. Cada um decide se responde ou se passa adiante
com `next()` — é assim que validação e autorização entram antes do handler final:

```js title="Vários handlers na mesma rota" showLineNumbers {1,4}
app.post('/users', requireJson, validateUser, createUser);

// Equivalente, com array:
app.post('/users', [requireJson, validateUser], createUser);
```

Quando várias rotas compartilham o caminho, `app.route()` evita repeti-lo:

```js title="app.route()" showLineNumbers
app.route('/users/:id')
  .get(showUser)
  .put(updateUser)
  .delete(destroyUser);
```

## Modularizando com `express.Router()`

Um arquivo único funciona bem com três rotas; com trinta, vira um problema. O `Router` é um
mini-aplicativo: tem as próprias rotas e os próprios middlewares, e é montado sob um prefixo.

<Mermaid title="Montagem de routers sob prefixos em uma aplicação Express">
{`
flowchart TD
  A["app"] --> B["app.use('/users', userRouter)"]
  A --> C["app.use('/products', productRouter)"]
  B --> D["router.get('/')<br/>→ GET /users"]
  B --> E["router.get('/:id')<br/>→ GET /users/:id"]
  C --> F["router.get('/')<br/>→ GET /products"]
`}
</Mermaid>

Dentro do router os caminhos são **relativos ao prefixo**: `'/'` vira `/users` e `'/:id'`
vira `/users/:id`. O projeto de exemplo separa ainda a montagem da aplicação da subida do
servidor:

<ProjectLinks path="examples/courses/express/projects/router" devcontainer="express-router" />

<FileTree>
{`
  express-router/
  ├── package.json
  └── src/
      ├── app.js
      ├── server.js
      └── routes/
          └── user-router.js
`}
</FileTree>

<CodeTabs
  syncKey="express-router"
  files={[
    { path: 'examples/courses/express/projects/router/src/app.js', title: 'src/app.js', showLineNumbers: true, mark: '8' },
    { path: 'examples/courses/express/projects/router/src/routes/user-router.js', title: 'src/routes/user-router.js', showLineNumbers: true, mark: ['router.get'] },
    { path: 'examples/courses/express/projects/router/src/server.js', title: 'src/server.js', showLineNumbers: true },
  ]}
/>

A mudança em `app.js` é pequena, mas muda a arquitetura:

```js title="src/app.js" del={2-4} ins={6}
// antes: a rota morava no arquivo da aplicação
app.get('/users', (req, res) => {
  res.json(users);
});

app.use('/users', userRouter);
```

<Aside type="tip" title="Por que separar `app.js` de `server.js`?">
  Um `app` sem `listen` é testável: a ferramenta de teste importa o `app` e faz requisições
  sem ocupar porta nenhuma. `server.js` fica com a única responsabilidade de subir o
  processo — e é ele que muda quando o deploy exige outra porta ou host.
</Aside>

## Executando

<Steps>

1. Entre no projeto e instale as dependências:

   ```bash
   cd examples/courses/express/projects/router
   pnpm install
   ```

2. Suba o servidor:

   ```bash
   pnpm dev
   ```

3. Liste os usuários:

   ```bash
   curl http://localhost:3000/users
   ```

</Steps>

## Testando

Nesta seção, testamos o roteamento modularizado com `express.Router()`. O objetivo é validar tanto a rota de listagem completa quanto a busca por ID inexistente.

A rota montada sob o prefixo `/users` responde com a coleção de usuários e status `200 OK`:

<details>
  <summary>Requisição .http correspondente</summary>

  ```http
  ### Listar todos os usuários
  GET http://localhost:3000/users
  ```
</details>

<ApiRequest method="GET" path="/users" baseUrl="http://localhost:3000" />

<ApiResponse status={200}>
{`
  [
    { "id": 1, "name": "Ana" },
    { "id": 2, "name": "Bruno" }
  ]
`}
</ApiResponse>

Já a busca com um parâmetro de rota apontando para um recurso inexistente (`GET /users/99`) ativa a lógica de validação do router e retorna status `404 Not Found` com mensagem de erro amigável:

<details>
  <summary>Requisição .http correspondente</summary>

  ```http
  ### Buscar usuário inexistente por ID
  GET http://localhost:3000/users/99
  ```
</details>

<ApiRequest method="GET" path="/users/99" baseUrl="http://localhost:3000" />

<ApiResponse status={404}>
{`
  {
    "message": "Usuário não encontrado"
  }
`}
</ApiResponse>

## Exercício

Ainda no projeto `router`:

1. Crie `src/routes/product-router.js` com `GET /products` e `GET /products/:id`.
2. Monte-o em `app.js` sob o prefixo `/products`.
3. Acrescente `GET /products?maxPrice=100` filtrando pela query string.
4. Responda `400` quando `maxPrice` não for numérico e `404` quando o `id` não existir.

<details>
  <summary>Possível resposta</summary>

  ```js title="src/routes/product-router.js" showLineNumbers
  import { Router } from 'express';

  const router = Router();

  const products = [
    { id: 1, name: 'Teclado', price: 150 },
    { id: 2, name: 'Mouse', price: 80 },
  ];

  router.get('/', (req, res) => {
    const { maxPrice } = req.query;

    if (maxPrice === undefined) {
      return res.json(products);
    }

    const limit = Number(maxPrice);

    if (Number.isNaN(limit)) {
      return res.status(400).json({ message: '"maxPrice" deve ser numérico' });
    }

    res.json(products.filter((product) => product.price <= limit));
  });

  router.get('/:id', (req, res) => {
    const product = products.find((item) => item.id === Number(req.params.id));

    if (!product) {
      return res.status(404).json({ message: 'Produto não encontrado' });
    }

    res.json(product);
  });

  export default router;
  ```

  Em `app.js`: `app.use('/products', productRouter);`
</details>

## Desafio

Escreva uma rota `GET /files/*splat` que devolva os segmentos capturados como array, e uma
rota `GET /users/:id?` que responda a lista completa quando o `id` não vier. Verifique em
qual ordem as duas precisam ser registradas para não se anularem.

## Perguntas de revisão

### Parâmetros

1. **Qual a diferença de propósito entre `req.params` e `req.query`?**

   <details>
     <summary>Possível resposta</summary>

     `req.params` identifica **qual** recurso está sendo acessado e faz parte do caminho.
     `req.query` **modifica** a consulta — filtro, ordenação, paginação — e é opcional por
     natureza.
   </details>

2. **Por que `req.query.role` pode ser um array?**

   <details>
     <summary>Possível resposta</summary>

     Porque a query string aceita o mesmo nome repetido: `?role=admin&role=editor` vira
     `['admin', 'editor']`. Código que assume string quebra quando o cliente repete o
     parâmetro — `[req.query.role].flat()` normaliza os dois casos.
   </details>

3. **O que falta quando `req.body` é `undefined` em um `POST` com JSON?**

   <details>
     <summary>Possível resposta</summary>

     O `app.use(express.json())` — ou ele foi registrado depois da rota. O corpo não é
     interpretado automaticamente.
   </details>

### Router

4. **Por que o caminho dentro do router é `'/'` e não `'/users'`?**

   <details>
     <summary>Possível resposta</summary>

     Porque o prefixo é definido na montagem: `app.use('/users', userRouter)`. Repetir
     `/users` dentro do router produziria `/users/users`. Isso permite remontar o mesmo
     router em outro prefixo — por exemplo, versionar a API em `/api/v1`.
   </details>

5. **Qual a vantagem de `app.js` exportar o `app` sem chamar `listen`?**

   <details>
     <summary>Possível resposta</summary>

     Testes importam o `app` e fazem requisições sem abrir porta, e o mesmo `app` pode ser
     servido por outro processo (serverless, cluster) sem alteração.
   </details>

## Referências

- [Express — Routing guide](https://expressjs.com/en/guide/routing.html)
- [Express — Router API](https://expressjs.com/en/5x/api.html#router)
- [Express 5 — Path route matching syntax](https://expressjs.com/en/guide/migrating-5.html#path-syntax)

## Na prática

Os projetos executáveis desta aula são [Express Router](../../practice/express-router/) e
[BMI API](../../practice/bmi-api/), que compara as três origens de dados no mesmo cálculo.

## Próxima aula

[Requisição e Resposta](../request-response/): cabeçalhos, negociação de conteúdo, status e
os métodos que encerram a resposta.
