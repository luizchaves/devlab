---
title: "Projeto: Express + Prisma"
description: CRUD persistente com Express, Prisma ORM e SQLite.
course: cstrc-jp-dw
project: express-prisma
---

import { Aside, Steps } from '@astrojs/starlight/components';
import CodeTabs from '@components/CodeTabs.astro';
import FileTree from '@components/FileTree.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';
import SourceCode from '@components/SourceCode.astro';

Nível **Avançado** · Node.js · Express.js · Prisma · SQLite

A mesma estrutura MVC do projeto anterior, com os dados em um banco de verdade.

## Estrutura

<FileTree>
{`
  express-prisma/
  ├── .env.example
  ├── package.json
  ├── prisma/
  │   ├── schema.prisma
  │   └── seed.js
  └── src/
      ├── app.js
      ├── server.js
      ├── controllers/
      │   └── user-controller.js
      ├── lib/
      │   └── prisma.js
      ├── middlewares/
      │   └── error-handler.js
      └── routes/
          └── user-router.js
`}
</FileTree>

## Schema

<SourceCode path="examples/express-prisma/prisma/schema.prisma" title="prisma/schema.prisma" showLineNumbers />

## Código

<CodeTabs
  syncKey="express-prisma"
  files={[
    { path: 'examples/express-prisma/src/app.js', title: 'src/app.js', showLineNumbers: true },
    { path: 'examples/express-prisma/src/routes/user-router.js', title: 'src/routes/user-router.js', showLineNumbers: true },
    { path: 'examples/express-prisma/src/controllers/user-controller.js', title: 'src/controllers/user-controller.js', showLineNumbers: true },
    { path: 'examples/express-prisma/src/lib/prisma.js', title: 'src/lib/prisma.js', showLineNumbers: true },
    { path: 'examples/express-prisma/prisma/seed.js', title: 'prisma/seed.js', showLineNumbers: true },
  ]}
/>

## Executando

<Steps>

1. ```bash
   cd examples/express-prisma
   ```

2. <PackageManagerTabs />

3. ```bash
   cp .env.example .env
   ```

4. <PackageManagerTabs run="db:push" />

5. <PackageManagerTabs run="db:seed" />

6. <PackageManagerTabs run="dev" />

</Steps>

## Conceitos abordados

- Schema declarativo e relacionamento `User` ↔ `Post`
- Instância única do `PrismaClient`
- Controllers assíncronos com tratamento de erro automático (Express 5)
- `include` para carregar relacionamentos
- Seed de dados iniciais

<Aside type="caution" title="O `.env` não vai para o Git">
  Só o `.env.example` é versionado. Rode `cp .env.example .env` depois de clonar.
</Aside>

<Aside type="tip">
  As aulas correspondentes são [Prisma](../../database/prisma/) e [CRUD](../../database/crud/).
</Aside>

## Onde continuar

Este é o ponto de partida do projeto final: acrescente validação de entrada em
middleware e autenticação por token.
