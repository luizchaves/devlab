---
title: "InvestFlow: Origem e evolução"
description: "Sétima sprint do InvestFlow: a carteira vista pela origem (corretora, categoria e emissor) em um treemap, e a linha de aportes acumulados contra o valor de mercado, por ativo e para a carteira inteira, com modo contínuo ou por eventos e janela de tempo."
course: npm
project: investflow-static
sidebar:
  label: "7. Origem e evolução"
---

import { Aside, Steps } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import Mermaid from '@components/Mermaid.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Etapa **7 de 11** · Nível **Avançado** · Views · SVG · Treemap · Séries temporais

A carteira da Sprint 3 diz **o que** a pessoa tem; a matriz da Sprint 6 diz **quanto rendeu** por mês. Esta sprint responde às duas perguntas que faltam: **onde** o patrimônio está (em qual corretora, de qual emissor, em qual categoria) e **quanto do valor de hoje é aporte e quanto é rendimento**, ao longo do tempo. As duas visões são SVG escrito à mão, sem biblioteca de gráficos, no mesmo critério de dependências da trilha.

<Aside type="note" title="Tópicos conceituais associados">
  Consulte [Canvas](../../../../web-api/media/canvas/) para o desenho por código (aqui em SVG, pela mesma lógica de coordenadas) e [relacionamentos](../../../../expressjs/persistence/relations/) para as views que cruzam ativo, corretora e transações.
</Aside>

## Objetivo

Construir duas leituras derivadas dos mesmos fatos (ativos, transações e cotações) sem criar nenhuma tabela nova. Ao final da etapa, o leitor deve ser capaz de:

- Modelar uma view de agregação por dimensão (corretora, categoria, emissor) que respeite o RLS.
- Implementar um treemap *squarified* como função pura e renderizá-lo em SVG.
- Separar, em uma série temporal, o que foi aportado do que o mercado fez com o aporte.

## Requisitos, histórias e critérios

**Épico EP07 · Origem e evolução** › **Feature FT07 · Onde está e quanto rendeu**

| Requisito | Recorte da etapa | Estado ao final |
| --------- | ---------------- | --------------- |
| RF09 | Treemap por corretora, categoria e emissor. | atendido |
| RF10 | Aportes acumulados versus valor de mercado, por ativo e carteira. | atendido |
| RNF01 | As duas views correm com `security_invoker`. | atendido |

### US12 · Ver a carteira pela origem · RF09

> **Como** investidor,
> **quero** ver a carteira agrupada por corretora, por categoria e por emissor,
> **para** saber onde o patrimônio está custodiado e de quem depende (o CDB do BB vem do BB).

```gherkin
Cenário: CA07.1 - Treemap por corretora
  Dados ativos em duas corretoras
  Quando abro origins.html
  Então vejo um bloco por corretora
  E a área de cada bloco é proporcional ao valor atual custodiado nela

Cenário: CA07.2 - Troca de recorte
  Quando escolho "emissor" no seletor
  Então os blocos passam a ser os emissores, sem recarregar a página
  E a soma das áreas continua igual ao patrimônio

Cenário: CA07.3 - Só a própria carteira
  Dadas duas contas com ativos
  Quando a conta A consulta allocation_by_origin
  Então recebe apenas as próprias linhas
```

### US13 · Comparar aportes com o valor atual · RF10

> **Como** investidor,
> **quero** ver, ao longo do tempo, a linha do que aportei e a linha do que a carteira vale,
> **para** deduzir o rendimento de um ativo ou da carteira inteira sem confundir aporte com ganho.

```gherkin
Cenário: CA07.4 - Série da carteira
  Dadas transações e cotações conhecidas
  Quando abro analytics.html
  Então vejo duas linhas no mesmo eixo de tempo: aportado acumulado e valor de mercado

Cenário: CA07.5 - Série do ativo
  Quando abro asset.html de um ativo
  Então vejo as mesmas duas linhas restritas àquele ativo

Cenário: CA07.6 - A diferença é o rendimento
  Quando comparo as duas linhas em um mês
  Então a distância entre elas é o resultado acumulado até ali
  E um mês sem cotação fica sem ponto na linha de valor, e não em zero

Cenário: CA07.7 - Linha contínua ou por eventos
  Quando escolho o modo "Eventos"
  Então só os meses com aporte ou resgate aparecem, sem recarregar a página
  E no modo "Contínua" um mês sem lançamento repete a última posição e o último valor

Cenário: CA07.8 - Janela de tempo
  Quando escolho "2 anos"
  Então as duas séries são recortadas e a URL guarda ?range=2y
```

### Tasks da etapa

As sete tasks se dividem em duas trilhas paralelas, uma por história, e terminam nos testes das duas e nos controles da linha do tempo.

<Steps>

1. **TK07-1 · Criar a view `allocation_by_origin`**: dono, corretora, categoria, emissor e valor atual, com `security_invoker`.
2. **TK07-2 · Criar `src/lib/treemap.js`**: o algoritmo *squarified* como função pura e a renderização em SVG.
3. **TK07-3 · Criar `origins.html` e `src/pages/origins.js`**: a página, o seletor de recorte e a legenda.
4. **TK07-4 · Criar a view `portfolio_evolution`**: mês a mês, aportado acumulado e valor de mercado, por ativo.
5. **TK07-5 · Criar `src/lib/line-chart.js`**: duas séries em SVG, ligadas em `analytics.html` e `asset.html`.
6. **TK07-6 · Testes da sprint**: unidade para os dois módulos de desenho, integração para as views, E2E para o seletor.
7. **TK07-7 · Modos e janela da linha do tempo**: `fillContinuousMonths()`, `filterMovementMonths()` e os botões de modo e janela nas duas páginas.

</Steps>

## Duas perguntas, duas views

As duas leituras partem dos mesmos fatos. A de origem agrupa a posição atual por três dimensões que já existem em `assets` desde a Sprint 3; a de evolução acumula as transações no tempo e as encontra com a cotação do mês.

<Mermaid title="Dos fatos às duas visões">
{`
flowchart LR
  A[("assets<br/>broker_id · category · issuer")] --> O["allocation_by_origin<br/>valor atual por dimensão"]
  T[("transactions")] --> O
  T --> E["portfolio_evolution<br/>aportado acumulado × valor de mercado por mês"]
  Q[("quotes_history")] --> E
  O --> TM["Treemap (SVG)<br/>origins.html"]
  E --> LC["Linhas (SVG)<br/>analytics.html · asset.html"]
`}
</Mermaid>

## Estrutura da aplicação

A sprint nasce como cópia da Sprint 6 mais o delta: uma migration com duas views, dois módulos de desenho, um serviço, uma página nova e os gráficos nas duas telas existentes. Nenhuma tabela nova. Este é também o projeto das Sprints 8 a 11, que acrescentam camadas a ele em vez de copiá-lo; por isso a pasta traz mais arquivos do que esta página descreve.

<ProjectLinks path="examples/courses/npm/projects/investflow-origins" devcontainer="investflow-origins" />

<FileTree>
{`
  investflow-origins/
  ├── supabase/migrations/
  │   └── 20260912000011_origin_and_evolution_views.sql   TK07-1, TK07-4: allocation_by_origin e portfolio_evolution
  ├── src/
  │   ├── lib/
  │   │   ├── treemap.js                  TK07-2: layout() squarified, renderTreemap() e groupBy()
  │   │   ├── treemap.test.js
  │   │   ├── line-chart.js               TK07-5: scalePoints(), pathOf() e renderLineChart()
  │   │   └── line-chart.test.js
  │   ├── services/origins.js             as duas views, totalsByMonth(), toSeries(); TK07-7: fillContinuousMonths() e filterMovementMonths()
  │   └── pages/
  │       ├── origins.js                  TK07-3: seletor de recorte e legenda
  │       ├── analytics.js                + gráfico da carteira
  │       └── asset.js                    + gráfico do ativo
  ├── tests/
  │   ├── integration/origins.test.js     as duas views com dados de referência
  │   └── e2e/origins.spec.js             o seletor e os gráficos pela tela
  ├── origins.html                        nova
  ├── analytics.html · asset.html         + seção "Aportes versus valor de mercado"
  └── vercel.json                         herdado da Sprint 6: deploy do front estático
`}
</FileTree>

## O que muda nesta etapa?

| Antes (Sprint 6) | Agora (Sprint 7) |
| ---------------- | ---------------- |
| Corretora e emissor só na tabela da carteira | `origins.html`: treemap com a área de cada bloco proporcional ao valor custodiado |
| Distribuição só por categoria | Seletor que troca o recorte entre corretora, categoria e emissor sem recarregar |
| Matriz de retorno mês a mês | + a linha do aportado acumulado contra a do valor de mercado, na carteira e no ativo |
| Nenhum gráfico | Dois módulos de SVG escritos à mão, testados como funções puras |
| Uma só leitura no tempo | Modo contínuo ou por eventos e janela de tempo (Tudo, 2 anos, Ano), com o estado na URL |

## Descrição das tarefas

Abaixo estão detalhadas as tarefas de implementação desta etapa, com orientações e os trechos de código do projeto.

### TK07-1 · Criar a view `allocation_by_origin`

A view devolve **uma linha por ativo** com o valor atual (posição × cotação) e as três dimensões de origem: corretora (o `left join` em `brokers`, com "Sem corretora" quando não há), categoria e emissor. Agrupar fica para o front: a mesma linha alimenta o treemap por qualquer um dos três recortes, e trocar o seletor não volta ao banco. `security_invoker` mantém o RLS: Bruno recebe lista vazia.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/supabase/migrations/20260912000012_origin_and_evolution_views.sql"
  title="supabase/migrations/20260912000012_origin_and_evolution_views.sql"
  region="allocation_by_origin"
/>

### TK07-2 · Criar `src/lib/treemap.js`

O treemap *squarified* é uma função pura: recebe `[{ label, value }]` e a área, devolve retângulos. Os itens entram em ordem decrescente e cada linha do layout ocupa o lado menor do espaço restante; a função `worst()` mede a pior proporção da linha, e um item só entra nela se não piorar essa proporção. Sem DOM e sem biblioteca, o módulo cabe em cem linhas e é testável.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/lib/treemap.js"
  title="src/lib/treemap.js"
  region="layout"
/>

Os testes afirmam as três propriedades que definem um treemap correto: a soma das áreas é a área total, a área de cada retângulo é proporcional ao valor, e nenhum retângulo sai dos limites. Valor zero fica de fora.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/lib/treemap.test.js"
  title="src/lib/treemap.test.js"
  region="layout"
/>

A renderização é outra função: recebe os retângulos e devolve um `<svg>`, com rótulo só quando cabe e um `<title>` em cada bloco para o *hover*.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/lib/treemap.js"
  title="src/lib/treemap.js"
  region="render"
/>

### TK07-3 · Criar `origins.html` e o seletor de recorte

A página lê a view uma vez e redesenha a cada troca do `<select>`: `groupBy()` soma por corretora, categoria ou emissor, `layout()` calcula e `renderTreemap()` desenha. A legenda traz valor e fatia de cada bloco, e o E2E prova a troca de recorte sem recarregar (a URL não muda, o `data-dimension` sim).

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/pages/origins.js"
  title="src/pages/origins.js"
  region="draw"
/>

### TK07-4 · Criar a view `portfolio_evolution`

Mês a mês, por ativo: o que foi **aportado** até ali (compras menos vendas, ao preço de cada transação) e o que a posição **valia** na última cotação do mês. A distância entre as duas é o resultado acumulado, inclusive o realizado. Um mês sem cotação não gera linha: é lacuna, não zero, e o teste de integração confere que março não aparece.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/supabase/migrations/20260912000012_origin_and_evolution_views.sql"
  title="supabase/migrations/20260912000012_origin_and_evolution_views.sql"
  region="portfolio_evolution"
/>

A série da carteira é a soma das séries por ativo, mês a mês, feita no serviço; a série do ativo é a mesma consulta filtrada por `asset_id`.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/services/origins.js"
  title="src/services/origins.js"
  region="views"
/>

### TK07-5 · Criar `src/lib/line-chart.js`

Duas séries no mesmo eixo de tempo. `scalePoints()` alinha os meses das duas, converte valores em coordenadas e deixa `y` nulo onde não há ponto; `pathOf()` recomeça o caminho com `M` depois de uma lacuna, para que a linha de valor **pule** o mês sem cotação em vez de descer a zero. Ambas são puras, e é isso que os testes conferem.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/lib/line-chart.js"
  title="src/lib/line-chart.js"
  region="scale"
/>

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/lib/line-chart.test.js"
  title="src/lib/line-chart.test.js"
  region="unit"
/>

Em `analytics.html` o gráfico mostra a carteira; em `asset.html`, o ativo. As duas páginas chamam o mesmo `renderLineChart()`.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/src/pages/asset.js"
  title="src/pages/asset.js"
  region="evolution"
/>

### TK07-6 · Testes da sprint

| Camada | O que prova | Tasks |
| ------ | ----------- | ----- |
| Unidade | as três propriedades do treemap, `groupBy()`, o eixo comum e a lacuna do gráfico | TK07-2, TK07-5 |
| Integração | `allocation_by_origin` com as três dimensões e valores; `security_invoker`; `portfolio_evolution` = `[jan 3000/3100, fev 6400/6600]` e sem março; o CDB sem cotação fica fora da evolução; `fillContinuousMonths()` e `filterMovementMonths()` | TK07-1, TK07-4, TK07-7 |
| E2E | dois ativos em corretoras e emissores diferentes, cotação pela Edge Function, treemap por corretora → emissor → categoria sem recarregar; as duas linhas na carteira e no ativo; modo e janela na URL | CA07.1, CA07.2, CA07.4, CA07.5, CA07.7, CA07.8 |

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/tests/integration/origins.test.js"
  title="tests/integration/origins.test.js"
  region="evolution"
/>

### TK07-7 · Modos e janela da linha do tempo

A view devolve uma linha por mês **com cotação**. Para a leitura contínua, `fillContinuousMonths()` preenche os meses intermediários repetindo a última posição e o último valor; para a leitura por eventos, `filterMovementMonths()` deixa só os meses em que o aportado mudou (ou os meses informados pelas transações). A janela (Tudo, 2 anos, Ano) é um recorte sobre a série já montada, e as três escolhas ficam na URL pela mesma `setQueryParams()` da Sprint 8.

<SourceCode
  path="examples/courses/npm/projects/investflow-origins/tests/integration/origins.test.js"
  title="tests/integration/origins.test.js"
  region="timeline"
/>

## Decisões de desenho

| Decisão | Regra | Razão |
| ------- | ----- | ----- |
| Sem biblioteca de gráficos | Treemap e linhas em SVG escrito à mão. | O algoritmo *squarified* cabe em 40 linhas e é uma função pura testável; uma biblioteca traria mais código que a página inteira. |
| Emissor é texto, corretora é tabela | `assets.issuer` livre; `brokers` por dono. | A corretora reaparece em muitos ativos e vira filtro; o emissor descreve o ativo e raramente se repete além do próprio banco. |
| Aportado acumulado, não custo | A linha soma compras menos vendas ao preço da transação. | É o dinheiro que entrou; a diferença para o valor de mercado é o resultado, inclusive o realizado. |
| Mês sem cotação fica sem ponto | A linha de valor tem lacuna. | Interpolar inventaria um patrimônio que ninguém observou. |

<Aside type="caution" title="Dica de IA: Peça o algoritmo como função pura, não como componente">
  Um assistente tende a devolver um treemap acoplado ao DOM, com dados de exemplo dentro. Peça a separação: uma função que recebe `[{ label, value }]` e a largura e altura, e devolve `[{ label, x, y, w, h }]`; a renderização em SVG é outra função. É o que torna o teste de unidade possível (a soma das áreas tem que ser a área total).

  **Prompt:** *"Implemente o algoritmo squarified treemap em JavaScript como função pura `layout(items, width, height)` que devolve retângulos; sem DOM, sem biblioteca. Depois escreva os testes: soma das áreas igual a `width * height`, itens em ordem decrescente, e nenhum retângulo fora dos limites."*
</Aside>

## Testando

```bash title="Terminal"
pnpm test
```

```txt title="Output" frame="terminal"
 ✓ |unit| src/lib/treemap.test.js (7 tests)
 ✓ |unit| src/lib/line-chart.test.js (4 tests)
 ✓ |integration| tests/integration/origins.test.js (7 tests)
   ✓ CA07.1 / CA07.2: allocation_by_origin traz as tres dimensoes e o valor atual
   ✓ CA07.3: a view e security_invoker
   ✓ CA07.4 / CA07.6: portfolio_evolution reproduz aportado x valor da planilha, sem marco
   ✓ CA07.5: a serie do ativo e a serie da carteira coincidem quando so um ativo tem cotacao
   ✓ CA07.7: modo continuo preenche os meses intermediarios com a ultima posicao
   ✓ CA07.7: modo eventos deixa so os meses com movimentacao
   ✓ CA09.11: a serie Valor + Proventos acumula os proventos pagos ate cada mes
 ✓ (as demais suítes das Sprints 2 a 11)

 Test Files  35 passed (35)
      Tests  216 passed (216)
```

```bash title="Terminal"
pnpm test:e2e tests/e2e/origins.spec.js
```

```txt title="Output" frame="terminal"
  ✓ o treemap agrupa por corretora, categoria e emissor sem recarregar (CA07.1, CA07.2)
  ✓ a linha de aportes e a de valor aparecem na carteira e no ativo (CA07.4, CA07.5)
  ✓ modo e janela da linha do tempo trocam sem recarregar e ficam na URL (CA07.7, CA07.8)

  3 passed (6.5s)
```

Uma armadilha de teste que vale registrar: com um único mês de cotação, a "linha" do gráfico é um ponto, e um `<path>` com caixa de zero pixels conta como **invisível** para o Playwright. O E2E confere o atributo `d` do caminho, não a visibilidade.

## Executando

<Steps>

1. Entre no projeto, instale e suba a stack:

   ```bash title="Terminal"
   cd examples/courses/npm/projects/investflow-origins
   pnpm install
   pnpm db:start
   supabase status -o env   # preencha o .env
   pnpm db:reset
   ```

2. Suba o Vite, cadastre dois ativos em corretoras diferentes com emissores diferentes, registre aportes, atualize as cotações e abra `origins.html`:

   ```bash title="Terminal"
   pnpm dev
   ```

3. Rode as suítes:

   ```bash title="Terminal"
   pnpm test
   pnpm test:e2e
   ```

</Steps>

## Conceitos abordados

- View com `security_invoker` como agregação que respeita o RLS
- Treemap *squarified*: proporção de área e ordem decrescente
- SVG gerado por JavaScript, sem biblioteca
- Série temporal derivada de transações e cotações, com lacuna explícita
- Separar aporte de rendimento

## Exercício

Escreva a consulta que devolve, por corretora, o valor atual da carteira do usuário logado. Depois explique o que muda se a view não tiver `security_invoker`.

## Desafio

Acrescente um terceiro nível ao treemap: dentro de cada corretora, os ativos. Discuta quando o aninhamento ajuda e quando a página fica ilegível em um celular.

## Perguntas de revisão

1. Por que o emissor é uma coluna de texto e a corretora uma tabela?
2. O que a distância entre as duas linhas representa em um mês?
3. Por que um mês sem cotação não pode virar zero na linha de valor?
4. Qual é a propriedade que o teste de unidade do treemap tem que provar?

## Referências

- [Squarified treemaps (Bruls, Huizing, van Wijk)](https://www.win.tue.nl/~vanwijk/stm.pdf)
- [MDN: SVG](https://developer.mozilla.org/en-US/docs/Web/SVG)
- [Supabase: views e `security_invoker`](https://supabase.com/docs/guides/database/postgres/row-level-security#views)

## Próxima etapa

Em [Lançamentos e cotação manual](../ledger/), a Sprint 8 devolve o controle sobre os fatos: editar e excluir lançamentos, acompanhar renda fixa pelo saldo e informar a cotação à mão quando o provedor não responde, no mesmo projeto desta sprint.
