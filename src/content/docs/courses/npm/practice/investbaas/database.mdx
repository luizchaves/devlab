---
title: "InvestBaaS: Banco de dados e RLS"
description: "Terceira sprint do InvestBaaS: migrations PostgreSQL para ativos, transações e histórico de preços, com precisão decimal, integridade referencial e isolamento por Row Level Security."
course: npm
project: investbaas-static
sidebar:
  label: "3. Banco de dados e RLS"
---

import { Aside, Steps } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import Mermaid from '@components/Mermaid.astro';
import ProjectLinks from '@components/ProjectLinks.astro';
import SourceCode from '@components/SourceCode.astro';

Etapa **3 de 7** · Nível **Avançado** · PostgreSQL · Migrations · RLS · Dados financeiros

Com uma identidade estável, o InvestBaaS pode registrar a carteira. Esta etapa modela fatos que precisam sobreviver ao tempo, como compra, venda e preço diário. Ela também transfere a regra "cada pessoa vê apenas o que é seu" da interface para o PostgreSQL.

<Aside type="note" title="Tópicos conceituais associados">
  Consulte [modelagem relacional](../../../../expressjs/persistence/relations/) para relações e integridade, e [autorização](../../../../expressjs/auth/authorization/) para comparar policy de banco com uma checagem de aplicação.
</Aside>

## Objetivo

Modelar a carteira como dados relacionais auditáveis e protegidos no ponto de persistência. Ao final da etapa, o leitor deve ser capaz de:

- Distinguir ativo, transação e cotação como fatos com responsabilidades diferentes.
- Planejar migrations com integridade referencial e precisão decimal.
- Derivar policies RLS de leitura e escrita a partir de `auth.uid()`.

## Requisitos, histórias e critérios

**Épico EP03 · Carteira e isolamento** › **Feature FT03 · Dados financeiros por investidor**

São três histórias: a carteira, o isolamento e a tela de cada investimento. As onze tasks se dividem entre elas.

| Requisito | Recorte da etapa | Estado ao final |
| --------- | ---------------- | --------------- |
| RF03 | Ativos, compras, vendas e leitura da carteira. | atendido |
| RF08 | Tela de cada investimento com todos os aportes. | atendido |
| RF09 | Corretora de custódia e emissor por ativo. | atendido (a visão por origem é a Sprint 7) |
| RF05 | Histórico de preços por ativo. | preparado |
| RF06 | Base histórica para indicadores. | preparado |
| RNF01 | RLS em todas as tabelas de domínio. | atendido |
| RNF03 | Valores financeiros em `numeric`. | atendido |

### US05 · Gerenciar posições e transações · RF03

> **Como** investidor autenticado,
> **quero** registrar ativos e suas compras ou vendas,
> **para** acompanhar posição, custo e valor de mercado sem depender de uma planilha isolada.

```gherkin
Cenário: CA03.1 - Ativo válido
  Quando cadastro um ativo com ticker, nome e categoria permitida
  Então o banco cria uma linha com user_id igual ao usuário da sessão
  E a categoria fora do conjunto permitido é recusada

Cenário: CA03.2 - Transação vinculada
  Dado que possuo um ativo
  Quando registro uma compra com quantidade, preço e data válidos
  Então a transação referencia aquele ativo
  E pertence ao mesmo investidor

Cenário: CA03.3 - Métricas reproduzíveis
  Quando consulto a carteira com dados de teste conhecidos
  Então quantidade, custo e valor atual podem ser reproduzidos a partir das transações e cotações

Cenário: CA03.7 - Corretora e emissor
  Quando cadastro um ativo informando corretora e emissor
  Então a corretora passa a existir para mim, se ainda não existia
  E o ativo guarda broker_id e issuer
```

### US06 · Isolar dados por investidor · RNF01

> **Como** investidor,
> **quero** que o banco recuse operações sobre a carteira de outra conta,
> **para** não depender de cada tela lembrar de filtrar `user_id`.

```gherkin
Cenário: CA03.4 - Leitura isolada
  Dadas duas contas com ativos
  Quando a conta A consulta assets
  Então recebe apenas as próprias linhas

Cenário: CA03.5 - Escrita isolada
  Quando a conta A tenta inserir uma transaction com user_id da conta B
  Então a policy WITH CHECK rejeita a operação

Cenário: CA03.6 - Registro indireto isolado
  Quando a conta A pede quotes_history de um ativo da conta B
  Então não obtém a linha nem confirma que o ativo existe
```

### US11 · Acompanhar os aportes de um investimento · RF08

> **Como** investidor,
> **quero** abrir um ativo e ver todos os aportes e resgates registrados nele,
> **para** entender como cheguei à posição atual e conferir cada lançamento.

```gherkin
Cenário: CA03.8 - Tela do ativo
  Quando clico em um ativo da carteira
  Então abro asset.html?id=… com nome, ticker, categoria, corretora, emissor, posição, preço médio e resultado

Cenário: CA03.9 - Aportes em ordem
  Dado um ativo com três transações
  Quando abro a tela do ativo
  Então vejo as três em ordem cronológica, com tipo, quantidade, preço, total e data
  E posso registrar uma nova sem sair da tela

Cenário: CA03.10 - Ativo alheio
  Quando abro asset.html com o id de um ativo de outra conta
  Então vejo "não encontrado"
  E a resposta não confirma que o ativo existe
```

### Tasks da etapa

As tasks criam uma migration por responsabilidade e só conectam o dashboard depois de policies e índices. A ordem reduz o risco de criar uma tela que funciona somente para um usuário de teste.

<Steps>

1. **TK03-1 · Criar `assets`**: categorias, ticker, preço e dono do ativo.
2. **TK03-2 · Criar `transactions`**: tipo, quantidade, preço, data e referência ao ativo.
3. **TK03-3 · Criar `quotes_history`**: uma série temporal por ativo e data.
4. **TK03-4 · Habilitar RLS**: `profiles`, `assets`, `transactions` e `quotes_history`.
5. **TK03-5 · Criar policies**: leitura e escrita limitadas por `auth.uid()`.
6. **TK03-6 · Ligar a carteira**: consultar ativos reais em `dashboard.html`.
7. **TK03-7 · Criar formulários de ativo e transação**: validar e persistir pelo SDK.
8. **TK03-8 · Calcular indicadores**: custo, patrimônio, lucro e rentabilidade a partir dos fatos.
9. **TK03-9 · Criar índices**: consultas por dono, ativo, ticker e data.
10. **TK03-10 · Criar `brokers`**: corretora por dono, criada sozinha quando um ativo cita um nome novo; `assets` ganha `broker_id` e `issuer`.
11. **TK03-11 · Criar `asset.html`**: a tela de um investimento, com todos os aportes e o formulário de nova transação.

</Steps>

## Modelo de dados

O ativo descreve o instrumento que está na carteira; a transação registra um evento imutável da história; a cotação registra um preço observado. Essa separação permite recalcular qualquer métrica sem sobrescrever a origem do dado.

<Mermaid title="Relações e donos das tabelas de domínio">
{`
erDiagram
  PROFILE ||--o{ ASSET : owns
  ASSET ||--o{ TRANSACTION : has
  ASSET ||--o{ QUOTE_HISTORY : has
  PROFILE ||--o{ TRANSACTION : owns
  PROFILE { uuid id PK string role }
  ASSET { uuid id PK uuid user_id FK string ticker string category numeric current_price }
  TRANSACTION { uuid id PK uuid user_id FK uuid asset_id FK string type numeric quantity numeric price date transaction_date }
  QUOTE_HISTORY { uuid id PK uuid asset_id FK numeric price date quote_date }
`}
</Mermaid>

Para valores de dinheiro, use `numeric`, não `real` ou `double precision`. Quantidade também pode precisar de casas decimais, especialmente para frações de criptoativos e fundos.

## Estrutura da aplicação

A sprint nasce como cópia da Sprint 2 mais o delta: quatro migrations, três serviços, a função pura dos indicadores, o dashboard ligado ao banco e a tela nova do investimento. Os testes acompanham cada camada.

<ProjectLinks path="examples/courses/npm/projects/investbaas-database" devcontainer="investbaas-database" />

<FileTree>
{`
  investbaas-database/
  ├── supabase/migrations/
  │   ├── 20260912000001_profiles.sql          da Sprint 2
  │   ├── 20260912000002_assets.sql            TK03-1 e TK03-10: brokers e assets
  │   ├── 20260912000003_transactions.sql      TK03-2
  │   ├── 20260912000004_quotes_history.sql    TK03-3
  │   └── 20260912000005_rls_policies.sql      TK03-4, TK03-5 e TK03-9: grants, RLS, policies e índices
  ├── src/
  │   ├── lib/
  │   │   ├── portfolio.js                     TK03-8: summarize() e totals(), funções puras
  │   │   └── portfolio.test.js
  │   ├── services/
  │   │   ├── assets.js                        listar, buscar por id e criar (com a corretora)
  │   │   ├── assets.test.js
  │   │   ├── brokers.js                       TK03-10: findOrCreateBroker()
  │   │   └── transactions.js                  registrar compra ou venda
  │   └── pages/
  │       ├── dashboard.js                     TK03-6 e TK03-7: tabela, KPIs e diálogo de novo ativo
  │       └── asset.js                         TK03-11: a tela do investimento
  ├── tests/
  │   ├── integration/portfolio.test.js        migrations, CHECKs, corretora e cada policy
  │   └── e2e/portfolio.spec.js                do cadastro do ativo à tela com dois aportes
  ├── dashboard.html                           sem linhas escritas à mão; + <dialog> de novo ativo
  └── asset.html                               nova: indicadores, aportes e resgates do ativo
`}
</FileTree>

## O que muda nesta etapa?

A carteira deixa de ser cinco linhas escritas no HTML e passa a vir de tabelas que só devolvem o que pertence a quem chama. A mudança de fora é pequena (a tabela parece a mesma); a de dentro é a maior da trilha.

| Antes (Sprint 2) | Agora (Sprint 3) |
| ---------------- | ---------------- |
| Cinco posições escritas em `dashboard.html` | Linhas geradas a partir de `assets` e `transactions` |
| Só a tabela `profiles` | `+ assets`, `transactions` e `quotes_history` |
| Botão **+ Novo Ativo** sem efeito | `<dialog>` que insere pelo SDK, cria a corretora se for nova e recarrega a lista |
| Nome do ativo sem link | Clique abre `asset.html?id=…`, com todos os aportes e resgates |
| Ativo sem origem | `broker_id` (corretora de custódia) e `issuer` (emissor ou gestor) |
| Isolamento só na guarda de página | RLS em todas as tabelas de domínio, com `USING` e `WITH CHECK`, e grants explícitos |
| KPIs de exemplo | Custo, patrimônio, resultado não realizado e realizado calculados das transações |
| — | Índices para `user_id`, `asset_id`, `broker_id` e datas |

## Descrição das tarefas

Abaixo estão detalhadas as tarefas de implementação desta etapa, com orientações e os trechos de código do projeto. As migrations vêm primeiro, porque tudo o mais depende delas.

### TK03-1 · Criar a migration de `assets`

O ativo é o instrumento que está na carteira: um ticker, um nome, uma categoria, onde está custodiado (`broker_id`), quem o emite ou administra (`issuer`) e o preço mais recente. As cinco categorias são as mesmas que a Sprint 1 desenhou como *badges*, e o `check` é o que impede um sexto valor de entrar por qualquer caminho, inclusive pelo SQL Editor. `numeric(14, 4)` guarda quatro casas para o preço, porque um FII pode ser negociado a `R$ 9,8750`; `real` e `double precision` não representam esse valor com exatidão.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000002_assets.sql"
  title="supabase/migrations/20260912000002_assets.sql"
  region="assets"
/>

O `unique (user_id, ticker)` segue a regra que a TaskAPI e o InvestApp já usam para o título: único **por dono**, não no sistema inteiro. Duas contas podem ter `PETR4`; a mesma conta, não. O `issuer` é texto livre de propósito: ele descreve o ativo (o BB de um CDB do BB, a gestora de um fundo) e raramente se repete além do próprio emissor; a Sprint 7 o usa como um dos recortes do treemap.

### TK03-2 · Criar a migration de `transactions`

A transação é um fato: aconteceu uma compra ou uma venda, de tanta quantidade, a tanto, em tal dia. Ela nunca é editada para "corrigir" a posição; um erro vira outra transação. É essa imutabilidade que torna a carteira auditável e permite recalcular qualquer métrica a partir da origem.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000003_transactions.sql"
  title="supabase/migrations/20260912000003_transactions.sql"
  region="transactions"
/>

`user_id` aparece de novo, mesmo já existindo em `assets`. É redundância de propósito: a policy RLS da TK03-5 compara `auth.uid()` com uma coluna da **própria** tabela, sem `join`, o que a torna mais simples de ler e mais barata de executar. `receipt_path` fica vazio até a Sprint 5.

### TK03-3 · Criar a migration de `quotes_history`

Uma linha por ativo e por dia. A chave `unique (asset_id, quote_date)` é o que torna a atualização da Sprint 4 idempotente: rodar a função duas vezes no mesmo dia faz `upsert`, e não duplica.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000004_quotes_history.sql"
  title="supabase/migrations/20260912000004_quotes_history.sql"
  region="quotes_history"
/>

### TK03-4 · Habilitar RLS nas quatro tabelas

Nesta versão do Supabase uma tabela nova não concede **nada** a ninguém: `select`, `insert`, `update` e `delete` são grants explícitos por papel, e a Sprint 2 já mostrou isso em `profiles`. A migration de segurança começa pelos grants (o que cada papel pode fazer) e liga o RLS antes de escrever a primeira policy (com RLS ligado e nenhuma policy, a tabela fica fechada para todo mundo, que é o estado seguro para partir). Repare em `quotes_history`: `authenticated` só tem `select`.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000005_rls_policies.sql"
  title="supabase/migrations/20260912000005_rls_policies.sql"
  region="grants"
/>

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000005_rls_policies.sql"
  title="supabase/migrations/20260912000005_rls_policies.sql"
  region="enable"
/>

### TK03-5 · Criar as policies por `auth.uid()`

Quatro operações, quatro regras, e a distinção que a seção de decisões de segurança explica: `using` filtra as linhas que já existem, `with check` julga a linha que está tentando entrar. Sem o `with check` no `insert`, um cliente poderia gravar um ativo com o `user_id` de outra conta.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000005_rls_policies.sql"
  title="supabase/migrations/20260912000005_rls_policies.sql"
  region="policies"
/>

`quotes_history` não tem `user_id`: a leitura passa por uma subconsulta no ativo, e a escrita fica **sem policy**, porque só a Edge Function da Sprint 4 grava ali, com a chave de serviço, que ignora RLS.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000005_rls_policies.sql"
  title="supabase/migrations/20260912000005_rls_policies.sql"
  region="quotes-policy"
/>

Cada policy tem um teste de integração com duas contas. Os quatro cenários abaixo cobrem leitura, escrita em nome de outro, busca por id de ativo alheio e o `update`/`delete` cruzado, que não devolve erro: devolve **zero linhas afetadas**, como um `UPDATE` sem `WHERE` satisfeito.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/tests/integration/portfolio.test.js"
  title="tests/integration/portfolio.test.js"
  region="rls"
/>

### TK03-6 · Ligar a carteira ao banco

O serviço encapsula a consulta. `select('…, broker:brokers(id, name), transactions(…)')` traz a corretora e as transações aninhadas em uma chamada só, no estilo do `include` do Prisma; o `order` garante uma tabela estável entre recargas. Repare no que **não** está aqui: nenhum `.eq('user_id', …)`. O filtro por dono é do RLS, e escrevê-lo no cliente só daria a falsa impressão de que a segurança mora nele. O teste de unidade do serviço afirma exatamente isso: a string do `select` não contém `user_id`.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/src/services/assets.js"
  title="src/services/assets.js"
  region="list"
/>

`getAsset` usa `maybeSingle()`, e não `single()`: zero linhas vira `null` em vez de erro. É o que deixa "não é seu" e "não existe" indistinguíveis para o cliente, o mesmo `404` sem `403` das outras trilhas.

### TK03-7 · Criar o formulário de ativo

O botão **+ Novo Ativo** da Sprint 1 passa a abrir um `<dialog>` com o formulário: ticker, nome, categoria, corretora (com `<datalist>` das já cadastradas) e emissor. O `user_id` vai no `insert` porque a policy `with check` exige que ele seja o do token; enviar o de outra pessoa não "funciona": o banco responde `42501`.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/src/services/assets.js"
  title="src/services/assets.js"
  region="create"
/>

Na página, o erro do banco chega com `code`; `23505` é a violação do `unique (user_id, ticker)`, e a tela o traduz para uma frase. O resto é renderizar: uma linha por ativo, com o nome linkado à tela do investimento, e os quatro KPIs vindos de `totals()`.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/src/pages/dashboard.js"
  title="src/pages/dashboard.js"
  region="create"
/>

### TK03-8 · Calcular os indicadores da carteira

Custo, quantidade e patrimônio nascem das transações, e é por isso que este módulo é uma função pura: recebe o ativo com as transações aninhadas e devolve números. Sem `fetch`, sem DOM, ele é o candidato natural para o primeiro teste de unidade do projeto.

A venda é o caso que merece atenção: ela baixa o custo pelo **preço médio**, não pelo preço de venda. O que sai da carteira é o custo daquela quantidade; a diferença entre o preço de venda e o preço médio é resultado **realizado**, mostrado à parte. E `value` fica `null` para um ativo sem cotação: a tela mostra `—`, não `R$ 0,00`.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/src/lib/portfolio.js"
  title="src/lib/portfolio.js"
  region="summarize"
/>

A conversão com `Number()` é obrigatória: o PostgREST devolve `numeric` como **string**, justamente para não perder precisão no caminho. Somar sem converter concatena texto, e há um teste só para isso. Os números dos testes são os da planilha de referência da seção Testando: duas compras de 100, a `R$ 30,00` e a `R$ 34,00`.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/src/lib/portfolio.test.js"
  title="src/lib/portfolio.test.js"
  region="summarize"
/>

### TK03-9 · Criar os índices

Toda consulta desta etapa filtra por `user_id` (o RLS faz isso em cada linha) e ordena ou agrupa por ativo e data. Sem índice, o PostgreSQL varre a tabela inteira para aplicar a policy, e o custo cresce com o número de contas, não com o tamanho da carteira de quem consulta.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000005_rls_policies.sql"
  title="supabase/migrations/20260912000005_rls_policies.sql"
  region="indexes"
/>

### TK03-10 · Criar `brokers` e a corretora automática

A corretora é a instituição de custódia, e reaparece em muitos ativos: por isso é tabela, e não texto como o emissor. Ela é **por dono** (`unique (user_id, name)`) e nasce sozinha quando um ativo cita um nome novo, como no InvestApp. O `upsert` sobre a chave única é o que garante uma linha só para `XP` citada dez vezes.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/supabase/migrations/20260912000002_assets.sql"
  title="supabase/migrations/20260912000002_assets.sql"
  region="brokers"
/>

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/src/services/brokers.js"
  title="src/services/brokers.js"
  region="find-or-create"
/>

### TK03-11 · Criar `asset.html`, a tela do investimento

Clicar em um ativo da carteira abre `asset.html?id=…`. A tela lê o ativo com `getAsset(id)`, mostra nome, ticker, categoria, corretora e emissor, os cinco indicadores de `summarize()` e a tabela de **todos os aportes e resgates** em ordem cronológica, com a posição acumulada depois de cada lançamento. O botão **+ Registrar aporte ou resgate** abre um `<dialog>` que insere a transação e recalcula tudo.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/src/pages/asset.js"
  title="src/pages/asset.js"
  region="render"
/>

Um `id` que não existe ou pertence a outra conta chega como `null`, e a tela mostra "não encontrado" sem distinguir os dois casos. O E2E prova a sequência inteira: cadastro do ativo com corretora e emissor, dois aportes pela tela, posição `200`, preço médio `R$ 32,00`, custo `R$ 6.400,00`, e o id alheio.

<SourceCode
  path="examples/courses/npm/projects/investbaas-database/tests/e2e/portfolio.spec.js"
  title="tests/e2e/portfolio.spec.js"
  region="flow"
/>

## Decisões de segurança

RLS não é apenas uma `SELECT` com filtro. Cada operação precisa de uma regra: `USING` controla linhas existentes que podem ser vistas, alteradas ou removidas; `WITH CHECK` controla a linha nova ou alterada que tenta entrar na tabela.

| Operação | Regra mínima para `assets` | Motivo |
| -------- | -------------------------- | ------ |
| `SELECT` | `auth.uid() = user_id` em `USING`. | Não listar carteiras de terceiros. |
| `INSERT` | `auth.uid() = user_id` em `WITH CHECK`. | Não gravar um ativo em nome de outra conta. |
| `UPDATE` | A condição em `USING` e `WITH CHECK`. | Não editar nem trocar o dono do registro. |
| `DELETE` | `auth.uid() = user_id` em `USING`. | Não remover ativos de terceiros. |

<Aside type="caution" title="Dica de IA: Uma policy de leitura não protege escrita">
  É frequente gerar apenas `FOR SELECT` e concluir que RLS está pronto. Sem policies explícitas de inserção e alteração, a aplicação falha ou abre espaço para um `user_id` forjado, conforme a policy criada.

  **Prompt:** *"Para assets e transactions do InvestBaaS, derive as quatro policies RLS e inclua um teste negativo para INSERT e UPDATE que tenta trocar user_id para outra conta."*
</Aside>

## Testando

As três camadas da Sprint 2 continuam, e cada task desta sprint entra em pelo menos uma. Os números de referência são os mesmos em todas: duas compras de 100 `PETR4`, a `R$ 30,00` e a `R$ 34,00`, com cotação `R$ 33,00`.

| Camada | Comando | O que prova | Tasks |
| ------ | ------- | ----------- | ----- |
| Unidade (Vitest + jsdom) | `pnpm test:unit` | `summarize()` e `totals()` com a planilha de referência, venda pelo preço médio, ativo sem cotação; o serviço não filtra `user_id` no cliente | TK03-6, TK03-7, TK03-8, TK03-10 |
| Integração (Vitest + SDK real) | `pnpm test:integration` | `CHECK` de categoria e quantidade, `unique` por dono, corretora única, e **cada policy** com duas contas (leitura, `with check`, id alheio, `update`/`delete` cruzados, `quotes_history` só de leitura) | TK03-1 a TK03-5, TK03-9, TK03-10 |
| E2E (Playwright) | `pnpm test:e2e` | carteira vazia → novo ativo com corretora e emissor → ticker repetido → tela do ativo com dois aportes e a posição recalculada → ativo alheio | CA03.1, CA03.3, CA03.7 a CA03.10 |

| Mês | Compra | Posição | Custo | Preço médio |
| --- | ------ | ------- | ----- | ----------- |
| jan | 100 × 30 | 100 | 3.000 | 30,00 |
| fev | 100 × 34 | 200 | 6.400 | **32,00** |
| valor a 33 | | | | 200 × 33 = **6.600** (+3,13%) |

```bash title="Terminal"
pnpm test
```

```txt title="Output" frame="terminal"
 ✓ |unit| src/services/auth.test.js (5 tests)
 ✓ |unit| src/services/assets.test.js (3 tests)
 ✓ |unit| src/lib/portfolio.test.js (6 tests)
 ✓ |unit| src/guards/require-session.test.js (2 tests)
 ✓ |unit| src/lib/supabase-client.test.js (2 tests)
 ✓ |integration| tests/integration/portfolio.test.js (12 tests)
 ✓ |integration| tests/integration/auth.test.js (6 tests)

 Test Files  7 passed (7)
      Tests  36 passed (36)
```

```bash title="Terminal"
pnpm test:e2e
```

```txt title="Output" frame="terminal"
  ✓ Sprint 3 › carteira vazia, cadastro de ativo com corretora e emissor (CA03.1, CA03.7) (3.2s)
  ✓ Sprint 3 › ticker repetido e recusado com mensagem (unique por dono) (1.0s)
  ✓ Sprint 3 › tela do ativo lista os aportes e recalcula a posicao (CA03.8, CA03.9, CA03.3) (1.3s)
  ✓ Sprint 3 › ativo de outra conta mostra nao encontrado (CA03.10) (973ms)
  ✓ Sprint 2 › (os quatro testes da sprint anterior)

  8 passed (7.8s)
```

O SQL Editor do Studio (`http://127.0.0.1:54323`) permite repetir o cenário de RLS à mão, consultando *como* uma conta. É o equivalente manual do teste de integração, e vale fazer uma vez para ver o `auth.uid()` em ação:

```sql title="SQL Editor: consultar como a Ana"
select set_config('request.jwt.claims',
  json_build_object('sub', (select id from auth.users where email = 'ana@example.com'), 'role', 'authenticated')::text,
  true);
set local role authenticated;

select ticker, user_id from public.assets;
-- Esperado: so os ativos da Ana (CA03.4)
```

## Executando

<Steps>

1. Entre no projeto e instale as dependências:

   ```bash title="Terminal"
   cd examples/courses/npm/projects/investbaas-database
   pnpm install
   ```

2. Suba a stack local e copie os valores para o `.env` (o `.env.example` diz quais):

   ```bash title="Terminal"
   pnpm db:start
   supabase status -o env
   ```

3. Aplique as cinco migrations:

   ```bash title="Terminal"
   pnpm db:reset
   ```

4. Suba o Vite, crie uma conta e cadastre o primeiro ativo em `http://localhost:5173/dashboard.html`:

   ```bash title="Terminal"
   pnpm dev
   ```

5. Rode as suítes:

   ```bash title="Terminal"
   pnpm test
   pnpm test:e2e
   ```

</Steps>

## Conceitos abordados

| Conceito | Aplicação nesta etapa |
| -------- | --------------------- |
| Migration | Versiona a evolução do schema e das policies. |
| Chave estrangeira | Mantém transação e cotação ligadas a um ativo existente. |
| Integridade | `CHECK`, `NOT NULL` e `UNIQUE` recusam estados inválidos. |
| RLS | Aplica autorização no ponto onde os dados residem. |
| Índice | Protege o tempo das consultas que filtram dono e período. |
| Grant por papel | Decide quais operações cada papel alcança, antes de a policy decidir quais linhas. |
| Fato imutável | A transação nunca é editada; posição, custo e resultado são derivados dela. |

## Exercício

Escreva os critérios de integridade para uma venda: quantidade positiva, tipo permitido e ativo existente. Indique qual validação pertence ao banco e qual pode melhorar a experiência no front.

## Desafio

Proponha um mecanismo para impedir que a posição calculada fique negativa após uma venda. Compare uma constraint simples, trigger e função transacional, deixando claras as limitações de cada opção.

## Perguntas de revisão

1. Por que `transactions` não deve ser substituída por colunas acumuladas em `assets`?
2. Qual é a diferença entre `USING` e `WITH CHECK` em RLS?
3. Por que valores financeiros usam `numeric`?
4. Qual índice atende a busca de histórico por ativo e data?
5. Por que o front-end não é a camada final de autorização?

## Referências

- [Supabase: Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [PostgreSQL: tipos numéricos](https://www.postgresql.org/docs/current/datatype-numeric.html)
- [Supabase: migrations](https://supabase.com/docs/guides/deployment/database-migrations)

## Próxima etapa

As [Cotações com Edge Functions](../edge-quotes/) acrescentam preço atual e histórico sem levar a chave do provedor financeiro para o navegador.
