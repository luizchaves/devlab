---
title: "HTML: Texto, Listas e Links"
description: "Cabeçalhos h1 a h6, parágrafos, quebras de linha, elementos de ênfase strong e em, listas ordenadas, não-ordenadas e de definição, e hiperlinks com o elemento a."
course: html
sidebar:
  label: "Texto, Listas e Links"
---
import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import HtmlPreview from '@components/HtmlPreview.astro';
import SourceCode from '@components/SourceCode.astro';

Materiais: [slides da aula](../../../../slides/courses/html/elements/text-formatting/) e
[mapa mental](../../../../mindmaps/courses/html/elements/text-formatting/)

Esta aula apresenta os elementos essenciais para estruturar conteúdos de texto em HTML: a hierarquia de cabeçalhos (`<h1>` a `<h6>`), marcação de parágrafos, ênfases semânticas, listas (ordenadas, não-ordenadas e de definição) e hiperlinks com o elemento `<a>`.

## Objetivo

Compreender e aplicar a hierarquia correta de títulos, utilizar marcações de ênfase semântica (`<strong>` e `<em>`), construir listas de itens e criar conexões de hipertexto entre páginas com o elemento `<a>`.

## Conteúdo de Texto

O HTML oferece tags para categorizar a importância e o significado de cada trecho de texto.

### Cabeçalhos (`<h1>` a `<h6>`)

Os cabeçalhos definem a hierarquia de tópicos da página. `<h1>` representa o título mais importante (geralmente único por página) e `<h6>` o de menor nível.

```html title="Hierarquia de cabeçalhos"
<h1>Título Principal da Página (H1)</h1>
<h2>Seção Secundária (H2)</h2>
<h3>Subseção do Tópico (H3)</h3>
```

<Aside type="caution" title="Não Escolha Cabeçalhos pelo Tamanho Visual">
  Não escolha uma tag `<h3>` apenas porque ela tem um tamanho de fonte menor. Use CSS para alterar o tamanho das letras e mantenha os cabeçalhos fiéis à hierarquia do documento (`h1` -> `h2` -> `h3`).
</Aside>

### Parágrafos e Formatação de Texto

- **`<p>`**: Define um parágrafo de texto corrido.
- **`<strong>`**: Indica alta importância ou urgência (exibido em negrito por padrão).
- **`<em>`**: Indica ênfase no significado do texto (exibido em itálico por padrão).
- **`<mark>`**: Destaca um trecho relevante para o contexto atual.
- **`<code>`**: Representa um trecho de código de computador em fonte monoespaçada.

```html title="Exemplo de marcação de texto"
<p>
  O estudo de <strong>HTML5</strong> é <em>essencial</em> para quem deseja desenvolver para a Web.
</p>
<p>
  Para instalar o projeto, execute o comando <code>pnpm install</code> no terminal.
</p>
```

### Exemplo completo

Texto é o conteúdo mais comum de uma página, e é onde a diferença entre marcar por
significado e marcar por aparência fica mais visível. O exemplo abaixo reúne os
elementos de texto que você vai usar em praticamente toda página.

<Tabs syncKey="html-basics">
  <TabItem label="Código">
    <SourceCode
      path="examples/html/basics/texto.html"
      title="texto.html"
      showLineNumbers
      mark={[
        '<h1>',
        '</h1>',
        '<h2>',
        '</h2>',
        '<p>',
        '</p>',
        '<strong>',
        '</strong>',
        '<em>',
        '</em>',
        '<b>',
        '</b>',
        '<i>',
        '</i>',
        '<blockquote cite="https://developer.mozilla.org/pt-BR/docs/Web/HTML">',
        '</blockquote>',
        '<code>',
        '</code>',
      ]}
    />
  </TabItem>

  <TabItem label="Resultado">
    <HtmlPreview path="examples/html/basics/texto.html" height="26rem" />
  </TabItem>
</Tabs>

O exemplo usa estas tags de texto:

| Tag            | Papel no conteúdo                                                   |
| -------------- | ------------------------------------------------------------------- |
| `<h1>`         | Título principal do documento. Títulos vão de `<h1>` a `<h6>` e formam o **sumário** do documento: use um `<h1>` por página e não pule níveis só para mudar o tamanho da fonte. |
| `<h2>`         | Subtítulo de segundo nível, subordinado ao `<h1>`                   |
| `<p>`          | Parágrafo de texto                                                  |
| `<strong>`     | Trecho importante; normalmente aparece em negrito                   |
| `<em>`         | Trecho com ênfase; normalmente aparece em itálico                   |
| `<b>`          | Destaque visual em negrito, sem comunicar importância semântica     |
| `<i>`          | Destaque visual em itálico, sem comunicar ênfase semântica          |
| `<blockquote>` | Citação em bloco, usada para conteúdo citado de outra fonte         |
| `<code>`       | Trecho de código, comando, nome de arquivo ou valor escrito literal |


## Listas em HTML

HTML suporta três tipos de listas estruturadas:

### 1. Listas Não-Ordenadas (`<ul>`)

Utilizadas quando a ordem dos itens não altera o significado (exibidas com marcadores de ponto):

```html title="Lista de compras (Não-ordenada)"
<ul>
  <li>Café</li>
  <li>Leite</li>
  <li>Pão de queijo</li>
</ul>
```

### 2. Listas Ordenadas (`<ol>`)

Utilizadas quando a sequência dos passos importa (exibidas com numeração sequencial):

```html title="Passo a passo (Ordenada)"
<ol>
  <li>Escreva o arquivo HTML.</li>
  <li>Salve com a extensão .html.</li>
  <li>Abra no navegador.</li>
</ol>
```

### 3. Listas de Definição (`<dl>`)

Utilizadas para dicionários, termos e conceitos (combinação de `<dt>` para o termo e `<dd>` para a descrição):

```html title="Lista de termos"
<dl>
  <dt>HTML</dt>
  <dd>Linguagem de marcação para estrutura de páginas Web.</dd>
  <dt>CSS</dt>
  <dd>Folhas de estilo para apresentação e layout visual.</dd>
</dl>
```

### Exemplo completo

Sempre que o conteúdo for uma sequência de itens, como um menu, um passo a passo ou
um glossário, ele é uma lista, mesmo que visualmente não pareça. Marcar como lista faz
o leitor de tela anunciar quantos itens existem e permite navegar entre eles.

<Tabs syncKey="html-basics">
  <TabItem label="Código">
    <SourceCode
      path="examples/html/basics/listas.html"
      title="listas.html"
      showLineNumbers
      mark={[
        '<ul>',
        '</ul>',
        '<ol>',
        '</ol>',
        '<li>',
        '</li>',
        '<dl>',
        '</dl>',
        '<dt>',
        '</dt>',
        '<dd>',
        '</dd>',
      ]}
    />
  </TabItem>

  <TabItem label="Resultado">
    <HtmlPreview path="examples/html/basics/listas.html" height="26rem" />
  </TabItem>
</Tabs>

A escolha do tipo de lista depende do significado da relação entre os itens:

| Tipo               | Elementos              | Quando usar                                                                      |
| ------------------ | ---------------------- | -------------------------------------------------------------------------------- |
| Lista não ordenada | `<ul>`, `<li>`         | Itens do mesmo grupo, sem ordem obrigatória, como links de navegação ou interesses |
| Lista ordenada     | `<ol>`, `<li>`         | Etapas ou classificações em que a posição muda o significado                      |
| Lista de descrição | `<dl>`, `<dt>`, `<dd>` | Pares de termo e descrição, como glossário, ficha técnica ou perguntas com respostas |

<Aside type="tip" title="Menu também é lista">
  Um menu de navegação é uma lista de links. Mesmo que o CSS remova os marcadores,
  a estrutura correta continua sendo `<ul>` com um `<li>` para cada link.
</Aside>


## Hiperlinks (`<a>`)

O hiperlink é o elemento fundador da Web (*HyperText*). Ele conecta documentos através do elemento `<a>` (*anchor*) e do atributo `href`.

Um hyperlink é um destino (`href`) mais um conteúdo clicável:

```html title="Sintaxe"
<a href="url">conteúdo</a>
```

Um exemplo simples:

```html title="Link para outra página"
<a href="paginas/contato.html">Entre em contato</a>
```

Quem clica no texto "Entre em contato" navega para o arquivo indicado em `href`.

Entender a URL é o que permite escolher o `href` certo:

```plaintext title="Partes de uma URL"
                                                    query
 esquema      domínio    porta      caminho        string   fragmento
┌───┴──┐┌───────┴──────┐┌─┴┐┌──────────┴─────────┐┌───┴───┐┌────┴────┐
https://luizchaves.github.io:443/devlab/html/?busca=tag#links
```

O valor de `href` pode ser absoluto ou relativo. Um endereço absoluto informa o
destino completo, como protocolo e domínio. Um endereço relativo é resolvido a
partir da página atual ou da raiz do site. Com isso, links podem apontar para
endereços externos, arquivos do próprio projeto, trechos da página ou ações do
navegador:

| Forma do `href`                | Resolve para                                                  |
| ------------------------------ | ------------------------------------------------------------- |
| `https://exemplo.com/sobre.html` | URL completa, com protocolo, domínio e arquivo HTML         |
| `/sobre.html`                  | Caminho a partir da raiz do site atual, sem escrever o domínio |
| `contato.html`                 | Arquivo ao lado do atual                                      |
| `paginas/contato.html`         | Arquivo em subdiretório                                       |
| `../index.html`                | Sobe um diretório                                             |
| `#links`                       | Âncora dentro da própria página                               |
| `mailto:ana@exemplo.com`       | Abre o cliente de correio eletrônico                          |

Caminhos relativos ficam mais claros olhando a estrutura do projeto:

<FileTree>
{`
  html-basics/
  ├── fragments.html
  ├── index.html
  ├── texto.html
  ├── img/
  │   └── logo.svg
  └── paginas/
      └── contato.html
`}
</FileTree>

De `paginas/contato.html`, voltar para a raiz exige `../`:

<Tabs syncKey="html-basics">
  <TabItem label="Código">
    <SourceCode
      path="examples/html/basics/paginas/contato.html"
      title="paginas/contato.html"
      showLineNumbers
      mark={['../index.html', '../img/logo.svg']}
    />
  </TabItem>

  <TabItem label="Resultado">
    <HtmlPreview path="examples/html/basics/paginas/contato.html" height="24rem" />
  </TabItem>
</Tabs>

<Aside type="caution" title='Use rel="noopener" com target="_blank"'>
  Sem ele, a página aberta ganha uma referência à sua via `window.opener` e pode
  manipulá-la. Navegadores atuais já aplicam o `noopener` por padrão, mas declarar
  deixa a intenção explícita.
</Aside>

Fragmentos começam com `#` e apontam para um elemento que tem o `id`
correspondente. Esse recurso permite navegar entre partes da mesma página:

<Tabs syncKey="html-basics">
  <TabItem label="Código">
    <SourceCode
      path="examples/html/basics/fragments.html"
      title="fragments.html"
      showLineNumbers
      mark={[
        'id="inicio"',
        'href="#html"',
        'href="#css"',
        'href="#javascript"',
        'id="html"',
        'id="css"',
        'id="javascript"',
        'href="#inicio"',
      ]}
    />
  </TabItem>

  <TabItem label="Resultado">
    <HtmlPreview path="examples/html/basics/fragments.html" height="24rem" serveFromPublic />
  </TabItem>
</Tabs>


## Executando

<Steps>

1. Crie um arquivo `content.html`:

   ```html title="content.html"
   <!DOCTYPE html>
   <html lang="pt-BR">
     <head>
       <meta charset="UTF-8">
       <title>Texto e Links</title>
     </head>
     <body>
       <h1>Guia DevLab</h1>
       <p>Confira os recursos <strong>gratuitos</strong>:</p>
       <ul>
         <li><a href="https://devlab.ifpb.edu.br">DevLab Portal</a></li>
       </ul>
     </body>
   </html>
   ```

2. Abra o arquivo no navegador e teste o clique no link.

</Steps>

## Exercício

1. Crie uma página com um título `<h1>` e dois tópicos `<h2>`;
2. Em um dos tópicos, insira uma lista ordenada com 3 passos;
3. No outro tópico, insira um hiperlink abrindo o site do W3C em uma nova aba (`target="_blank"`).

<details>
  <summary>Possível resposta</summary>

  ```html title="exercise_text.html"
  <!DOCTYPE html>
  <html lang="pt-BR">
    <head>
      <meta charset="UTF-8">
      <title>Exercício de Texto e Links</title>
    </head>
    <body>
      <h1>Aprenda Desenvolvimento Web</h1>

      <h2>Passos para Começar</h2>
      <ol>
        <li>Aprenda HTML para estrutura.</li>
        <li>Aprenda CSS para estilo.</li>
        <li>Aprenda JavaScript para dinamismo.</li>
      </ol>

      <h2>Links Úteis</h2>
      <p>
        Visite o site do <a href="https://www.w3.org" target="_blank" rel="noopener">Consórcio W3C</a>.
      </p>
    </body>
  </html>
  ```
</details>

## Perguntas de revisão

**1. Qual é a diferença semântica entre `<b>` e `<strong>`?**

<details>
  <summary>Possível resposta</summary>

  `<b>` aplica apenas estilo visual negrito sem adicionar importância extra. `<strong>` indica alta importância semântica no texto, lida com ênfase por leitores de tela.
</details>

**2. Quando devemos usar `<ol>` em vez de `<ul>`?**

<details>
  <summary>Possível resposta</summary>

  Usamos `<ol>` quando a sequência dos itens possui ordem lógica relevante (como em um algoritmo ou receita). Usamos `<ul>` quando a ordem não altera o sentido.
</details>

**3. Por que um menu de navegação pode ser marcado como lista?**

<details>
  <summary>Possível resposta</summary>

  Um menu é uma sequência de links relacionados. Mesmo que o CSS remova os
  marcadores visuais, a estrutura continua sendo uma lista, normalmente com `<ul>`,
  `<li>` e `<a>`.
</details>

**4. Quando um link com `href="#html"` funciona?**

<details>
  <summary>Possível resposta</summary>

  Ele funciona quando existe na mesma página um elemento com `id="html"`. O
  navegador usa o fragmento depois do `#` para mover a página até esse elemento.
</details>

## Referências

- [HTML text fundamentals | MDN](https://developer.mozilla.org/en-US/docs/Learn/HTML/Introduction_to_HTML/HTML_text_fundamentals)
- [Creating hyperlinks | MDN](https://developer.mozilla.org/en-US/docs/Learn/HTML/Introduction_to_HTML/Creating_hyperlinks)

## Próxima aula

Com o domínio sobre formatação de texto e hiperlinks, o próximo passo é incorporar mídias visuais e dados tabulares.

[Imagens e Tabelas](../media-tables/): Elementos de imagem, figuras, legendas e tabelas estruturadas.
