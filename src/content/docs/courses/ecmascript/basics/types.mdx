---
title: "JavaScript: Tipos de Dados e Coerção"
description: "Tipos primitivos e de referência, operador typeof, undefined vs null, coerção de tipos implícita e explícita, valores truthy/falsy e igualdade estrita em JavaScript."
course: ecmascript
sidebar:
  label: "Tipos de Dados e Coerção"
---
import { Aside, Steps } from '@astrojs/starlight/components';



Esta aula apresenta o sistema de tipos de dados do ECMAScript, detalhando a separação entre valores primitivos imutáveis e objetos, o comportamento do operador `typeof`, a distinção entre `undefined` e `null`, as regras de coerção de tipos e os operadores de comparação.

Materiais: [slides da aula](../types/) e [mapa mental](../types/).

## Objetivo

Compreender o sistema de tipos dinâmico do JavaScript. Identificar todos os tipos primitivos e de referência, utilizar corretamente o operador `typeof`, manipular conversões explícitas de tipos, reconhecer armadilhas de coerção implícita e utilizar estritamente o operador de igualdade `===`.

## Categorias de Tipos de Dados

No ECMAScript, as variáveis não possuem tipo fixo; os **valores** armazenados é que possuem tipo. A linguagem divide seus tipos de dados em duas categorias fundamentais: **Primitivos** e **Objetos (Tipos de Referência)**.

| Categoria | Tipo | Descrição | Exemplo de Literal |
| :--- | :--- | :--- | :--- |
| Primitivo | `undefined` | Ausência de valor atribuído por padrão | `undefined` |
| Primitivo | `null` | Ausência intencional de objeto | `null` |
| Primitivo | `boolean` | Valor lógico verdadeiro ou falso | `true`, `false` |
| Primitivo | `number` | Número em ponto flutuante IEEE 754 (64-bit) | `42`, `3.14` |
| Primitivo | `bigint` | Inteiro com precisão arbitrária | `9007199254740991n` |
| Primitivo | `string` | Sequência de caracteres de texto imutável | `"DevLab"`, `'JS'` |
| Primitivo | `symbol` | Identificador único e imutável | `Symbol("id")` |
| Objeto | `object` | Coleção de propriedades, funções e arrays | `{ name: "Ana" }`, `[1, 2]` |

## Imutabilidade dos Primitivos

Valores primitivos são imutáveis. Métodos chamados em uma string ou number retornam **novos** valores sem alterar a instância original.

```js title="Imutabilidade de primitivos"
let title = "javascript";
title.toUpperCase(); // Retorna "JAVASCRIPT"
console.log(title); // Continua "javascript" (imutable)

title = title.toUpperCase(); // Reatribuição do identificador para o novo valor
console.log(title); // "JAVASCRIPT"
```

## O Operador `typeof`

O operador unário `typeof` inspeciona e retorna uma string representando o tipo do operando atual.

```js title="Exemplos com typeof"
console.log(typeof 42); // "number"
console.log(typeof "DevLab"); // "string"
console.log(typeof true); // "boolean"
console.log(typeof undefined); // "undefined"
console.log(typeof Symbol()); // "symbol"
console.log(typeof 10n); // "bigint"
console.log(typeof {}); // "object"
console.log(typeof [1, 2]); // "object" (Arrays são objetos!)
console.log(typeof function() {}); // "function"
```

<Aside type="caution" title="A Armadilha Histórica: `typeof null === 'object'`">
  Na especificação inicial do JavaScript (1995), os valores eram representados por rótulos de tipo em memória de 32 bits. O rótulo para objetos era `000`. Como o ponteiro para `null` era um ponteiro nulo (`0x00`), o `typeof null` retornou `"object"`. Esse comportamento foi mantido no ECMAScript para preservar compatibilidade com a web antiga.

  ```js
  console.log(typeof null); // "object" (Erro histórico do JS!)
  ```
</Aside>

## `undefined` vs `null`

Ambos os valores representam ausência de dados, mas com intenções semânticas distintas:

- **`undefined`**: Significa que a variável foi declarada, mas ainda **não recebeu nenhum valor**, ou que uma função não retornou explicitamente nada. É o padrão do motor JavaScript.
- **`null`**: Significa a **ausência intencional** de um objeto ou valor. É atribuído programaticamente pelo desenvolvedor para indicar "sem valor".

```js title="Comparação entre undefined e null"
let unassigned;
console.log(unassigned); // undefined

const emptyUser = null; // Definido intencionalmente como vazio
console.log(emptyUser); // null

console.log(undefined == null); // true (Igualdade ampla)
console.log(undefined === null); // false (Tipos diferentes!)
```

## Booleanos, Falsy e Truthy

Em contextos lógicos (como condicionais `if`), todos os valores em JavaScript são avaliados como **Truthy** (verdadeiro) ou **Falsy** (falso).

Existem exatamente **8 valores Falsy** no ECMAScript:
1. `false`
2. `0` (e `-0`)
3. `0n` (BigInt zero)
4. `""` (string vazia)
5. `null`
6. `undefined`
7. `NaN` (Not-a-Number)
8. `document.all` (em navegadores)

**Todos os demais valores são Truthy**, incluindo strings não-vazias (`"0"`, `"false"`), objetos vazios (`{}`), arrays vazios (`[]`) e qualquer número diferente de zero.

```js title="Testando Truthy e Falsy"
console.log(Boolean("")); // false (Falsy)
console.log(Boolean("0")); // true (Truthy! String não-vazia)
console.log(Boolean([])); // true (Truthy! Array é objeto)
console.log(Boolean({})); // true (Truthy! Objeto)
```

## Coerção de Tipos (Implícita vs Explícita)

Coerção é o processo de conversão de um valor de um tipo de dado para outro.

### Coerção Explícita (Casting)

Ocorre quando o desenvolvedor converte o tipo intencionalmente usando funções construtoras nativas como `String()`, `Number()` e `Boolean()` (sem a palavra `new`).

```js title="Conversão explícita"
const input = "42";
const count = Number(input); // 42 (number)
const text = String(100); // "100" (string)
const isValid = Boolean(1); // true (boolean)
```

### Coerção Implícita

Ocorre automaticamente quando o motor JavaScript tenta realizar uma operação entre tipos incompatíveis.

```js title="Coerção implícita e surpresas"
console.log("5" + 2); // "52" (Operador + com string realiza concatenação!)
console.log("5" - 2); // 3 (Operador - força conversão para number)
console.log("5" * "2"); // 10 (Multiplicação força conversão numérica)
console.log(true + 1); // 2 (true é convertido para 1)
console.log(false + 1); // 1 (false é convertido para 0)
```

<Aside type="tip" title="Use Sempre Igualdade Estrita (`===`)">
  O operador de igualdade ampla (`==`) tenta realizar coerção implícita de tipos antes de comparar, gerando resultados contraintuitivos:

  ```js
  console.log("0" == 0); // true (Coerção implícita de string para number)
  console.log(0 == false); // true
  console.log("" == false); // true
  ```

  Sempre utilize a igualdade estrita (`===`) e a desigualdade estrita (`!==`), que comparam o **tipo** e o **valor** sem coerção:

  ```js
  console.log("0" === 0); // false
  console.log(0 === false); // false
  ```
</Aside>

## Executando

<Steps>

1. Crie um arquivo `types.js`:

   ```js title="types.js"
   const val1 = "10";
   const val2 = 5;

   console.log("Tipo val1:", typeof val1);
   console.log("Tipo val2:", typeof val2);
   console.log("Soma com +:", val1 + val2);
   console.log("Soma com Number():", Number(val1) + val2);
   ```

2. Execute via Node.js:

   ```bash title="Terminal"
   node types.js
   ```

3. Verifique o resultado:

   ```txt title="Output"
   Tipo val1: string
   Tipo val2: number
   Soma com +: 105
   Soma com Number(): 15
   ```

</Steps>

## Exercício

1. Inspecione os tipos dos seguintes valores usando `typeof`: `42`, `"Texto"`, `true`, `undefined`, `null`, `Symbol()`, `[]`, `{}` e `function(){}`;
2. Explique a diferença entre `undefined` e `null`;
3. Liste 5 valores que são convertidos para `false` em condicionais (falsy);
4. Escreva uma função que receba uma entrada e utilize coerção explícita para somar dois números passados como texto.

<details>
  <summary>Possível resposta</summary>

  ```js title="exercise_types.js"
  console.log(typeof 42); // "number"
  console.log(typeof "Texto"); // "string"
  console.log(typeof true); // "boolean"
  console.log(typeof undefined); // "undefined"
  console.log(typeof null); // "object" (bug histórico)
  console.log(typeof Symbol()); // "symbol"
  console.log(typeof []); // "object"
  console.log(typeof {}); // "object"
  console.log(typeof function() {}); // "function"

  // undefined indica falta de inicialização padrão do JS.
  // null indica ausência intencional definida pelo programador.

  // Falsy values: false, 0, "", null, undefined, NaN

  function sumStrings(a, b) {
    return Number(a) + Number(b);
  }
  console.log(sumStrings("15", "25")); // 40
  ```
</details>

## Perguntas de revisão

## Tipos e Coerção

**1. O que significa dizer que o tipo está no valor, não na variável?**

<details>
  <summary>Possível resposta</summary>

  Significa que variáveis em JS são dinamicamente tipadas e não possuem um tipo fixo. O tipo é uma propriedade do valor atribuído naquele momento.
</details>

**2. Por que `typeof null` retorna `"object"`?**

<details>
  <summary>Possível resposta</summary>

  Devido a uma limitação técnica histórica da primeira implementação do JS em 1995 na representação binária de ponteiros nulos.
</details>

**3. Quais valores são avaliados como Falsy em JavaScript?**

<details>
  <summary>Possível resposta</summary>

  `false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` e `NaN`.
</details>

**4. O que ocorre no comando `"10" + 5` vs `"10" - 5`?**

<details>
  <summary>Possível resposta</summary>

  No operador `+`, a presença de uma string força a concatenação (`"105"`). No operador `-`, ocorre coerção implícita da string para número, resultando em `5`.
</details>

## Referências

- [JavaScript data types and data structures | MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures)
- [`typeof` | MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof)
- [Equality comparisons and sameness | MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Equality_comparisons_and_sameness)

## Próxima aula

Com o entendimento completo de tipos de dados, coerção e igualdade, o próximo passo é aprender a combinar valores em expressões e operadores formais.

[Expressões e Operadores](../operators/): Operadores aritméticos, lógicos, relacionais e regras de precedência.
