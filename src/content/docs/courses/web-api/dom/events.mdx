---
title: "Web APIs: Eventos"
description: "Manipulação de eventos com atributos HTML, handlers e addEventListener."
course: web-api
sidebar:
  label: "Eventos"
---
import Mermaid from '@components/Mermaid.astro';

## Propagação de Eventos no DOM (Event Flow)

Quando um evento ocorre em um elemento do DOM, ele não é disparado apenas naquele elemento isolado. O navegador processa o evento através de **três fases de propagação**:

1. **Fase de Captura (Capturing Phase)**: O evento desce do objeto raiz `Window` e `Document` até a árvore DOM chegar ao elemento ancestral do alvo.
2. **Fase de Alvo (Target Phase)**: O evento atinge diretamente o elemento onde a interação ocorreu (`event.target`).
3. **Fase de Borbulhamento (Bubbling Phase)**: O evento sobe do elemento alvo de volta até a raiz `Window`, acionando os ouvintes registrados em cada ancestral.

<Mermaid title="Fases de Propagação de Eventos no DOM (Captura -> Alvo -> Borbulhamento)">
{`
flowchart TD
  subgraph Capturing["1. Fase de Captura (Downwards)"]
    W["Window / Document"] --> Body["Elemento body"]
    Body --> Div["div#container"]
  end

  subgraph Target["2. Fase de Alvo"]
    Div --> Btn["button#btn (Target)"]
  end

  subgraph Bubbling["3. Fase de Borbulhamento (Upwards)"]
    Btn -.-> DivUp["div#container"]
    DivUp -.-> BodyUp["Elemento body"]
    BodyUp -.-> WUp["Window / Document"]
  end
`}
</Mermaid>

## GlobalEventHandlers

![](/pw2/imgs/w3c/event-handling/global-event-handlers-hierarchy.svg)

[MDN](https://developer.mozilla.org/en-US/docs/Web/API/GlobalEventHandlers)

### Hierarchy

![](/dw/imgs/w3c/event-handling/global-event-handlers-hierarchy.svg)

### Registering on-event handlers

HTML attribute named on(eventtype):

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Document</title>
  </head>
  <body>
    <h1>Lorem ipsum</h1>
    <button onclick="window.print()">Print</button>
  </body>
</html>
```

Setting the on-event handlers property from JavaScript:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Document</title>
  </head>
  <body>
    <h1>Lorem ipsum</h1>
    <button>Print</button>
  </body>
</html>
```

```js
const button = document.querySelector('button');
button.onclick = function () {
  window.print();
};
```

### On-event handlers

- onclick
- onkeyup
- onload
- onchange

## EventTarget

![](/pw2/imgs/w3c/event-handling/event-target-hierarchy.svg)

[MDN](https://developer.mozilla.org/en-US/docs/Web/API/EventTarget)

![](/dw/imgs/w3c/event-handling/event-target-hierarchy.svg)

### EventTarget.addEventListener()

Click ([MouseEvent](https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent)):

```js
const button = document.querySelector('button#print');

button.addEventListener('click', function () {
  window.print();
});
```

Keyup ([KeyboardEvent](https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent), [Key Values](https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values)):

```js
document.addEventListener('keyup', function (event) {
  if (event.key == 'Enter') {
    console.log('Enter key');
  }
});
```

[MDN](https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener)
