---
title: "Web APIs: Axios"
description: "Cliente HTTP Axios aplicado a requisições para APIs e organização de serviços."
course: web-api
sidebar:
  label: "Axios"
---
import { Aside } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import SourceCode from '@components/SourceCode.astro';

## Objeto Axios

[axios](https://axios-http.com/)

```bash
$ npm install axios
```

```js title="via-cep/index.js"
import axios from 'axios';

const response = await axios.get('https://viacep.com.br/ws/01001000/json/');

console.log(response.data);
```

### Invest-app (CRUD)

<Aside type="note" title="Código de apoio">
  Projeto de apoio: `/codes/package/axios/invest-app/`.
</Aside>

#### Axios Config

```js title="/codes/package/axios/invest-app/js/services/api.js"
import axios from 'axios';

const API = axios.create({
  baseURL: 'https://example.supabase.co/rest/v1',
  headers: {
    apikey: 'SUPABASE_ANON_KEY',
    Authorization: 'Bearer SUPABASE_ANON_KEY',
    Prefer: 'return=representation',
  },
});

export default API;
```

```js title="/codes/package/axios/invest-app/js/services/storage.js"
import API from '/services/api.js';

async function create(resource, data) {}
async function read(resource) {}
async function update(resource, data) {}
async function remove(resource) {}

export default { create, read, update, remove };
```

#### Create a investment

![](/dw/imgs/package/supabase-api/post-investment.png)

[Axios - POST Request](https://axios-http.com/docs/post_example)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function create(resource, data) {
  const { data: createdData } = await API.post(resource, data);

  return createdData?.[0];
}
```

```js
import Storage from './services/storage.js';

const investment = {
  name: "Tesouro Selic 2029",
  value: 10000,
  origin: "Tesouro Direto",
  category: "Pós",
  interest: "100% Selic",
  created_at: "2021-09-10T00:00:00-03:00",
};

Storage.create('investments', investment);
```

#### Read investments

![](/dw/imgs/package/supabase-api/get-investments.png)

[Axios - GET Request](https://axios-http.com/docs/example)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function read(resource) {
  const { data } = await API.get(resource);

  return data;
}
```

```js
import Storage from './services/storage.js';

Storage.read('investments');
Storage.read('investments/?id=eq.1');
```

#### Update a investment

![](/dw/imgs/package/supabase-api/patch-investment.png)

[Axios - PATCH Request](https://axios-http.com/docs/api_intro)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function update(resource, data) {
  const { data: updatedData } = await API.patch(resource, data);

  return updatedData?.[0];
}
```

```js
import Storage from './services/storage.js';

Storage.update('investments/?id=eq.1', { value: 15000 });
```

#### Delete a investment

![](/dw/imgs/package/supabase-api/delete-investment.png)

[Axios - DELETE Resquest](https://axios-http.com/docs/api_intro)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function remove(resource) {
  const { error } = await API.delete(resource);

  if (error) {
    throw error;
  } else {
    return true;
  }
}
```

```js
import Storage from './services/storage.js';

Storage.remove('investments/?id=eq.1');
```

## Exemplo completo: Invest App (CRUD)

O exemplo a seguir isola o acesso HTTP em um módulo de serviço e implementa as quatro
operações de um CRUD com Axios.

[Código Fonte](https://github.com/ifpb/pw2/tree/main/public/codes/package/axios/invest-app/)

<FileTree>
{`
invest-app/
├── css/
│   └── style.css
├── js/
│   ├── components/
│   │   ├── InvestmentCard.js
│   │   ├── InvestmentForm.js
│   │   └── Modal.js
│   ├── lib/
│   │   ├── dom.js
│   │   ├── format.js
│   │   └── investments.js
│   ├── services/
│   │   ├── api.js
│   │   └── storage.js
│   └── main.js
├── public/
│   └── vite.svg
├── .env.example
├── .gitignore
├── index.html
├── package-lock.json
├── package.json
├── postcss.config.js
└── tailwind.config.js
`}
</FileTree>

### Configuração do Axios

<SourceCode path="examples/web-api/pw2/axios/invest-app/js/services/api.js" />

```js title="/codes/package/axios/invest-app/js/services/storage.js"
import API from '/services/api.js';

async function create(resource, data) {}
async function read(resource, id) {}
async function update(resource, id, data) {}
async function remove(resource, id) {}

export default { create, read, update, remove };
```

### Criar um investimento

![](/pw2/imgs/package/supabase-api/post-investment.png)

[Axios - POST Request](https://axios-http.com/docs/post_example)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function create(resource, data) {
  const { data: createdData } = await API.post(resource, data);

  return createdData?.[0];
}
```

```js
import Storage from './services/storage.js';

const investment = {
  name: "Tesouro Selic 2029",
  value: 10000,
  origin: "Tesouro Direto",
  category: "Pós",
  interest: "100% Selic",
  created_at: "2021-09-10T00:00:00-03:00",
};

Storage.create('investments', investment);
```

### Ler investimentos

![](/pw2/imgs/package/supabase-api/get-investments.png)

[Axios - GET Request](https://axios-http.com/docs/example)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function read(resource, id) {
  if (id) {
    resource = `${resource}?id=eq.${id}`;
  }

  const { data } = await API.get(resource);

  return data;
}
```

```js
import Storage from './services/storage.js';

Storage.read('investments');
Storage.read('investments', 1);
```

### Atualizar um investimento

![](/pw2/imgs/package/supabase-api/patch-investment.png)

[Axios - PATCH Request](https://axios-http.com/docs/api_intro)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function update(resource, id, data) {
  resource = `${resource}?id=eq.${id}`;

  const { data: updatedData } = await API.patch(resource, data);

  return updatedData?.[0];
}
```

```js
import Storage from './services/storage.js';

Storage.update('investments', 1, { value: 15000 });
```

### Remover um investimento

![](/pw2/imgs/package/supabase-api/delete-investment.png)

[Axios - DELETE Resquest](https://axios-http.com/docs/api_intro)

```js  title="/codes/package/axios/invest-app/js/services/storage.js"
async function remove(resource, id) {
  resource = `${resource}?id=eq.${id}`;

  const { error } = await API.delete(resource);

  if (error) {
    throw error;
  } else {
    return true;
  }
}
```

```js
import Storage from './services/storage.js';

Storage.remove('investments', 1);
```
