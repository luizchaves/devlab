---
title: "Web APIs: Outras Web APIs do Navegador"
description: "Panorama e guia de referência das Web APIs avançadas e especializadas que complementam a plataforma web: IndexedDB, Service Workers, Web Audio, OffscreenCanvas, WebXR, WebAuthn, WebCodecs e acesso a hardware."
course: web-api
sidebar:
  label: "Outras Web APIs"
---
import { Aside, Card, CardGrid, Steps } from '@astrojs/starlight/components';
import Mermaid from '@components/Mermaid.astro';

Este tópico apresenta um panorama exaustivo das Web APIs avançadas e especializadas que complementam as trilhas principais do guia. Ele cataloga interfaces de alto desempenho, áudio modular, decodificação acelerada, realidade estendida, autenticação biométrica, armazenamento estruturado offline e integração com periféricos físicos que expandem a plataforma web para além do desenvolvimento de páginas tradicionais.

Materiais: [slides do tópico](../../../../slides/courses/web-api/reference/other-apis/) e [mapa mental](../../../../mindmaps/courses/web-api/reference/other-apis/).

## Objetivo

Compreender o alcance estendido da plataforma web, identificando as Web APIs especializadas para computação gráfica em segundo plano, multimídia modular, segurança biométrica, armazenamento avançado e integração de hardware. Ao final do tópico, o leitor deve ser capaz de:

- Categorizar as principais Web APIs avançadas por domínio de aplicação (gráficos, persistência, mídia, segurança, hardware e sistema).
- Diferenciar os casos de uso de APIs básicas e suas contrapartes avançadas (ex: Canvas tradicional vs OffscreenCanvas, Web Storage vs IndexedDB).
- Avaliar requisitos de segurança, permissões e suporte entre navegadores para recursos experimentais ou de baixo nível.
- Selecionar a especificação adequada para cenários de aplicações web progressivas (PWA), processamento de áudio profissional, autenticação moderna sem senha e integração com hardware.

## O Panorama Estendido da Plataforma Web

Além das interfaces fundamentais de manipulação do DOM, requisições HTTP, desenho 2D/3D, áudio/vídeo e armazenamento básico abordadas nas seções principais deste guia, a plataforma web padronizada pela W3C e WHATWG conta com dezenas de especificações voltadas para cenários específicos, ferramentas de produtividade, hardware dedicado e aplicações offline de alta escala.

O mapa abaixo agrupa essas interfaces em domínios especializados:

<Mermaid title="Domínios de Web APIs especializadas e avançadas">
{`
flowchart LR
  A["Outras Web APIs"] --> B["Gráficos & Imersão<br/>OffscreenCanvas, WebXR, EyeDropper"]
  A --> C["Offline & Dados<br/>IndexedDB, Service Workers, Cache API"]
  A --> D["Mídia & Áudio<br/>Web Audio, WebCodecs, Media Capabilities"]
  A --> E["Fluxos & Compressão<br/>Streams API, Compression, File System"]
  A --> F["Segurança & Auth<br/>WebAuthn, Passkeys, FedCM, Permissions"]
  A --> G["UI & Transições<br/>View Transitions, Web Animations, Web Share"]
  A --> H["Hardware & Periféricos<br/>Web Bluetooth, Web USB, Web NFC"]
  A --> I["Sistema & Janelas<br/>Screen Wake Lock, Web Locks, Window Overlay"]
`}
</Mermaid>

A seguir, analisamos cada uma dessas famílias, seus objetivos, interfaces principais e quando adotá-las.

---

## Gráficos em Segundo Plano, Imersão e Cores

Para renderização gráfica desacoplada da interface, experiências de realidade virtual/aumentada e seleção de cores nativa, o navegador oferece interfaces avançadas:

| Web API | Finalidade Principal | Tecnologias e Interfaces | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **OffscreenCanvas API** | Renderização gráfica 2D/WebGL em segundo plano desacoplada da thread de UI | `canvas.transferControlToOffscreen()`, `OffscreenCanvas` | Jogos e renderizações matemáticas pesadas executadas em Web Workers sem causar travamentos na rolagem da página. |
| **WebXR Device API** | Acesso a dispositivos de Realidade Virtual (VR) e Realidade Aumentada (AR) | `navigator.xr.requestSession('immersive-vr')` | Visualizadores de ambientes 3D imersivos, visitas virtuais e aplicações de realidade mista com rastreamento espacial. |
| **EyeDropper API** | Ferramenta nativa de conta-gotas para captura de cores de qualquer pixel na tela | `new EyeDropper().open()` | Editores gráficos na web, ferramentas de design e configuradores de temas onde o usuário escolhe cores da interface. |

<Aside type="tip" title="Renderização sem travamentos com OffscreenCanvas">
Ao desenhar gráficos complexos ou jogos com muitos elementos, transfira o controle do `<canvas>` para um Web Worker via `OffscreenCanvas`. Isso garante que as operações de renderização nunca compitam por tempo de CPU com o manuseio de cliques e eventos da thread principal.
</Aside>

---

## Armazenamento Estruturado e Aplicações Offline (PWA)

Aplicações que operam sem conexão de rede (*offline-first*) ou que lidam com grandes volumes de dados no cliente utilizam interfaces além do `localStorage`:

| Web API | Finalidade Principal | Características | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **IndexedDB API** | Banco de dados orientado a objetos no cliente | Transacional, assíncrono, índices múltiplos, suporta blobs e objetos complexos | Cache offline de grandes coleções de registros, editores de texto com histórico local, clientes de e-mail offline. |
| **Service Workers API** | Proxy de rede programável em segundo plano | Roda fora da thread da UI, intercepta requisições HTTP, gerencia cache e notificações push | Progressive Web Apps (PWAs), funcionamento offline completo, sincronização em segundo plano. |
| **Cache API** | Armazenamento persistente de pares de `Request`/`Response` | Acessível por scripts da página e por Service Workers | Cache de assets estáticos (HTML, CSS, JS, imagens) e respostas da API para navegação instantânea. |
| **Storage Manager API** | Estimativa de uso de disco e persistência garantida | `navigator.storage.estimate()` e `navigator.storage.persist()` | Verificar quota de armazenamento disponível e evitar que o navegador limpe o cache automaticamente. |

---

## Autenticação, Criptografia e Permissões

Para substituir senhas inseguras por autenticação biométrica nativa, gerenciar permissões e federar identidades:

| Web API | Finalidade Principal | Interfaces Chave | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **Web Authentication API (WebAuthn / Passkeys)** | Autenticação forte e sem senha baseada em criptografia assimétrica | `navigator.credentials.create()`, `get()` | Login com biometria (Touch ID, Face ID, Windows Hello) ou chaves físicas de segurança (FIDO2 / YubiKey). |
| **FedCM API (Federated Credential Management)** | Autenticação federada com provedores de identidade preservando a privacidade | `navigator.credentials.get({ identity: ... })` | Login com Google, Apple ou Microsoft sem expor cookies de rastreamento de terceiros. |
| **Permissions API** | Consulta programática do status de permissões da origem | `navigator.permissions.query({ name: 'geolocation' })` | Verificar se uma permissão já foi concedida, negada ou precisa de prompt antes de invocar a API. |
| **Credential Management API** | Armazenamento e preenchimento federado de credenciais | `navigator.credentials.store()` | Login automático com 1 clique e sincronização de credenciais de login entre dispositivos do usuário. |

---

## Áudio Modular, Codecs e Capacidades de Mídia

A manipulação de áudio em nível de nó e o acesso direto ao hardware de codificação de vídeo:

| Web API | Finalidade Principal | Interfaces Chave | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **Web Audio API** | Processamento, síntese e efeitos de áudio em tempo real via grafo de nós | `AudioContext`, `GainNode`, `AnalyserNode`, `OscillatorNode` | Sintetizadores musicais, equalizadores, jogos com áudio espacial 3D, analisadores de espectro sonoro. |
| **WebCodecs API** | Acesso de baixo nível a decodificadores e codificadores de mídia da GPU | `VideoEncoder`, `VideoDecoder`, `AudioDecoder` | Edição de vídeo no navegador com aceleração de hardware, streaming personalizado e transcodificação rápida. |
| **Media Capabilities API** | Consulta da fluidez e eficiência de reprodução de codecs pelo hardware | `navigator.mediaCapabilities.decodingInfo()` | Escolher dinamicamente a melhor resolução e codec (AV1, HEVC, VP9) que o dispositivo consegue reproduzir sem perda de quadros. |
| **Remote Playback API** | Controle de reprodução em receptores externos remotos | `video.remote.prompt()`, `remote.state` | Transmitir e controlar vídeos do navegador em smart TVs e dispositivos Chromecast / AirPlay. |

---

## Interface Moderna, Animações e Compartilhamento

Interfaces modernas que aproximam a experiência de aplicações web da experiência de aplicativos móveis nativos:

| Web API | Finalidade Principal | Interfaces Chave | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **View Transitions API** | Transições de tela animadas e suaves entre estados e páginas | `document.startViewTransition()` | Navegação cinematográfica com animação contínua de elementos compartilhados (SPA e MPA nativo). |
| **Web Animations API (WAAPI)** | Controle imperativo e programático do motor de animação CSS | `element.animate()`, `Animation` | Animações dinâmicas dirigidas por código JS, controle de reprodução (`pause`, `reverse`, `playbackRate`). |
| **Web Share API** | Compartilhamento de conteúdo com outros apps instalados no SO | `navigator.share()`, `navigator.canShare()` | Botão de "Compartilhar" que abre a folha nativa do celular/desktop para WhatsApp, Telegram ou e-mail. |
| **Badging API** | Notificação visual de contadores no ícone do aplicativo | `navigator.setAppBadge()`, `clearAppBadge()` | Exibir a contagem de mensagens não lidas no ícone do PWA na barra de tarefas ou na tela inicial do celular. |

---

## Streams, Compressão e Manipulação de Arquivos

Para processamento eficiente de fluxos contínuos de dados sem carregar arquivos gigantescos inteiramente na memória RAM:

| Web API | Finalidade Principal | Interfaces Chave | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **Streams API** | Criação, encadeamento e consumo de fluxos de dados em blocos | `ReadableStream`, `WritableStream`, `TransformStream` | Download progressivo de grandes volumes de dados e parsing incremental de payloads JSON. |
| **Compression Streams API** | Compressão e descompressão nativa de dados em tempo real | `new CompressionStream('gzip')`, `DecompressionStream` | Compactar dados antes de enviar ao servidor ou descompactar arquivos `.gz` diretamente no navegador. |
| **File System Access API** | Leitura e gravação direta em arquivos do sistema operacional | `window.showOpenFilePicker()`, `FileSystemFileHandle` | IDEs web (como VS Code for Web), editores de imagem, ferramentas de edição de código local no navegador. |

---

## Redes de Próxima Geração e Métricas de Performance

Ferramentas para transporte de dados em alta velocidade e monitoramento de desempenho da aplicação em produção:

| Web API | Finalidade Principal | Interfaces Chave | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **WebTransport API** | Transporte bidirecional cliente-servidor sobre HTTP/3 (QUIC) | `WebTransport`, `datagrams`, `incomingUnidirectionalStreams` | Jogos multiplayer online em tempo real, streaming de baixa latência e alternativas flexíveis a WebSockets. |
| **Performance Timeline API** | Coleta precisa de métricas de temporização e Core Web Vitals | `PerformanceObserver`, `performance.now()` | Medição em tempo real de LCP (*Largest Contentful Paint*), FID/INP e requisições lentas de rede. |
| **Network Information API** | Consulta do status da rede e modo de economia de dados | `navigator.connection` (`effectiveType`, `saveData`) | Adaptar a qualidade das imagens e vídeos automaticamente quando o usuário estiver em redes móveis lentas. |

---

## Hardware, Periféricos e Conectividade Local

Ambientes restritos e sistemas operacionais desktop/mobile permitem conectar dispositivos físicos via navegador sob permissões explícitas:

| Web API | Finalidade Principal | Protocolo / Transporte | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **Web Bluetooth API** | Comunicação com dispositivos Bluetooth Low Energy (BLE) | GATT (Generic Attribute Profile) | Monitoramento de frequência cardíaca, leitura de sensores IoT, controle de drones e robôs educacionais. |
| **Web USB API** | Acesso seguro e direto a dispositivos USB | USB standard transfers | Configuração de teclados customizados, placas microcontroladoras (Arduino, ESP32) e leitores fiscais. |
| **Web Serial API** | Comunicação com portas seriais e adaptadores USB-to-Serial | Porta COM / UART serial | Gravação de firmware em microcontroladores, conexão com impressoras térmicas e equipamentos industriais. |
| **Web NFC API** | Leitura e gravação sem contato de tags NFC em celulares | NDEF (NFC Data Exchange Format) | Validação de ingressos, controle de acesso físico, automações e crachás de identificação. |
| **WebHID API** | Integração com dispositivos de interface humana não padrão | Protocolo HID | Gamepads especiais, pedais de controle, mesas de som e controladores de voo. |

<Aside type="caution" title="Compatibilidade e Contexto de Execução">
As APIs de acesso a hardware (Web Bluetooth, Web USB, Web Serial, Web NFC, WebHID) são padronizadas principalmente para o ecossistema Chromium e exigem estritamente **Contexto Seguro (HTTPS)** e **Gesto Explícito do Usuário**. Elas possuem suporte restrito ou ausente em navegadores como Mozilla Firefox e Apple Safari por políticas de privacidade e segurança do sistema.
</Aside>

Assistentes de código costumam gerar soluções baseadas em APIs avançadas sem verificar a compatibilidade entre motores de navegadores:

<Aside type="caution" title="Dica de IA: Valide o suporte multiplataforma para APIs avançadas">
Ao implementar manipulação de arquivos locais ou conexões com periféricos, assistentes frequentemente sugerem a File System Access API ou Web Bluetooth. Essas interfaces não são universais e deixam a aplicação inoperante no Safari e Firefox.

**Prompt:** *"Adicione detecção de compatibilidade com fallbacks universais da plataforma (como `<input type="file">` e `URL.createObjectURL`), assegurando que a aplicação funcione em todos os navegadores."*
</Aside>

---

## Sensores, Janelas e Controle do Sistema

Interfaces que controlam o comportamento da aba e reagem ao estado físico do dispositivo:

| Web API | Finalidade Principal | Interfaces Chave | Casos de Uso Típicos |
| --- | --- | --- | --- |
| **Screen Wake Lock API** | Impede que a tela do dispositivo escureça ou bloqueie | `navigator.wakeLock.request('screen')` | Receitas culinárias na tela, aplicativos de apresentação de slides, teleprompters. |
| **Window Controls Overlay API** | Integra a barra de título da janela desktop na área útil de exibição do PWA | `navigator.windowControlsOverlay` | Criar cabeçalhos personalizados elegantes em PWAs instaladas no Windows, macOS e Linux. |
| **Picture-in-Picture API** | Exibe vídeos ou elementos HTML em uma janela flutuante sobreposta | `videoElement.requestPictureInPicture()` | Reprodutores de vídeo e chamadas de videoconferência enquanto o usuário navega em outras abas. |
| **Web Locks API** | Coordena acesso concorrente a recursos entre abas e workers da mesma origem | `navigator.locks.request()` | Evitar que duas abas abertas sincronizem ou gravem dados conflitantes no IndexedDB simultaneamente. |
| **Broadcast Channel API** | Comunicação simples por broadcast entre diferentes abas e iframes da mesma origem | `new BroadcastChannel('app_channel')` | Deslogar o usuário em todas as abas abertas quando ele clicar em "Sair" em uma delas. |
| **Sensor APIs** | Acesso a sensores inerciais e de ambiente do aparelho | `Accelerometer`, `Gyroscope`, `AmbientLightSensor` | Jogos baseados em inclinação do celular, ajuste dinâmico de brilho e bússolas digitais. |

---

## Perguntas de revisão

Teste seus conhecimentos sobre o panorama estendido de Web APIs do navegador:

### Escolha e Cenários de Aplicação

1. **Qual a diferença fundamental de aplicação entre Local Storage e IndexedDB?**

<details>
  <summary>Possível resposta</summary>

  O `localStorage` é síncrono, armazena apenas pares simples de chave-valor como strings e possui limite pequeno (aproximadamente 5 MB). O `IndexedDB` é um banco de dados relacional/orientado a objetos transacional, totalmente assíncrono, indexável e projetado para suportar gigabytes de dados estruturados e arquivos binários (blobs).
</details>

2. **Qual é a vantagem da OffscreenCanvas API em relação à renderização em Canvas tradicional?**

<details>
  <summary>Possível resposta</summary>

  A OffscreenCanvas API permite transferir o controle de desenho (2D ou WebGL) para uma thread de Web Worker dedicada. Isso garante que renderizações pesadas e simulações gráficas não bloqueiem a thread principal de interface (UI) do navegador, preservando animações fluidas a 60/120 fps.
</details>

3. **Qual é a vantagem da View Transitions API em relação a bibliotecas JS de animação?**

<details>
  <summary>Possível resposta</summary>

  A View Transitions API opera diretamente no motor de renderização do navegador, capturando instantâneos visuais do estado anterior e do novo estado do DOM. Isso permite criar transições contínuas e fluidas a 60/120 fps sem recalcular posições ou criar árvores virtuais pesadas em JavaScript.
</details>

4. **Como o WebAuthn melhora a segurança em relação a senhas tradicionais?**

<details>
  <summary>Possível resposta</summary>

  O WebAuthn utiliza pares de chaves criptográficas assimétricas criadas diretamente no enclave seguro do hardware do usuário (Touch ID, Face ID, YubiKey). A chave privada nunca sai do dispositivo e o desafio de assinatura é vinculado à origem (domínio), tornando o login imune a ataques de phishing e vazamentos de banco de dados.
</details>

### Requisitos e Segurança

5. **Por que APIs como Web Bluetooth e Web USB exigem gesto do usuário e HTTPS?**

<details>
  <summary>Possível resposta</summary>

  Por segurança e proteção à privacidade do usuário. Comunicar-se com dispositivos físicos locais sem criptografia ou sem o consentimento intencional do usuário poderia permitir que scripts maliciosos invadam periféricos, capturem dados de sensores ou gravem dados não autorizados no hardware da máquina.
</details>

6. **Como a Screen Wake Lock API melhora a experiência do usuário em aplicações específicas?**

<details>
  <summary>Possível resposta</summary>

  Ela evita que a tela entre em modo de descanso ou bloqueie automaticamente enquanto o usuário está acompanhando uma atividade passiva, como ler uma receita na cozinha, acompanhar um cronômetro ou ler um roteiro durante uma apresentação.
</details>

## Referências

- [Web APIs Reference | MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/API)
- [W3C Standards and Technical Reports](https://www.w3.org/TR/)
- [WHATWG Living Standard](https://spec.whatwg.org/)
- [OffscreenCanvas API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/OffscreenCanvas)
- [Web Authentication API (WebAuthn) | MDN](https://developer.mozilla.org/en-US/docs/Web/API/Web_Authentication_API)
- [View Transitions API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API)
- [IndexedDB API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API)
- [Service Worker API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API)
- [Web Audio API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API)
- [WebCodecs API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/WebCodecs_API)
- [Compression Streams API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/Compression_Streams_API)
- [Web Bluetooth API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/Web_Bluetooth_API)

## Próximo tópico

[Visão geral do Guia de Web APIs](../../): retorne ao catálogo principal e às trilhas estruturadas de aprendizado.
