---
title: "Web APIs: Local Storage"
description: "Persistência simples no navegador com localStorage e serialização JSON."
course: web-api
sidebar:
  label: "Local Storage"
---
import FileTree from '@components/FileTree.astro';
import SourceCode from '@components/SourceCode.astro';

## Objeto localStorage

```js
const name = 'value';

localStorage.setItem('name', name);

console.log(localStorage.getItem('name'));
//=> value

localStorage.setItem('person', JSON.stringify({ name: 'John' }));

console.log(JSON.parse(localStorage.getItem('name')));
//=> { name: 'John' }
```

Alternativas Remotas

- [JSON Server](../../../packages/mock/json-server/)

{/*
- Supabase
- Firebase
 */}

## Monitor-app (CRUD)

![](/dw/imgs/w3c/local-storage/monitor-app-crud.png)

<FileTree>
{`
monitor-app/
    css/
        style.css
    js/
        components/
        data/
        lib/
        services/
        main.js
    public/
        vite.svg
    .gitignore
    index.html
    package-lock.json
    package.json
`}
</FileTree>

## Local Storage

<SourceCode path="examples/web-api/local-storage/monitor-app/js/data/seed.js" />

```js title="/codes/w3c/local-storage/monitor-app/js/services/storage.js"
import { v4 as uuidv4 } from 'uuid';

function storageInsert(key, value) {
  if (typeof value === 'object') {
    value = JSON.stringify(value);
  }

  localStorage.setItem(`@monitor-app:${key}`, value);
}

function storageSelect(key, isJSON = true) {
  let value = localStorage.getItem(`@monitor-app:${key}`);

  if (isJSON) {
    value = JSON.parse(value);
  }

  return value;
}

function load(resourse, data) {
  if (storageSelect('loaded', false) !== 'ok') {
    storageInsert(resourse, data);

    storageInsert('loaded', 'ok');
  }
}

function create(resourse, value) {}

function read(resourse, id) {}

function update(resourse, id, value) {}

function remove(resourse, id) {}

export default { load, create, read, update, remove };
```

### Create a host

```js title="/codes/w3c/local-storage/monitor-app/js/services/storage.js"
function create(resource, value) {
  const values = storageSelect(resource);

  value = { ...value, id: uuidv4() };

  storageInsert(resource, [...values, value]);

  return value;
}
```

```js
import Storage from './services/storage.js';

const host = {
    name: 'Google DNS',
    address: '8.8.8.8',
    mask: '255.255.255.0',
    version: 'v4',
  };

Storage.create('hosts', host);
```

### Read hosts

```js title="/codes/w3c/local-storage/monitor-app/js/services/storage.js"
function read(resourse, id) {
  const values = storageSelect(resourse);

  if (id) {
    return values.find((value) => value.id === id);
  } else {
    return values;
  }
}
```

```js
import Storage from './services/storage.js';

Storage.read('hosts');
Storage.read('hosts', 1);
```

### Update a host

```js title="/codes/w3c/local-storage/monitor-app/js/services/storage.js"
function update(resourse, id, value) {
  const values = storageSelect(resourse);

  const index = values.findIndex((value) => value.id === id);

  if (index >= 0) {
    value = { id, ...value };

    values[index] = {...values[index], ...value};

    storageInsert(resourse, values);

    return value;
  } else {
    return false;
  }
}
```

```js
import Storage from './services/storage.js';

Storage.update('hosts', { id: 1, value: 15000 });
```

### Delete a host

```js title="/codes/w3c/local-storage/monitor-app/js/services/storage.js"
function remove(resourse, id) {
  const values = storageSelect(resourse);

  const index = values.findIndex((value) => value.id === id);

  if (index >= 0) {
    values.splice(index, 1);
  }

  storageInsert(resourse, values);
}
```

```js
import Storage from './services/storage.js';

Storage.remove('hosts', 1);
```

## Front-end

```bash
$ npm i uuidv4 iconify-icon bootstrap
```

<SourceCode path="examples/web-api/local-storage/monitor-app/index.html" />

<SourceCode path="examples/web-api/local-storage/monitor-app/js/main.js" />

<SourceCode path="examples/web-api/local-storage/monitor-app/css/style.css" />

### Utilitários

<SourceCode path="examples/web-api/local-storage/monitor-app/js/lib/dom.js" />

<SourceCode path="examples/web-api/local-storage/monitor-app/js/lib/hosts.js" />

### Componentes

<SourceCode path="examples/web-api/local-storage/monitor-app/js/components/HostTableRow.js" />

<SourceCode path="examples/web-api/local-storage/monitor-app/js/components/HostForm.js" />

<SourceCode path="examples/web-api/local-storage/monitor-app/js/components/Modal.js" />

## Exemplo completo: Invest App (CRUD)

A mesma estrutura de CRUD do exemplo anterior, agora aplicada ao Invest App.

[Código Fonte](https://github.com/ifpb/pw2/tree/main/public/codes/w3c/local-storage/invest-app/)

<FileTree>
{`
invest-app/
├── css/
│   └── style.css
├── js/
│   ├── components/
│   │   ├── InvestmentCard.js
│   │   ├── InvestmentForm.js
│   │   └── Modal.js
│   ├── data/
│   │   └── seed.js
│   ├── lib/
│   │   ├── dom.js
│   │   ├── format.js
│   │   └── investments.js
│   ├── services/
│   │   └── storage.js
│   └── main.js
├── public/
│   └── vite.svg
├── .gitignore
├── index.html
├── package-lock.json
├── package.json
├── postcss.config.js
└── tailwind.config.js
`}
</FileTree>
### Implementação do serviço de storage

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/js/data/seed.js" />

```js title="/codes/w3c/local-storage/invest-app/js/services/storage.js"
import { v4 as uuidv4 } from 'uuid';

function storageInsert(key, data) {
  if (typeof data === 'object') {
    data = JSON.stringify(data);
  }

  localStorage.setItem(`@invest-app:${key}`, data);
}

function storageSelect(key, isJSON = true) {
  let data = localStorage.getItem(`@invest-app:${key}`);

  if (isJSON) {
    data = JSON.parse(data);
  }

  return data;
}

function load(resource, dataset) {
  if (storageSelect('loaded', false) !== 'ok') {
    storageInsert(resource, dataset);

    storageInsert('loaded', 'ok');
  }
}

function create(resourse, data) {}
function read(resourse, id) {}
function update(resourse, id, data) {}
function remove(resourse, id) {}

export default { load, create, read, update, remove };
```

#### Criar um investimento

```js title="/codes/w3c/local-storage/invest-app/js/services/storage.js"
function create(resource, data) {
  const datas = storageSelect(resource);

  data = { ...data, id: uuidv4() };

  storageInsert(resource, [...datas, data]);

  return data;
}
```

```js
import Storage from './services/storage.js';

const investment = {
  name: "Tesouro Selic 2029",
  value: 10000,
  origin: "Tesouro Direto",
  category: "Pós",
  interest: "100% Selic",
  created_at: "2021-09-10T00:00:00-03:00",
};

Storage.create('investments', investment);
```

#### Ler investimentos

```js title="/codes/w3c/local-storage/invest-app/js/services/storage.js"
function read(resource, id) {
  const datas = storageSelect(resource);

  if (id) {
    return datas.find((data) => data.id === id);
  } else {
    return datas;
  }
}
```

```js
import Storage from './services/storage.js';

Storage.read('investments');
Storage.read('investments', 1);
```

#### Atualizar um investimento

```js title="/codes/w3c/local-storage/invest-app/js/services/storage.js"
function update(resource, id, data) {
  const dataset = storageSelect(resource);

  const index = dataset.findIndex((data) => data.id === id);

  if (index >= 0) {
    data = { id, ...data };

    dataset[index] = { ...dataset[index], ...data };

    storageInsert(resource, dataset);

    return data;
  } else {
    return false;
  }
}
```

```js
import Storage from './services/storage.js';

Storage.update('investments', 1, { value: 15000 });
```

#### Remover um investimento

```js title="/codes/w3c/local-storage/invest-app/js/services/storage.js"
function remove(resource, id) {
  const dataset = storageSelect(resource);

  const index = dataset.findIndex((data) => data.id === id);

  if (index >= 0) {
    dataset.splice(index, 1);
  }

  storageInsert(resource, dataset);
}
```

```js
import Storage from './services/storage.js';

Storage.remove('investments', 1);
```

### Front-end

```bash
$ npm i @iconify/iconify
$ npm i preline
$ npm i @preline/overlay
```

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/tailwind.config.js" />

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/index.html" />

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/js/main.js" />

### Utilitários

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/js/lib/dom.js" />

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/js/lib/format.js" />

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/js/lib/investments.js" />

### Componentes

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/js/components/InvestmentCard.js" />

<SourceCode path="examples/web-api/pw2/local-storage/invest-app/js/components/InvestmentForm.js" />
