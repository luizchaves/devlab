---
title: "Web APIs: Drag and Drop API"
description: "Arrastar e soltar no navegador com eventos dragstart, dragover, drop e DataTransfer."
course: web-api
sidebar:
  label: "Drag and Drop API"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import HtmlPreview from '@components/HtmlPreview.astro';
import SourceCode from '@components/SourceCode.astro';

A **Drag and Drop API** permite criar interações de arrastar e soltar. Ela aparece em kanbans,
uploads, reordenação de listas e interfaces em que o gesto espacial ajuda mais do que um formulário.

## Objetivo

Entender o ciclo de eventos de drag and drop, usar `DataTransfer` para carregar dados entre origem e
destino, e diferenciar a permissão visual do drop do processamento final do item solto.

## Modelo da API

O fluxo depende de eventos encadeados. O destino só aceita o item quando o código cancela o
comportamento padrão em `dragover`.

| Evento ou objeto | Papel |
| --- | --- |
| `draggable="true"` | Habilita o elemento como origem de arrasto |
| `dragstart` | Define o que está sendo arrastado |
| `dragover` | Permite que uma área receba o item |
| `drop` | Executa a ação final quando o item é solto |
| `DataTransfer` | Transporta texto, IDs, arquivos ou outros dados |
| `dragend` | Permite limpar estado visual depois do gesto |

<Aside type="caution" title="O detalhe que mais quebra">
Sem `event.preventDefault()` no `dragover` do destino, o navegador não dispara o `drop` como você
espera. Esse detalhe é pequeno, mas faz parte do contrato da API.
</Aside>

## Exemplo com preview e código

O exemplo importado mostra a origem do arrasto, a área de destino e o uso dos eventos principais.

<HtmlPreview path="examples/courses/web-api/browser-web-apis/dragdrop.html" height="26rem" label="dragdrop.html" allowScripts serveFromPublic />

<SourceCode path="examples/courses/web-api/browser-web-apis/dragdrop.html" />

## Cuidados práticos

Drag and drop é útil, mas não deve ser a única forma de executar uma ação importante.

| Situação | Cuidado |
| --- | --- |
| Reordenar lista | Ofereça alternativa por teclado ou botões |
| Upload de arquivos | Valide tipo e tamanho também no servidor |
| Mover item entre colunas | Atualize o estado de dados, não só o DOM visual |
| Interface mobile | Teste suporte e considere Pointer Events |

## Executando

<Steps>
1. Abra o exemplo `dragdrop.html`.
2. Arraste o item para a área de destino.
3. Observe no código os listeners de `dragstart`, `dragover` e `drop`.
4. Remova temporariamente o `preventDefault()` de `dragover` e teste a diferença.
</Steps>

## Exercício

1. Adicione uma segunda área de destino.
2. Mostre uma classe visual enquanto o item estiver sobre uma área válida.
3. Guarde no `DataTransfer` o ID do elemento arrastado.

<details>
  <summary>Possível resposta</summary>

  No `dragstart`, use `event.dataTransfer.setData('text/plain', elemento.id)`. No `drop`, leia com
  `getData('text/plain')`, busque o elemento por ID e mova para o destino atual.
</details>

## Referências

- [HTML Drag and Drop API | MDN](https://developer.mozilla.org/en-US/docs/Web/API/HTML_Drag_and_Drop_API)
- [DataTransfer | MDN](https://developer.mozilla.org/en-US/docs/Web/API/DataTransfer)

## Próxima aula

[Fullscreen API](../fullscreen/): exibição de elementos em tela cheia.
