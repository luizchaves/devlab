---
title: "Projeto: MonitorApp"
description: "Aplicação prática de monitoramento de hosts com manipulação dinâmica do DOM, modais e persistência com LocalStorage."
course: web-api
sidebar:
  label: "MonitorApp (DOM e Storage)"
---
import { Aside, Steps } from '@astrojs/starlight/components';
import FileTree from '@components/FileTree.astro';
import SourceCode from '@components/SourceCode.astro';
import PackageManagerTabs from '@components/PackageManagerTabs.astro';

O **MonitorApp** é um projeto prático que demonstra a integração de múltiplas Web APIs: manipulação dinâmica do DOM, captura de eventos em formulários e modais, e persistência de dados no cliente utilizando a `localStorage` API.

## Objetivo do Projeto

Analisar a arquitetura de uma aplicação JavaScript moderna desacoplada em módulos (serviços de armazenamento, utilitários DOM e componentes visuais de interface), praticando as quatro operações de um CRUD completo sem dependência de servidor back-end.

---

## Estrutura de Arquivos

A aplicação é organizada seguindo a separação de responsabilidades em camadas:

<FileTree>
{`
monitor-app/
├── css/
│   └── style.css
├── js/
│   ├── components/
│   │   ├── HostForm.js
│   │   ├── HostTableRow.js
│   │   └── Modal.js
│   ├── data/
│   │   └── seed.js
│   ├── lib/
│   │   ├── dom.js
│   │   └── hosts.js
│   ├── services/
│   │   └── storage.js
│   └── main.js
├── index.html
└── package.json
`}
</FileTree>

---

## Código-Fonte e Componentes

### 1. Interface Principal (HTML)

O arquivo HTML define a estrutura da tabela e o container do modal de cadastro/edição:

<SourceCode path="examples/courses/web-api/local-storage/monitor-app/index.html" lang="html" title="index.html" />

### 2. Serviço de Armazenamento Local (`storage.js`)

Módulo responsável por ler, gravar e atualizar a chave no `localStorage` serializando objetos para JSON:

<SourceCode path="examples/courses/web-api/local-storage/monitor-app/js/services/storage.js" lang="js" title="js/services/storage.js" />

### 3. Componentes de Interface

#### Linha da Tabela de Hosts (`HostTableRow.js`)

Componente funcional que gera dinamicamente o nó de linha `<tr>` da tabela de hosts:

<SourceCode path="examples/courses/web-api/local-storage/monitor-app/js/components/HostTableRow.js" lang="js" title="js/components/HostTableRow.js" />

#### Formulário e Modal (`HostForm.js` e `Modal.js`)

<SourceCode path="examples/courses/web-api/local-storage/monitor-app/js/components/HostForm.js" lang="js" title="js/components/HostForm.js" />

<SourceCode path="examples/courses/web-api/local-storage/monitor-app/js/components/Modal.js" lang="js" title="js/components/Modal.js" />

### 4. Ponto de Entrada da Aplicação (`main.js`)

Orquestrador que conecta a leitura do storage, o envio dos dados e o evento de re-renderização da tabela:

<SourceCode path="examples/courses/web-api/local-storage/monitor-app/js/main.js" lang="js" title="js/main.js" />

---

## Como Executar o Projeto?

<Steps>
1. Acesse o diretório do exemplo em `examples/courses/web-api/local-storage/monitor-app/`.
2. Instale as dependências executando no terminal:

   <PackageManagerTabs />
3. Inicie o servidor de desenvolvimento: `npm run dev`.
4. Abra o endereço `http://localhost:5173` no seu navegador.
5. Cadastre novos hosts, atualize seus dados e verifique os dados persistidos na aba **Application -> Local Storage** das ferramentas do desenvolvedor (<kbd>F12</kbd>).
</Steps>
