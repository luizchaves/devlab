# express-router

Rotas separadas em modulos com `express.Router()`.

```bash
pnpm install
pnpm dev
```

Rotas disponiveis:

- `GET /users` — lista os usuarios
- `GET /users/:id` — busca um usuario pelo id

Documentacao: `courses/cstrc-jp-dw/projects/express-router`.
