# Segredo usado para assinar os tokens. Em producao, gere um valor aleatorio:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="troque-este-segredo"

# Tempo de vida do token, em segundos (1h).
JWT_EXPIRES_IN=3600

PORT=3000
